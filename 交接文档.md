# CarDash 项目交接文档

> 拷走时请**整个 `IPA` 文件夹一起拷** —— `_env` 里的 JDK / build-tools / git 是便携版，构建要用；
> `_env\ssh` 里的私钥推仓库要用（这两个目录都不能落下）。**整包约 2.2 GB**，
> 想瘦身就别带 `_refs\`（432MB，反编译素材）和 `_obsolete\`（389MB，历史包）—— 见 §7 开头。
> 本文档最后更新：**2026-09-25 夜 · 第九轮 + iOS 1.6.32 出包**（氛围灯内置 + 线序修复 +
> 退出导航快判；卡片嵌 App **已回退**；iOS 移除「设置 → 测试」三项模拟功能）。
> 🚩 **新开的 AI 会话：先读 §4 顶部那一段**（当前要装的包 / 等用户回报什么 / 接手路径），
> 再按需读 **§15**（氛围灯：实现与排查）或 **§16**（卡片嵌 App：调研 + 已回退的做法备查）。
>
> **路径写法**：文档里的 `<IPA>` = 项目根目录（`IPA` 文件夹本身）。
> 项目在别的电脑上换过位置（`d:\Desktop\IPA` ⇄ `C:\Users\Administrator\Desktop\IPA` 来回过三次），
> 所以**别写死盘符**；`_tools\*.py` 和 `CarDash\tools\*.py` 已全部改成按脚本自身位置定位，换机器不用改代码。
>
> 换电脑要做的三件事见 **第 7 节**；本机装了什么环境见 **第 10 节**。

---

## 1. 这个项目是什么

给深蓝车机（Deepal）做的一套**仪表盘投屏方案**，两块代码：

| 部分 | 位置 | 作用 |
|---|---|---|
| **车机端** | `CarDash/inject/`（Java）| 注入进原车机 APK，从车机内部取数据，开 HTTP 服务（端口 8765）对外发 |
| **iPhone 端** | `CarDash/ios/`（SwiftUI）| 连车机取数据，渲染仪表盘 + 高德官方导航 |

数据流：`车机内部信号 → 注入代码 → HTTP /state（JSON）→ iPhone 渲染`

---

## 2. 目录结构

```
IPA\
├─ CarDash\                 ← 主工程（git 仓库，remote: origin/main）
│  ├─ inject\src\com\cardash\inject\   ← 车机注入代码（.java）
│  ├─ solo\                            ← 独立包源码（**不注入任何东西**，见 §14）
│  ├─ ios\                             ← iOS 工程
│  │  ├─ CarDash\                      ← Swift 源码
│  │  ├─ project.yml                   ← xcodegen 配置（版本号在这）
│  │  ├─ Info.plist
│  │  └─ Podfile                       ← 高德 SDK 依赖
│  ├─ tools\build_local.py             ← Android 本地构建脚本
│  ├─ tools\build_solo.py              ← 独立包构建脚本（与上面那条完全独立）
│  ├─ tools\make_lantern_res.py        ← 氛围灯资源去表化（生成 Res.java，见 §15）
│  ├─ tools\port_lantern.py            ← 氛围灯源码批量改写（R.drawable → Res.*）
│  ├─ tools\refactor_lantern.py        ← 氛围灯面板化重构（多监听 + 宿主替换）
│  ├─ lantern\src\com\magiclantern\rgb\ ← **内置版氛围灯源码**（27 个 java + `Res.java`，见 §15）
│  ├─ tools\setup_env.py               ← 工具链下载器（_env 已有就不用跑）
│  ├─ keys\platform.pk8 / .x509.pem    ← 车机同款系统签名密钥（不入库，必须跟着文件夹走）
│  └─ .github\workflows\ios-ipa.yml    ← iOS CI（出 IPA）
├─ _env\                    ← 便携工具链：JDK17 / Android build-tools / platform / git
│  └─ ssh\cardash_ed25519   ← 推 GitHub 用的私钥（也要一起拷）
├─ _tools\                  ← 辅助脚本（check_swift.py 等，**不在 git 仓库里**）
├─ _apk\                    ← 反编译素材（从 `_refs\D.apk` 解出来的 dex/strings）
├─ 新-Deepal-CarDash-238.apk ← **要装的包①（新固件的车）**  ← 2026-09-25 夜最新
├─ 旧-Deepal-CarDash-239.apk ← **要装的包②（老固件的车）**  ← 2026-09-25 夜最新
│    ⚠️ 根目录**只有这两个能装的包**（`新-`/`旧-` 前缀）—— 用户 2026-09-25 定的规矩，见 §3 末尾
├─ _bases\                  ← **构建输入（别装！）**：`Deepal+ S05 v260925.apk`（新底包）、`旧底包-v26.0521.apk`（老底包）
├─ _lantern_src\            ← **氛围灯原始工程归档**（源码+资源+iOS 版，见 §15；`Res.java` 的生成源）
├─ _refs\                   ← 原始参考包（**不参与构建**）：`D.apk`、`原车导航.apk`、`后装导航.apk`
│    ├─ jadx_set\ / jadx_card\ ← 已反编译的桌面设置页 / 卡片窗口系统（§15、§16 的证据）
│    ├─ res_dump.txt        ← 基座资源表 dump（查 id 名用，如 `radioGroup`）
│    └─ card_scan.txt       ← 卡片/窗口/隐藏 API 的类名扫描结果
└─ _obsolete\               ← 历史归档：上一轮的包、独立包测试件（§14）、空壳 IPA
```

> `_tools\` 和根目录这些文件**不在 git 仓库里**（仓库只有 `CarDash\`），所以它们只靠"整个文件夹拷"来传递 —— 别单独 clone 仓库。

---

## 3. 怎么构建

### 车机 APK（本地，Windows）

```powershell
cd <IPA>\CarDash
# ① 新底包 —— 给「新固件」的车
python tools/build_local.py "..\_bases\Deepal+ S05 v260925.apk" ..\_env 240 v26.0925-cd11
# ② 老底包 —— 给「老固件」的车（每轮两个都要编，见本节末尾的规矩）
python tools/build_local.py "..\_bases\旧底包-v26.0521.apk" ..\_env 241 v26.0521-cd34
#  ↑ 上面两个号是"下一轮"该用的（当前已出到 新-238/cd10 + 旧-239/cd33，见 §4 版本表）
#                          ↑原车机包(同级目录)        ↑工具链   ↑versionCode ↑versionName
```

- 两个路径参数是**相对 `CarDash` 这个工作目录**算的，所以换盘符、换目录都不用改。
- 前提是本机能跑 `python`（装法见第 10 节）。
- 脚本会先做 AXML 写入器自检（必须 `ROUNDTRIP_OK`）、编译、转 dex、改写清单、zipalign、签名，
  最后校验证书 SHA-256 是否等于车机系统签名 —— 任何一步不对它自己就中止了。

产物在 `CarDash/dist/Deepal-CarDash.apk`，拷到根目录并改名交付。

> 🧹 **交付与目录规矩（2026-09-25 用户明确要求，以后每轮都照做）**：
> **每轮出两个包，都放根目录，用 `新-` / `旧-` 前缀区分**：
>
> | 文件名 | 用哪条底包（在 `_bases\`） | 装到哪台车 |
> |---|---|---|
> | `新-Deepal-CarDash-<code>.apk` | `_bases\Deepal+ S05 v260925.apk` | **新固件**的车 |
> | `旧-Deepal-CarDash-<code>.apk` | `_bases\旧底包-v26.0521.apk` | **老固件**的车 |
>
> * **两条线每轮都要编**（原话："旧版底包还是要改，同时改两个"）—— 别只编新的；
> * 版本号是**一条共享序列**，谁后编谁的号大 ⇒ 装包只看「谁大装谁」；
>   **跨线换（新↔旧）必须先卸载**（versionCode 变小 / 数据层不同）；
> * 上一轮的包**挪进 `_obsolete\`** 留档（用户原话："要留档就挪 `_obsolete\`"），别堆在根目录；
> * **没在用的底包**才删；**在用的底包统一放 `_bases\`**（那是构建输入，不放根目录）；
> * 参考用的原始 APK（`D.apk`、原车导航、后装导航）统一放 **`_refs\`**。
> ⇒ 根目录里能装的就**只有 `新-` / `旧-` 两个**，一眼看得出装哪个。
**模拟器（MuMu）可安装版**：命令末尾加 `--emu`（去 sharedUserId + 调试签名），
在用户运行的 MuMu（Android 15 x86_64，带 arm64 转译）实测能装、能跑、桥接能起。别把 `-emu` 包装车上。
**当前底包是 `_bases\Deepal+ S05 v260925.apk`（= 用户 2026-09-25 下午给的 `新版D.apk`，
`v26.0925.beta`，versionCode 220），新包 versionCode 必须 > 220。**
（`Deepal+ S05 v260924.apk` 是**上一版**新底包，用户 2026-09-25 明确说**不再用了** ⇒
**已按清理规矩删掉**（它的产物还在 `_obsolete\` 里）；
`_bases\旧底包-v26.0521.apk` 是更老的固件（原 `Deepal+_v26.0521.apk`），
**每轮也要用它出一个 `旧-` 包**（老固件的车用）—— 2026-09-25 起两个底包都在 `_bases\`。）
⚠️ 换底包之前**必须逐项核对反射目标**（见 §6 第 83 条），别直接编。
换底包时**出新旧两个包**：新底包产物命名 `新-Deepal-CarDash-<code>.apk`，
旧底包最后一个可用包命名 `旧-Deepal-CarDash-<code>.apk`，方便新包闪退时回退。
版本号是**各自递增、互不相干**的两条线：车机看 versionCode（`210`→`211`→…→`219`→**`221`**…），
iOS 看 `MARKETING_VERSION`。

### iPhone IPA（GitHub Actions）

**只能走 CI**（需要 macOS）。push 到 `main` 且 `ios/**` 有变化才触发：

```
仓库 → Actions → 最新一次运行 → 跑完去 Releases 下载
```

产出的是**未签名 IPA**，要用 Sideloadly / AltStore / TrollStore 自己签名安装。

⚠️ 只改了 `inject/`（车机）不会触发 iOS 打包 —— 这是对的，互不影响。

### 版本号怎么改

- iOS：`ios/project.yml` 的 `MARKETING_VERSION`（构建号由 CI 自动注入 `run_number`）
- 车机：构建命令里的 versionCode / versionName

---

## 4. 当前进度（接手先看这里）

### 下一步（2026-09-25，办公室 / 车上）—— 先看这一段

> 接手的人（或新的 AI 会话）先通读 §1~§6，再照这一节做。
>
> **一句话现状（2026-09-25 夜 · 第九轮 —— 包已出：`新-238`/cd10 + `旧-239`/cd33）**：
> ① 车机装 **`新-Deepal-CarDash-238.apk`（`v26.0925-cd10`）** ——
>    车上现在是 `新-236`/cd9，**直接覆盖装、不用卸载**；老固件机器装
>    **`旧-Deepal-CarDash-239.apk`（`v26.0521-cd33`）**；
> ② iPhone 装 **`1.6.32`**（提交 `d85e7db` → CI run #72 成功 →
>   Release `CarDash IPA 1.6.32 (build 72)`）：
>   `https://github.com/FanXia5280/cardash/releases/download/build-72/CarDash-unsigned.ipa`
>   —— 本轮**只删了「设置 → 测试」那三项模拟功能 + 高德模拟导航**，
>   其它一行没动（功能与 1.6.31 相同，见 §0.0b）；
> ③ 桌面设置里现在**只有一项**「氛围灯设置」（左侧列表最后一项，图标已对齐原厂样式），
>    点开在右侧区域直接使用 ⇒ **不用再单独装那个 apk，而且跟着桌面进程保活**。
>    **实现原理 + 怎么验 + 排查见 §15。**
>
> **第九轮做了两件事（2026-09-25 夜）**：
> * **回退"卡片自定义显示 App"**（第八轮刚做的东西）：用户说"作用不大" ⇒
>   删掉卡片上的「＋」按钮、桌面设置的「应用小窗」，为它加的两条签名权限也收回。
>   ⚠️ **做法没丢**，留在 **§16.6 / §16.7**（都标了"已回退"），哪天想捡回来照着做就行。
> * **修「氛围灯设置」的图标**：之前那一项**只有文字**、和旁边几项比很突兀 ⇒
>   现在尺寸直接照抄原厂项，而且用代码画图标兜底（任何车机都保证有图标）。
>
> **⏳ 等用户回报的三件事**（他装完 238 会看）：
> ① **颜色对不对** —— 连上灯直接看，**不要去改任何设置**（线序修复见 §15.6）；
> ② **退出导航是不是 5 秒内收**（快判 `QUICK_END_MS = 4000`，见下面第七轮那段）；
> ③ **「氛围灯设置」前面有没有图标、跟旁边一不一样**。
>
> **📌 新对话接手路径**：读这一段 → **§15**（氛围灯：实现 / 验证 / 排查 / 线序坑）→
> **§16**（卡片嵌 App 的调研与做法，**已回退，仅备查**）→ 需要构建看 **§3**。
> 代码侧**已改完、已构建、已自检，没有半成品**；`CarDash\` **未提交 git**
> （要提交就 `git add -A` 后按 §9 推；只动 `inject/lantern/tools` ⇒ 不触发 iOS CI）。
>
> **更早的轮次（第六轮起）**：第六轮把氛围灯内置进桌面设置；第七轮按实车反馈修了
> 线序/颜色 + 退出导航快判 + 做了卡片验证件；第八轮改成"在卡片上直接选 App"；
> 第九轮回退第八轮 + 修图标。**版本号只能往涨**：238 ← 236 ← 234 ← 232 ← 230，
> **跨底包或往回退**都要先卸载。
>
> **⏳ 第七轮（2026-09-25 夜）—— 用户实车反馈后的三处修改，包已出：`新-234`/cd8 + `旧-235`/cd31**
> 1. **氛围灯内置：功能全对，但颜色错** —— 用户 16:35/16:46 的日志把三行都打出来了
>    （`保活已挂上` / `入口已插入桌面设置（11 项）` / `面板已打开（容器 2037x1075px）`）⇒
>    **挂载链路完全成功**；但用户报「**调成红色显示成绿色**，去设置里改线序也没用」。
>    **根因（不是指令错，`LedCommand` 与原始工程逐字节相同）**：线序/点数这类设置是
>    **写在灯里**的，灯固件掉电/重连会回到默认线序，而 App **只在"用户手动改设置那一刻"
>    发过一次**，**连上时从不补发** ⇒ 重连后颜色必错，而设置页显示的还是他改过的值
>    （存的是 App 侧）⇒ 看起来"改了也没用"。
>    **修法**：`LedOutput.reapplyDeviceConfig()` —— 连上后按"用户手动设一遍"的顺序补发
>    **线序 → 灯带点数 → 开关 → 亮度 → 颜色**（发色用 Raw 版本，不打断渐变）；
>    `LanternPanel` 与 `LanternBootstrap` 两个连接回调都改成调它（详见 §15.6）。
> 2. **退出导航：从 15 秒 → ~4~5 秒**。诊断里【高德自己的"退出/结束导航"日志】**还是只有
>    我们自己的那行**（高德没打原话）⇒ 走"组合判据"这条路：新增 `QUICK_END_MS = 4000`，
>    条件是**活路线断了 ≥4 秒 **且** 最近一次 `EXTRA_STATE` 是空闲值（不是 8/9/24/25）**。
>    两个条件同时成立基本只有"用户点了退出导航"这一种场景（导航中引导广播 2 条/秒，
>    连停 4 秒本身就异常）。15 秒那份保留当兜底 —— 机制见 `AmapSignals.QUICK_END_MS` 的注释。
> 3. **卡片嵌任意 App：验证件已出**（用户说"插件市场没有这个功能，所以需要你改一下"）。
>    §16 的调研结论是"桌面自带这套但受插件市场权益控制"，而用户实测**插件市场里根本没有
>    这个插件**（只有 HUD 转向辅助影像 / S05 后台管理 / HUD 导航，¥9.9~36）⇒ 只能自己做。
>    **先把"最小验证件"做出来**：桌面设置里新增第二项「**应用小窗**」→ 选任意 App（含抖音）
>    → 在虚拟显示里拉起它、画面投到面板的 SurfaceView 上、触摸转发回去。
>    技术三件套：`DisplayManager.createVirtualDisplay`（`CAPTURE_VIDEO_OUTPUT`）、
>    `ActivityOptions.setLaunchDisplayId`（`INTERNAL_SYSTEM_WINDOW`）、
>    `InputManager.injectInputEvent`（`INJECT_EVENTS`）—— **这三条签名权限已加进构建流程**
>    （`inject_manifest.EXTRA_PERMISSIONS`，我们用 platform 签名 + system uid ⇒ 会被授予，
>    不会像 POST_NOTIFICATIONS 那样"永远 DENIED"，详见 §16.6）。
>    ⚠️ **这一步只是验证"能不能显示抖音"，不是成品**；成功之后才谈"把它做成真正的卡片"。
>
> 📌 **下一步要用户回报**：① 新包装完先看**颜色对不对**（不动设置就该对）；
> ② 退出导航是不是 **5 秒内**就收；③ 点「应用小窗」→ 选抖音 → 启动，**把面板上的状态文字
> 拍下来**（那几行就是成败证据：建虚拟显示 / 启动 / 有没有画面 / 能不能点）。
>
> 📌 **新对话接手路径**：读 §4 这一节 → **§15**（氛围灯：实现/验证/排查/线序坑）→
> **§16**（卡片嵌 App：调研结论 + 素材 + 验证件）。代码侧**已改完、已构建、已自检**；
> `CarDash\` **未提交 git**（要提交就 `git add -A` 后按 §9 推）。
>
> **第四轮修的（都已包含在 230 里）**：
> 🐞 **转向灯「关了还亮一两秒 / 右转切左转两边同时亮几秒」**：都是第三轮那个 **2.5 秒保持**
>    的锅（窗口太长：关灯后要等 2.5 秒才收；换边时另一侧还在窗口里）。
>    ⇒ 保持收到 **0.7 秒**（只够跨过闪动的"灭"相）+ 新增**换边互斥**（一侧亮起时，
>    另一侧若 300ms 内没亮过就立刻判灭 —— 300ms 取在"双闪交替间隔"和"真正换边间隔"之间，
>    不会误杀双闪）。见 §6 第 81 条。
> 🐞 **车机退出导航 IPA 还是不退出**：第三轮把判据换成"活路线"是对的，但 **30 秒窗口太长**。
>    本轮靠两条实测把窗口收到 **15 秒**，并把"判结束"从"驾驶模式变化那一刻"改成
>    **每一条广播都判**（拿每秒都在发的心跳广播当定时器）⇒ 检测延迟≈15 秒（原来最长 25~40 秒）。
>    见 §6 第 82 条。
>    ⚠️ 同时加了两块**取证**（下一轮也许就能把 15 秒变成 1 秒）：`/logcat` 的
>    【高德广播类型统计】上限 12→**24 种**；新增【**高德自己的"退出/结束导航"日志**】。
> 📦 `独立包A/B` 的实测结果**已经回来（关卡②③都通了）**，**不用再装**，见 §14.4。
>
> 📌 **第三轮实车（9-25 12:09~12:11，1 份一键诊断 + 1 张截图）拿到的关键实测**：
> 导航中 10001 是 **2 条/秒**（68 条 / 33 秒）、最长断流 **12 秒**；而车机**一退出导航
> 10001 就完全停**（12:09:19~12:09:50 车停着 31 秒里一条都没有）⇒ 用"活路线断多久"
> 当退出信号是可靠的，阈值才敢收到 15 秒。
>
> **第三轮（9-25 中午，`新-227`/cd4）修的两个 bug（都已验证有效）**：
> 🐞 **转向灯打了没反应**：真值语义**第二次猜错** —— 实际是 **1 = 灭、≥ 2 = 亮**
>    （打灯时属性跟着灯泡 1↔2 反复跳），上一版按"0 = 亮"实现 ⇒ 值 2 被判成"什么都不亮"。
>    见 §6 第 79 条。
> 🐞 **车机退出导航，IPA 不退出**：两个根因 —— ① **`EXTRA_STATE=8` 在停车不导航时也每分钟发**
>    （所以它不能当"在导航"的判据）；② **周期广播的载荷里夹带着 POI**，把目的地又点亮了。
>    判据换成"**引导广播里还有活路线**（还有多远/多久 > 0）"，且目的地**必须被会话背书**。
>    见 §6 第 77、78 条。
> 📦 `独立包A/B` 的实测结果**已经回来（关卡②③都通了）**，这一趟**不用再装**，见 §14.4。
>
> **第二轮实车（9-25 11:42~11:48，用户发来 1 份一键诊断 + 独立包 A 的输出）**：
> ✅ **独立包 A 装上了**：`myUid()=1000`、`READ_LOGS=有`、`logcat -d` 能读 ⇒ **关卡②过了**
> ✅ 手动导航 / 切目的地 / 坐标精度都还对（`dest` 6 位小数、`src=amap`、`nav.remain=2.5 公里`）
> 🐞 转向灯（打左/右/双闪都没反应，原始值历史里 1↔2 在跳）→ 见上，§6 第 79 条
> 🐞 退出导航（`EXTRA_STATE=8` 每分钟来一次 + 目的地被周期载荷重新点亮）→ 见上，§6 第 77、78 条
> ❓ **插件市场订阅还是没回报**（用户没测这一项）
>
> **第一轮实车（9-25 上午，两份完整诊断 `日志一.txt` 10:35 / `日志二.txt` 10:26）**：
> ✅ 手动点导航被识别（`dest` = `src: amap`、坐标 6 位小数）
> ✅ 语音导航也被识别　✅ 切目的地跟着换（用户口述；两份日志抓的是同一个目的地，日志本身看不出切换）
> 🐞 **转向灯绿光从连上车机起一直闪**（车上没打灯）→ 真值语义搞反了，见 §6 第 74 条
> 🐞 **车机退出导航后 iPhone 顶栏摘要还挂着**（用户截图红箭头）→ 见 §6 第 75、76 条
> ❓ 终点落点准不准**还没验**（用户这次没真开到目的地）
> ❓ ⑥退出导航、⑨超速红光、插件市场订阅 也没验
> 昨晚（9-24 深夜）连着做了 6 轮，都累积在这两个包里：
> 移植新底包（`CarS05InfoUtil`→`S05VehicleDataMonitor`、multi-dex）→
> 修**目的地坐标被舍到 2 位小数**（≈1.1km 误差，终点"只是接近但不准"）、
> **转向灯别名**（改成实车在推的 `vc_alias_turn_*_signal_on`）、加**退出导航**按钮 →
> 退出导航不再重建地图 → 清残留路口放大图 + 模拟转向灯/模拟超速开关 →
> 修**「车身信号类名写死新底包」**（旧底包装车后车速/档位/续航/转向灯全空，改成两个候选类依次试）→
> 加**一键复制全部诊断** → 插件市场签名核实（证书/公钥全部一致，不会被拒）。
> 上一轮（1.6.24 / cd24）的长途导航 + 手动导航识别修复也都在里面。
>
> 本机（`d:\Desktop\IPA`，另一台机器，用户名 `梵夏`）的环境已重新体检通过 —— 见 §7 第 0 步。
> ⚠️ **2026-09-25 复查：`core.sshCommand` 又被写成另一台机器的路径** ——
> 因为昨晚是在 `C:\Users\Administrator\Desktop\IPA` 那台机器上干活、今早整个文件夹拷回来了。
> **文件夹在两台机器之间来回拷是常态**，所以**每次拷回来第一件事：`python _tools\setup_git.py`**（见 §9）。
> 用户的工作方式：他自己在 GitHub 网页看 CI 结果，Agent 只负责本地构建车机包 + push iOS。

**0.0 上车测试清单（2026-09-25 第九轮 —— 车机 `新-238` + `旧-239` / iPhone `1.6.32`）**

**装哪个**（根目录就这两个包，看前缀）：
* **新固件的车** → **`新-Deepal-CarDash-238.apk`**（车上现在是 `新-236`/cd9，**直接覆盖装、不用卸载**）；
* **老固件的车** → **`旧-Deepal-CarDash-239.apk`**。
iPhone **不用动**；独立包 A/B **也不用再装**（结果已拿到，见 §14.4）。

> 📌 **第九轮 = 回退卡片功能**（用户："这个功能作用并不大了，卡片不用自定义显示其他软件了"）
> ⇒ 桌面设置回到**只有「氛围灯设置」一项**（没有「应用小窗」了，卡片上也没有「＋」按钮了），
> 为它加的三条签名权限也已**收回**（`CAPTURE_VIDEO_OUTPUT` / `INTERNAL_SYSTEM_WINDOW`；
> 只剩 `INJECT_EVENTS` —— 那是**原版底包本来就有**的）。做法留在 §16 备查，哪天想捡回来照着做即可。

**这一趟要测三件事（都是上一趟实测反馈后的修正）**：
1. **颜色对不对**（线序修复）：随便选个**纯红**，看灯是不是红的 —— **不要去改任何设置**，
   连上就应该是对的（原理见 §15.6）；顺便试绿/蓝各一次。
2. **退出导航是不是 5 秒内收**（快判）：车机导航中点「退出导航」→ 别碰手机 → 数到 5，
   顶栏摘要 + 导航视图应该没了；**顺手在 20 秒内点一次一键诊断**（看 `/log` 里有没有
   `导航会话结束（活路线已断 4 秒…）` 这种字样 + 【高德自己的"退出/结束导航"日志】那一段）。
3. **看一眼「氛围灯设置」那一项的图标**（第九轮改的，用户反馈"只有文字、看着突兀"）：
   现在它**前面有图标**了 —— 尺寸直接照着"关于我们"那几项量出来贴上去的，
   **和旁边一模一样**；而且图标是**代码兜底画的**（紫色渐变圆点），
   就算哪台车机解析不出内置的 vector 图标，也保证**不会只剩文字**。

> ✅ **已经验过、不用再测的**：①②③⑤⑦⑧（手动/语音导航、切目的地、坐标精度、转向灯、长途不掉、两导航不跳）、
> ⑩b 转向灯的延迟与换边、⑪ 退出导航（会自己退，等 ~15 秒）。
>
> **本轮只有一件事：内置氛围灯（下面 4 条，前三条约 3 分钟）**
>
> 1. **入口在不在**：车机 →「桌面设置」→ 左侧列表**最后一项**应是「**氛围灯设置**」
>    （图标是调色盘，样式和旁边原厂项一模一样）。
>    * 点它 → 右侧区域出现氛围灯界面（顶部"智能控制" + 连接状态，右边喇叭/电源两个圆钮）；
>    * 点回任意原厂项 → 右侧回到原厂内容；**再点回来 → 氛围灯界面还能出来**（可反复切）。
> 2. **蓝牙能不能控灯**（要开着灯）：「设备管理」（或右上角喇叭）→ 扫描到你的灯 → 连接；
>    连上后逐项试：**开关电源 / 亮度 / 颜色 / 自定义渐变**（确认功能没丢）。
>    * 右上角电源圆钮亮起成蓝色 = 开灯态。
> 3. **保活**：**不打开这个界面**，把车机重启一次 → 看灯是否**自动连上并恢复上次的渐变**
>    （氛围灯现在是跟着**桌面进程**活的）。
> 4. **顺手（可选）**：① 插件市场能不能订阅（问了三轮一直没结论）；
>    ② 车位 P 档时点「退出导航」，看是不是还是 15 秒才收 —— 这关系到下一轮要不要做
>    「停车加速收尾」（见 §15 末尾）。
>
> ⚠️ 氛围灯出问题**不影响原车功能**：入口是运行时插进去的，整段包在 try 里。
>    真有问题就把「桌面设置」那张截图 + 一键诊断发我 —— 诊断里会有
>    `氛围灯保活已挂上（跟随桌面进程）` / `氛围灯入口已插入桌面设置` /
>    `氛围灯面板已打开（容器 WxHpx）` 这几行（在 `/log` 桥接运行日志 段里，见 §15）。

| # | 要验的 | 怎么操作 | 期望 | 不对时抓什么 |
|---|---|---|---|---|
| ① | **终点落点准**（本轮重点） | 车机上随便设个目的地开始导航 | iPhone 的导航终点**和车机是同一个点**（不再是"只是接近"） | 一键诊断 |
| ② | **切目的地跟着换** | 导航中途在车机上**换成另一个目的地** | iPhone 路线**跟着换新目的地** | 一键诊断（看 `dest` 的 name/坐标有没有跟着变） |
| ③ | **手动 / 语音导航都能认** | ①手动点目的地开导航 ②语音说"导航到 X" | 两种都进导航视图 | 一键诊断 |
| ④ | **手机推送路线** | 手机高德「发送到车机」→ 开始导航 | iPhone 也跟上（上一轮存疑，多半也是坐标精度引起的） | **推完立刻**点一键诊断 |
| ⑤ | **转向灯绿光** | 依次打**左转向灯 / 右转向灯 / 双闪** | 左灯只闪左边、右灯只闪右边、双闪两边一起 | 一键诊断（看 `turn` / `turnRaw`） |
| ⑥ | **退出导航** | 导航中点「退出导航」（竖屏在总里程上面 / 横屏在右上角） | **立刻**回普通地图绿点，不卡、不留路口放大图 | 截图 |
| ⑦ | **不被改目的地 + 长途不掉** | 导航途中在车机上随便搜个地方（**别点导航**）；再跑长一点 | 目的地一动不动；导航不中途掉 | 一键诊断 |
| ⑧ | **两个导航一起开不跳** | 原厂导航 + 高德同时开 | 顶栏数字只跟高德那套 | 一键诊断 |
| ⑨ | **超速红光**（顺带） | 跑一段有测速电子眼并超速的路 | 两边冒红 | 一键诊断（看 `camera` / `limit`） |
| **⑩** | **转向灯真值语义定案**（第二轮重点，1 分钟） | 停在原地：**打左灯 5 秒 → 关 → 打右灯 5 秒 → 关 → 开双闪 5 秒 → 关**，然后**马上**点一键诊断 | 打左只闪左边、打右只闪右边、双闪两边一起、关掉**不闪** | 一键诊断（看 `/logcat` 里「转向灯（原始值变化历史）」那一段）|
| **⑪** | **退出导航自动化**（第二轮重点） | 让车机导航跑起来，然后**在车机上退出导航**，**别碰手机**，站着等 | 约 1 分钟内：顶栏摘要先消失，导航视图回普通地图 | 一键诊断（看 `nav.active` 变 false、`dest` 变空）|

> ⚠️ **①/② 的前提**：目的地坐标精度修复要两边都新才有效（车机端只舍坐标、iOS 端只按坐标去重，
> 缺一边都不行）。**cd25 起 + iOS 1.6.25 起就有，本轮的 `新-238`/`旧-239` 和 1.6.32 都含。**
> ⚠️ **⑤ 的前提**：转向灯别名按实车日志改成了 `vc_alias_turn_left/right_signal_on` + 真值语义按
> **0=亮 / 1=灭**（§6 第 74 条），**cd25 起才生效** —— 车上要是还留着 cd24 就一个绿光都不会有。
> ⑨ 超速红光需要**电子眼 + 车速**数据，没跑在有测速点的路上不会亮 —— 现在只能上路实测（见下面 0.0b）。

**0.0b ⚠️「设置 → 测试」已移除（2026-09-25 · 发布前，iOS 1.6.32）**

原来那三项调试功能**全都删了**，正式版里不再有：

* 「模拟转向灯」（左转/右转/双闪）、「模拟超速」、「模拟一条导航路线」+「清除模拟路线」；
* 跟着一起删的还有：`DashboardModel` 里的 `mockDest/mockNav/mockTurn/mockOverspeed/isSimulating`
  和 `sendMockDestination()/clearMockDestination()`，以及高德 SDK 的**模拟导航**那条路
  （`NaviKitNavView.simulate` → `startEmulatorNavi`）—— 现在只剩实时导航 `startGPSNavi`。

⇒ **转向灯绿光（⑩）和超速红光（⑨）只能上车实测**。想捡回来的话按上面这份清单反向加回即可：
`SettingsSheet.swift` 加一个「测试」Section + `DashboardModel` 加回那几个 mock 字段 /
`sendMockDestination()`，再给 `NaviKitNavView` 补回 `simulate` → `startEmulatorNavi`。

**0.0c 顺手确认：插件市场订阅还能不能用（2026-09-25 已核实，别再瞎担心）**

用户担心"改包重签后 `appSignatureSha256` 不匹配 → 服务端不下发 pack → 没法订阅插件"。
**已用 apksigner 逐项核对**（原版底包 vs 我们重签的包）：

| 项 | 原版 | 我们 | |
|---|---|---|---|
| 证书 SHA-256 | `c8a2e9bc…92ab8` | 同 | ✅ |
| 公钥 SHA-256 | `3d3df7dc…` | 同 | ✅ |
| 证书 SHA-1 / MD5 / DN / RSA2048 | — | 全部同 | ✅ |
| 验证方式 | 仅 v3 生效 | 仅 v3 生效 | ✅ |

⇒ **基于"签名证书/公钥"算的 `appSignatureSha256` 完全一致**（我们用同一个 `platform.pk8`），
按证书校验的服务器不会拒绝。新旧两版桌面证书都是 `c8a2e9bc`，所以 `旧-222` 一样成立。
唯一会变的是**整包内容哈希**（我们加了 dex/组件）——若对方校验整包哈希则必然失败，
但只要改包就无解。

**所以这一趟的动作**：装 `新-232`（或 `旧-233`）跑 §0.0 那批时，**顺手看一眼插件市场能不能正常订阅**
（点进插件市场、试订阅一个）—— 这就是最直接的验证。
> 上一轮用户装的是 `新-223`/cd2，**"插件市场能不能订阅"这一条一直还没回报**，别当成已验过。

**预案（仅当实测真的订阅不了才做）：改成独立 APK**
- 做法：独立包 + `sharedUserId="android.uid.system"` + 平台签名 ⇒ 真系统级应用，不被杀后台、
  能拿 `READ_LOGS` 等权限，**桌面本体一个字节都不动**（订阅 100% 安全）。
- ⚠️ 硬难点：车速/档位/续航/转向灯现在是**反射进桌面进程内部的类**读的，
  独立 APK 是另一个进程/ClassLoader ⇒ 那些类取不到、拿不到车身信号。
- 解法（选①，代价最小）：独立包声明 `android:process="com.deepalhome.launcher"`
  + 与桌面相同的 sharedUid ⇒ 组件跑在桌面进程里，**继续用它的类，数据全保留**；
  需上车验证一轮。解法②（自己绑 polymeric/virtualcar，逆 AIDL）工作量大，不首选。
- 会失去的：`S05Navi`（桌面内部的原生导航 IPC）—— 仅诊断用，不影响功能。

**0.1 上车要抓的 log —— 只要点一下（1.6.29 起）**

iPhone「设置 → 车机诊断」最上面那条 **「一键复制全部诊断（发我排查）」**：

- 一次抓完 `/state` → `/logcat` → `/diag` → `/log` → `/scan` 五段，每段带分隔标题
- 开头自动附上：**抓取时间 / App 版本 / 桥接版本 / 链路状态 / 车机地址 / 当前目的地**
  （以前每次都要追问"你装的哪个版本"，就是缺这段）
- **抓完直接进剪贴板** → 弹出后右上角显示「已复制」，去微信粘贴发出来就行

下面是分开看的入口，按需要点：

| 按钮 | 接口 | 我要从里面找什么 |
|---|---|---|
| **logcat 取数诊断** | `/logcat` | ⭐ 「高德导航广播」段的 `extrasAll()` = **高德每条广播的全部 key**；「目的地」段 = 来源/名称/坐标/命中规则 |
| **状态快照（JSON）** | `/state` | `dest`（含 `src`）、`nav`、`camera`、`turn`、`limit`、`src` 映射（`navIgnored` / `destVoiceIgnored`）|
| 车机运行状态 | `/diag` | 实时数据 + 信号来源 + 原生导航 IPC 探测 |
| 厂商属性扫描 | `/scan` | 车身信号全量（一般用不上）|
| 桥接运行日志 | `/log` | 启动过程（怀疑桥接没起来时才要）|

> 每个 log 顺手说一句**你当时做了什么操作**（尤其"手动点导航""两个导航一起开""手机推路线"这种）。

<details><summary>（旧做法：用手机浏览器打开 http://&lt;车机IP&gt;:8765/xxx —— 现在不必了，留档）</summary>

车机 IP 在「设置 → 连接状态 → 车机地址」里能看到（形如 `192.168.…:8765`）。
用手机浏览器打开下面几个地址、整页复制发回来就行（`/logcat` 和 `/diag` 是纯文本，很长没关系）：

| 编号 | 什么时候抓 | 抓哪个 | 要看什么 |
|---|---|---|---|
| **A** | 在**高德**里手动点导航、**刚起步**（最重要！） | `/logcat` | 「高德导航广播」段的 `extrasAll()` —— **这是高德每条广播的全部 key**，找有没有 `TO_POI_*` / `dest*` 之类；同时看「目的地」段的 `来源`/`命中规则` 是不是 `amap` |
| **B** | 高德导航中（跑起来之后） | `/state` + `/logcat` | `dest` 有没有值、`dest.src` 是不是 `amap`；`navSources` 相关诊断 |
| **C** | **原厂导航单独**导航中（高德不开） | `/logcat` + `/state` | 「内容像导航但不在白名单的通知」段有没有它；`src.navIgnored` 有没有东西 |
| **D** | **两个一起开**（用户习惯） | `/state` + `/logcat` | `src.navIgnored` 应写着被压住的那一路；顶栏数字实测是否稳 |
| **E** | 在高德里**只搜一个地方、不点导航** | `/state` | `dest` 必须**没变**（验证"搜索结果不会被当成目的地"） |
| **F** | 结束导航后等 1~2 分钟 | `/state` | `dest` 应消失（`src.dest` 显示「会话结束」）|

> 另外 `http://<车机IP>:8765/` 是状态页，`/diag` 是完整自检（含原生导航 IPC 探测段）。
> 抓 A 那一条的时候**务必是"手动点导航"**，不要在语音导航时抓 —— A 的目的就是
> 验证"不靠语音能不能拿到目的地"。

</details>

> ⚠️ **2026-09-24 自查踩的坑（跟这一节直接相关）**：上面那张表里让用户去 `/logcat`
> 看「目的地」段，可 `DestSignals.report()` **写了却没有任何调用点** ——
> 那段其实**从来没出现过**（死代码）。已经接上（`BridgeRuntime.logcatReport()` /
> `heartbeatText()`），并从 **cd24（219）** 起才有；**装旧包的话这段照样是空的，别以为没数据就是没识别到**。
> ⇒ 教训：**给排查用的输出，要在真机上确认它真的有内容**（同 §6 第 65 条）。

**0. 前提，以及先装两样东西**

* **前提**：`IPA` 文件夹是**整个**拷过来的 —— 本文档、`_tools\`、`_env\`（工具链 + SSH 私钥）
  都**不在 git 仓库里**（仓库只有 `CarDash\`）。少了它们：脚本跑不起来、车机包构建不了、也 push 不上去。
* **车机**：根目录 **`Deepal-CarDash-219.apk`（`v26.0521-cd24`）** —— 本轮的包。
  相对 cd21 新增：**高德广播里的目的地采集**（手动导航也能认）、导航会话看门狗、
  `/state` 的 `dest` 带 `src`、修 `/logcat` 的 extras dump、
  **导航字段来源仲裁**（原厂导航 + 高德同时开不跳）、
  **目的地诊断接线**（`/logcat` 和 `/diag` 里终于真有「目的地」那一段了）。装法见 §3。
  装完在「诊断 → 车机运行状态 → 桥接版本」确认是 **cd24**。
  （cd21 才是带「转向灯 + 限速 + 电子眼」那三类数据的版本，cd22~cd24 都是在它上面继续加的。）
* **iPhone**：GitHub Releases 里最新的应当是 **1.6.22**（提交 `adf3faa`，
  Release 名会是 `CarDash IPA 1.6.22 (build 61)` 之类；仓库是私有的，网页看 Actions / Releases）。
  > 不用 PAT 也能判断"CI 走到哪了"：CI 发布 Release 时会打 tag `build-<run_number>`，
  > 所以 `git ls-remote --tags origin` 看最新的 `build-N`，再把它指向的 commit 和 `main` 对比
  > （`build-59` → `2b09c41` 就是 1.6.20 这条）。
  > **tag 在 = Checkout/装环境/xcodegen/pod install/编译/打包/上传 artifact 全过了、发布步骤已执行**；
  > 唯一它证明不了的是"Release 里的 .ipa 附件本身传上去了没"（那步失败的极端情况也会留下 tag）
  > —— 去 Releases 页瞄一眼有没有 `.ipa` 附件即可。反之 **tag 没涨就说明这轮挂了**。

**1. 车上实测（每项都把"实际看到了什么"告诉用户）**

| # | 要验的 | 怎么测 | 期望 | 不对时发回什么 |
|---|---|---|---|---|
| A | **车机导航终点 → iPhone 自动开始导航**（今天重点） | 在车机上用**高德导航**随便设个目的地开始导航 | iPhone 自动切到导航视图，路线与车机一致 | iPhone「设置 → 诊断」里的 `dest` 段，以及 `/diag` 里「车机原生导航 IPC」段（见 §11） |
| B | 转向灯 → 两边闪绿光 | 打左/右转向灯、开双闪 | 左灯只闪左边、双闪两边一起 | 设置页里的 `turnRaw`（形如 `Light/TurnLeft=true`）—— 用来校准取值形态 |
| C | 测速电子眼 → 两边冒红 | 跑一段有测速的路并超速 | **只有测速类电子眼**（不是闯红灯/监控）才冒红 | 设置页里的 `camera` 行（`dist= type= speed=`） |
| D | 路口放大图 | 找个复杂路口（模拟导航也能试） | 图出现在**速度那一格**，不挡别的面板 | 截图 |
| E | 走过的路置灰 / 牵引线消失 | 导航一段 | 走过的路变灰；起点到终点的飞线不再出现 | 截图 |
| F | 流畅度 + 车标呼吸动效 | 进软件、开车、横竖屏来回切；再**切一次前后台**回来 | 车标一出现就在正确位置（不再先居中）；**绿点外面那圈绿晕在向外扩散+淡出、1.8 秒一轮**（1.6.21 才真的看得见，见 §6 第 60 条）；切前后台回来呼吸还在（第 61 条的自愈）；地图不卡 | 截图/录屏 |

**2. 代码上还欠的**（详见本文档末尾「待办 / 已知可优化」）

* 目的地链路还没彻底解决（§11 是挖 D.apk 的结论；`S05Navi` 已在采数据，先看 `/diag` 里有没有值）
* `driveViewEdgePadding:`（告诉高德"我们的面板挡住了哪块"，防止路线被压）—— 做不做等用户定
* pod 版本建议锁死（`AMapNavi-NO-IDFA`）
* 高德那**两个收费接口**（红绿灯倒计时 / 超速脉冲）没开，要显示得走商务合作工单

**3. 干活方式（别踩老坑）**

* 本机是 Windows：**改代码 → 本地构建车机包 → push 到 GitHub 触发 iOS CI**
  （本地编不了 Swift，只能靠 CI，见 §7）
* 动 iOS 代码后，push 之前**必须**跑 `python _tools\check_swift.py` ——
  它能抓「ObjC→Swift 命名」「形参遮蔽属性」这类**编译错误**，已经救过三轮 CI（见 §6 第 42~54 条）
* 车机包**本地能真编译**：
  `python tools\build_local.py "..\_bases\Deepal+ S05 v260925.apk" ..\_env <versionCode> <versionName>`
 （**每轮两个都要编**：老固件那条线把第 1 个参数换成 `"..\_bases\旧底包-v26.0521.apk"`；见 §3 末尾），
  必须看到 `ROUNDTRIP_OK` **且** `✅ 与车机系统签名一致` 才算过
* **不写死盘符**；每轮改动都要更新本文档（§12 有约定）；路径/环境/Python 装法见 §10

| 平台 | 版本 | 状态 |
|---|---|---|
| **车机（新固件）** | `新-Deepal-CarDash-238.apk`（`v26.0925-cd10`，底包 `_bases\Deepal+ S05 v260925.apk`）| ✅ 2026-09-25 夜 · 第九轮（**最新**）：内置氛围灯（`lantern/` 源码 + `LanternEntry` 挂进桌面设置，见 §15）+ **回退卡片自定义 App** + **修「氛围灯设置」图标**。本地真编 + `ROUNDTRIP_OK` + 系统签名一致（`c8a2e9bc…`）+ 产物已核对（新增 `classes3.dex`，含 `LanternPanel`/`Res`/`LanternEntry`/`LanternBootstrap`）。**车上原来是 `新-236`/cd9 → 直接覆盖装、不用卸载**；装完看「诊断 → 桥接版本」应是 **`cd10`**。上一版 `新-236`(cd9) = 卡片上直接选 App（**已回退**，见 §16.7） |
| **车机（老固件）** | `旧-Deepal-CarDash-239.apk`（`v26.0521-cd33`，底包 `_bases\旧底包-v26.0521.apk`）| ✅ 同一份代码同步重建（`classes2.dex`）+ 系统签名一致。**两条线每轮都出**（§3 末尾的交付规矩）；装完桥接版本应是 **`cd33`** |
| **iPhone** | **`1.6.32`**（提交 `d85e7db`）| ✅ **已出包**：Release `CarDash IPA 1.6.32 (build 72)`（`build-72` → `d85e7db`，run #72 成功）。**1.6.32 = 删掉「设置 → 测试」三项模拟功能（模拟路线/转向灯/超速）+ 高德模拟导航，为发布正式版**，功能与 1.6.31 相同（见 §0.0b）。上一个：1.6.31（`build-71` → `eac44ad`）= 只改 App 名字（→ 深蓝仪表盘）。1.6.30（`build-70` → `700cf33`）= 手点「退出导航」后顶栏摘要也一起收掉 + 收路线去抖 20 秒→10 秒（配合车机 cd26/cd3 的"车机退出导航 → IPA 自动退出"，见 §6 第 75、76 条）；1.6.29（`build-68`）= 一键复制全部诊断 |
| **独立包测试件** | `独立包A-系统uid.apk` / `独立包B-普通.apk`（各 20KB，包名 `com.cardash.solo`）| ✅ **2026-09-25 真车实测完毕（A 装上了）**：`myUid()=1000`（system）、`READ_LOGS=有`、`logcat -d` 能读 ⇒ **关卡② ✅ 通过**（Android 14 允许新装应用用平台签名加入 `android.uid.system`）；`polymeric_service` 拿到 binder、4 个服务绑定成功并拿到接口描述符 ⇒ **关卡③ 通路已探明**。详见 §14.4 / §14.6。**不用再装**（`独立包B` 也没必要）|

**版本实况（实测记录，别按印象走；开头两条是 9-23 的历史快照）**

- 根目录的车机包（**这一段是 2026-09-23 的实测快照，不是最新的**；最新的看下面两条）：
  `210`=`cd15`、`211`=`cd16`、`212`=`cd17`、`213`=`cd18`、`214`=`cd19`、`215`=`cd20`、`216`=`cd21`。
  实测 **`213` 的 `classes2.dex` 与当前源码的构建结果逐字节一致**（210 的不一致，
  说明它是更早的源码产物）；**`216` 在 2026-09-24 已整包复核：1656 个条目 + `classes.dex`/
  `classes2.dex`/`AndroidManifest.xml`/`resources.arsc` 与本地重编结果逐字节相同**。
  **源码层面最新的永远是对应最后那个号的那个包。**
- **2026-09-24 深夜起版本号是一条共享序列**（都是 `com.deepalhome.launcher`），**当前最新**：
  老固件线 `219`(cd24) → `222`(cd25) → `224`(cd26) → `226`(cd27) → `228`(cd28) → `231`(cd29) → **`233`(cd30)**；
  新固件线 `221`(cd1) → `223`(cd2) → `225`(cd3) → `227`(cd4) → `229`(cd5) → `230`(cd6) → **`232`(cd7)**。
  装包只看「谁大装谁」；**跨线换（新↔旧）或往更小的号退 = 要先卸载再装**（versionCode 变小装不进去）。
- **底包换过三次，别搞混**（2026-09-25 现状；两个在用的底包都在 **`_bases\`**）：
  * `_bases\旧底包-v26.0521.apk`（原 `Deepal+_v26.0521.apk`）—— **老固件**
    （`CarS05InfoUtil` 时代）⇒ 出 `旧-` 包（当前 `旧-233`/cd30）。
  * `Deepal+ S05 v260924.apk`（v26.0923.beta）—— 上一版新底包，**已作废并删除**
    （用户明确不用了；用它出的包 `新-225/227/229` 都在 `_obsolete\`）。
  * **`_bases\Deepal+ S05 v260925.apk`（v26.0925.beta）= 用户 2026-09-25 下午给的 `新版D.apk`
    = 现在的当前底包** ⇒ 出 `新-` 包（当前 `新-232`/cd7）。它把数据层类挪了包
    （`CacheCarS05Info` 在 `com.deepalhome.launcher.carinfo` 下、取值类仍是
    `util.s05.S05VehicleDataMonitor`），其余反射目标不变 —— 核对结论见 §6 第 83 条。
  车上到底装的哪版，看「诊断 → 车机运行状态 → 桥接版本」。
  另外根目录那两个 `独立包A/B` **不是车机包**（包名是 `com.cardash.solo`，见 §14），
  和这条序列无关、**别拿去覆盖装桌面**。
- **2026-09-24 换底包了**：旧底包 `Deepal+_v26.0521.apk`（versionCode 约 212）被换成了
  **`Deepal+ S05 v260924.apk`（`com.deepalhome.launcher` v26.0923.beta，versionCode 220）**，
  包名、系统签名（`c8a2e9bc…`）都没变，但**车机数据层重构了**（`CarS05InfoUtil`→`S05VehicleDataMonitor`、
  `psGetValueSync` 变异步 `psGetValue`、底包变 multi-dex）。移植后出的包是 **221 / `v26.0923-cd1`**
  （文件名 `新-Deepal-CarDash-221.apk`），旧底包最后一个包是 **219 / cd24**（`旧-Deepal-CarDash-219.apk`，
  留着当回退包）。两条版本线**互不相干**：旧线 210~219，新线从 221 起。
- **iOS 版本号那件事（已修）**：`2f7cb89` 的提交信息写着「iOS 1.6.8」，但该提交只改了
  `AMapSupport.swift` 和 `NaviKitView.swift`，`ios/project.yml` 的 `MARKETING_VERSION` 还是 `1.6.7`
  ⇒ 那次 CI 出的 Release 叫「CarDash IPA 1.6.7 (build N)」，App 里「设置 → 版本」也显示 1.6.7。
  2026-09-23 已补提交 `5049b9b` 跳到 **`1.6.8`** 并 push，此后 CI 出的包才是真的 1.6.8。
  ⇒ **以后动 iOS 版本，提交信息和 `project.yml` 要一起改**，改完先跑 `_tools\verify_ios_ver.py`（它会四方对账）。
- ~~根目录的 `CarDash-unsigned.ipa` 只有 5 个 zip 条目 / 141KB，**是空壳占位、不是真产物**。~~
  （2026-09-25 清根目录时挪进 `_obsolete\` 了 —— 它就是个占位空壳，**真 IPA 只能去 GitHub Releases 下**。）
  真 IPA 只能去 GitHub Releases 下。
- ⚠️ **2026-09-25：仓库改成公开了** —— 起因是 GitHub Actions 的**计费**：
  「The job was not started because recent account payments have failed or your spending
  limit needs to be increased」⇒ **job 根本没启动**，所以那轮 tag 不会涨（别误判成"构建挂了"）。
  私有仓库的 macOS runner 按 **10 倍**扣分钟（免费额度 2000 分钟 ≈ 200 分钟 macOS），
  我们一路跑了 68 轮，额度/账单到顶了。**转公开后 Actions 免费用**，问题消失。
  ⇒ **公开前必须确认没有密钥入库**（已确认：仓库只跟踪 50 个文件，`CarDash\keys\platform.pk8`
  和 `_env\ssh` 都**不在**里面，安全）。
- ✅ **仓库公开后，CI 状态可以直接读 API 了**（不用 PAT、不用再靠 tag 猜）：
  ```powershell
  $r = Invoke-RestMethod 'https://api.github.com/repos/FanXia5280/cardash/actions/runs?per_page=4' `
       -Headers @{'User-Agent'='cardash-check'}
  $r.workflow_runs | Select-Object run_number, status, conclusion, created_at
  ```
  （未认证调用有每小时 60 次限流，够用。以前的 `_tools/check_run_steps.py` 那几个脚本
  是为"私有 + PAT"写的，现在这条路更简单。）
- ⚠️ 公开后**高德的两个 Key 就成了公开信息**（在 `ios\CarDash\UI\AMapSupport.swift` 的
  `AMapConfig` 里）：`iOSKey` 要确保在高德控制台**绑定了 Bundle ID**（`com.cardash.dashboard`）；
  `webKey` 是当初给"自绘路线"的 Web 算路接口用的，**那整块 2026-09-24 已删** ⇒ 大概率没用了，
  建议在高德控制台**删掉/重置它**（免得别人拿去刷配额）。
  2026-09-25 已确认 `webKey` **零调用点**，从源码里删掉了（提交 `700cf33`）。
- ⚠️ **空提交不会触发 CI**（2026-09-25 我自己踩了）：workflow 里有 `paths: ios/**` 过滤，
  而 `git commit --allow-empty` **没有任何文件变更** ⇒ 路径过滤不匹配 ⇒
  **一次 run 都不会有**（tag 不涨，Actions 页面里连记录都没有）。
  要重新触发只能推**真正的 `ios/**` 改动**（实在没有就改一句注释）。
  ⇒ 判断"到底触发了没有"：**tag 有没有涨**，或者去 Actions 页面看有没有这条 run。
- ✅ **本机读不懂 GitHub API 时的正解**（2026-09-25 实测）：这台机器
  `Invoke-RestMethod api.github.com` **超时**（跟封 22 端口同一个网络问题），
  但可以用**工具侧的 web_fetch 抓 API 拿结果**（不走本机网络）：
  `https://api.github.com/repos/FanXia5280/cardash/actions/runs?per_page=3`
  ⇒ 能直接看到 run_number / head_sha / status / conclusion。
  本次就是这样确认的：**run #69（`924ec97`）6 秒就 failure**（就是你贴的那条计费提示 ——
  job 没启动，run 记录还是会建、结论记成 failure），而 **run #70（`700cf33`）= success**。
  ⇒ 查 CI 有三条路，按顺手程度：**web_fetch 抓 API** > tag（`git ls-remote --tags`）> 网页。

### 已解决的功能

- 档位、车速、总里程、续航：**实时推送源**（不再读滞后快照）
- 歌词：直接复用车机 D.apk 已解析好的时间线（同进程反射读取）
- 专辑封面：切歌时清空旧图，不再串上一首
- 导航：**接入高德官方导航 SDK**（`AMapNaviDriveView`），原版路线 + 红绿灯倒计时 + 电子眼
- 横竖屏两套版式；比例尺每档可点；停车时车头对齐路线方向
- 设置页可看版本号（App 版本 + 桥接版本）

### 更早（iOS 1.6.9 / 车机 cd18，2026-09-23 晚）

**车机端（必须重装 APK 才生效）**

- 🐞 **修掉「IPA 目的地乱跳」的根因**：`DestSignals` 原来"猜三种坐标写法一起试"，
  其中两条正好命中**各种 App 上报 GPS 位置**的日志（`{"data":{"lat":..,"lon":..}}`、
  `gps:GPS(lon=..,lat=..)`）→ 目的地变成了车机自己几公里前的位置，IPA 于是画出一条
  完全不同的路线，而且车一动就重算。现在只认语音 NLU 日志里的 `"dest"` / `"destNavi"`，
  且坐标必须紧跟在目的地名字之后 5 分钟内出现。**别把那些 GPS 写法加回来。**
- 新增 `S05Navi.java`：反射读 D.apk 的**车机原生导航 IPC**（`NaviIpcBridge` / `HudNaviManager`），
  每秒采一次、并支持"主动查终点"。结果进 `/diag` 的「车机原生导航 IPC」段。
  这一版是**探测器**：先拿真实字段格式，再决定怎么用它当目的地（见第 11 节）。
- 修「导航信息每分钟闪一次」：高德 `EXTRA_STATE` 在导航中会 **8 → 40 → 8 每分钟翻一次**，
  老代码一见"空闲"就把 navTurn/navEta/... 全清掉。现在改成「60 秒没有引导广播才算导航结束」。
- `/state` 新增 `carNav.line1/line2`（车机自己 HUD 上的两行字，只做对照）。

**iPhone 端（1.6.9 已发布，用户已装上实测）**

- 移除底部的**转向卡**（原来显示「↗ 进入 大成路北段」）—— 转向看车机屏幕。
- 移除**比例尺**（`− 25米 ＋`，原 `ZoomPill`）：导航视图自己会缩放，那尺子只管非导航地图。
- 顶栏导航摘要的图标从**转向箭头**换成**终点旗**（`flag.checkered`），不再显示左右转。
- **竖屏**下总里程挪到原来比例尺的位置（右下角）；档位留在左边。
- **日夜模式自动**：导航视图用 SDK 的 `mapViewModeType = 2 (DayNightAuto)`（按日出日落）；
  非导航地图按本地时间窗（6:30~19:00 白天）切 `.standard` / `.standardNight`；
  栅格兜底地图也跟着切。以前是写死的白天样式 → 就是"晚上地图还是白的"。
- 算路加了安全阀：车机导航没开着就不算路线；目的地离车头 <150 米（"把自己的位置当终点"
  的典型症状）也不画路线 —— 宁可不画，也不画一条和车机不一样的。

### 最近一轮（iOS 1.6.19 / 车机 cd21，2026-09-24 深夜）—— **两边都更新了**

用户当晚最后三条要求：

1. ✅ **删掉「自绘路线」整块**（用户原话："这个版本已经用高德官方的了，就没必要再自绘增加功耗"）
   * iOS 删了：`AMapNavView` 的 `route`/`segments` 参数与 polyline 绘制、栅格兜底
     `NavMapView` 的路线、`DashboardModel` 里的 `amapRoute`（高德 Web API 算路）/
     `appleRoute`（MKDirections 兜底）/`parsePolyline`/`statusIndex`/`routeBearing`/
     `route`/`routeSegments`，以及 `RouteSegment` 类型和 `MusicPanel` 之外的 `displayMusic`。
     现在非导航地图只画**车标 + 目的地 pin**，路线/剩余时间距离全交给导航视图。
     `syncDestination()`（原 `replanRoute`）现在只做一件事：解出目的地坐标写进 `routeDest`。
   * 车机侧没动这块（车机高德导航本来就有自己的路线）。
2. ✅ **删掉车机音乐整块**（用户："以后也不会添加这些功能"）
   * 删文件：`MediaSignals.java`、`Lyrics.java`、`LauncherLyrics.java`。
   * `StateHub` 去掉 `mTitle/mArtist/mAlbum/mPlaying/mPosition/mDuration/mCover/mLrc`、
     `clearMusic()`、JSON 里的 `"music"` 对象与 `mediaError`；
     `BridgeRuntime` 去掉媒体启动/停止与相关诊断；`StatusPage` 去掉「音乐」那一行。
   * iOS 去掉 `MusicState`、`CarSnapshot.music`、`displayMusic`、`MusicPanel`。
   * ⇒ 车机端出 **cd21**（根目录 `Deepal-CarDash-216.apk`）。
3. 🐞 **修「呼吸光晕没对准圆心」**（用户实测："绿色呼吸特效在一旁呼吸"）
   * 根因：`CAShapeLayer` 的 `bounds` / `position` **默认都是 0**，而我只设了 `path` ——
     于是 `transform.scale` 是**绕父层左上角**缩放的，看上去就是光晕在一边呼吸。
   * 修法：每层都设 `bounds`（圆的外接矩形）+ `position`（视图中心），
     `path` 相对自己的 `bounds` 画 ⇒ 缩放才绕自己的圆心。

> ⚠️ **这一轮的教训（已写进 §6 第 57 条）**：这轮用脚本"整块删代码"，
> 脚本按花括号配对删，遇到「同一行既有 `{` 又有 `}`」「注释行里没有花括号」就会多删/少删，
> 结果 Swift 和 Java **都留下了结构残体**（多一个 `}`、孤立的 `.append(...)`、空 `try {}`）。
> **删完必须立刻跑** `check_swift.py`（Swift）、`java_lint.py` + `tools\build_local.py`（Java 本地真编译）。
>
> **2026-09-24 补丁（iOS 1.6.20，提交 `2b09c41`）**：用户实测反馈「**非导航态看不到绿点了**」——
> 就是上面第 1 条**删多了**：车标（`carPin`）的创建代码正好**夹在**路线绘制块和目的地 pin 之间
> （三个东西同一区间），被区间删除一起带走。⚠️ 这种错**编译和自检都发现不了**
> （`carPin` 声明还在、`viewFor` 还在，只是永远不会有这个标注），已补回。
> 教训写进 §6 第 58 条：整块删代码之后**必须逐条功能过一遍**，不能只看编译绿。

### 上一轮（iOS 1.6.18，2026-09-24）—— 只动 iPhone，车机仍 cd20

用户要求：非导航态那个绿点**要复刻苹果「查找」App 的呼吸动效**（之前只是个静态实心圆）。

改法：车标从"一张静态 `UIImage`"换成**自定义 `MAAnnotationView` 子类 `CarDotView`**
（`AMapSupport.swift`），用三个 `CAShapeLayer` 叠出来：

* `halo`：外层光晕，做「**放大 + 淡出、1.8 秒、无限循环**」的呼吸动效；
* `ring`：中层白圈；
* `core`：里层实心圆（**绿=在线 / 灰=不在线**）。

呼吸动效交给 Core Animation（跑在渲染服务上，**不吃主线程**，不影响地图帧率 —— 用户明确要求别把地图拖卡）。
⚠️ 这个子类**故意不写 `init`**：ObjC 的 `init(annotation:reuseIdentifier:)` 空值标注一写错就编译不过
（本项目在这类地方栽过三次），所以走继承来的初始化器，再调一次 `prepare(online:)` 建层。

### 上一轮（iOS 1.6.17，2026-09-24）—— 只动 iPhone，车机仍 cd20

用户带两张截图反馈：

1. 🐞 **进软件后车标（绿点）先在屏幕正中、几秒后才落到靠下的正确位置**
   （截图一 → 截图二）。
   根因：**非导航地图**的车头定位是拿 `MAMapView.convert()` 反算的，而地图刚建出来
   **还没渲染好时 `convert` 会返回垃圾坐标** ⇒ 原来那段
   `if abs(dLat) < 0.02`（"偏移量必须合理"的兜底）不成立就**直接放弃偏移**，
   车于是停在正中，等下一次定位更新（几秒后）再算才成功。
   **横屏是同一段代码，一样有这个问题**（用户也问了这点）。
   改法：`applyCamera` 改成返回「有没有真偏移成功」，失败就交给新的 `reapplyCamera`
   **排队重试**（0.1~0.8s 退避、最多 8 次，成功即清零）—— 正常情况第一帧就成，
   重试根本不会发生。（启动遮罩那 1.8s 本来就盖着这段，现在再加上"首帧就对"，两头保险。）
2. 🐞 **模拟导航会闪一下北京**（用户："很快、基本无感，但还是被我看见了"）。
   根因：导航**真正开始之前**，`AMapNaviDriveView` 显示的就是 SDK 默认位置（北京），
   而算路 + 起步要 1 秒左右，这一秒就是那一下。
   改法：新增 `onStarted` 回调（来自 `didStartNavi`）；DashboardView 的导航分支改成
   **起步前垫一张非导航地图**（位置、车标都对）挡着导航视图，`didStartNavi` 一来
   就撤掉垫底、把导航视图显出来 ⇒ 用户看不到那一帧。
   目的地清空时重置该状态（`onChange(of: model.routeDest == nil)`）。

### 上一轮（iOS 1.6.16，2026-09-24）—— 只动 iPhone，车机仍 cd20

用户要求接入**路口放大图**（官方文档《导航实时数据获取》），并明确摆放规则：
「竖屏显示在速度那里、遮挡住速度都行，横屏也是；过了路口或高德不发这种信号时正常显示速度。
**只可以遮挡速度，不能遮挡其他元素**」。

两个坑都在头文件里写着，已写进代码注释：

* **`v.showCrossImage = false` 是前提** —— 头文件原话：「10.0.2版本开始，自定义 View 中
  如果设置了 showCrossImage 为 YES，回调中 crossImage 为 nil」。而它**默认就是 YES**，
  不设成 false 就拿不到图。
* 图走**数据回调**（`AMapNaviDriveDataRepresentable`，协议内全是 @optional）：
  `driveManager:showCrossImage:` 给图（比例固定 **25:16**）、
  `driveManagerHideCrossImage:` 收图。所以 `NaviCoordinator` 也 conform 了这个协议，
  并**在算路之前** `addDataRepresentative(self)`（官方文档要求"注册必须在算路前"）。
* 摆放：新视图 `SpeedSlot` 把「速度表」和「放大图」做成**同一格** ——
  图高锁 `150*scale`，比速度表本身（约 `181*scale`）矮 ⇒ 有没有图，这一格高度都不变，
  **布局不抖、别的元素不挪位**；竖屏居中、横屏贴左（与速度表一致）⇒ 就是压在速度上。
  这就是"只遮速度、不挡别的元素"的实现方式。

> **2026-09-24 补丁二（仍是 1.6.16，提交 `270706d`）**
>
> * 🐞 **CI 第三次挂**（同一类问题：ObjC 名字直接照抄到 Swift）：
>   `error: 'driveManager(_:showCrossImage:)' has been renamed to 'driveManager(_:showCross:)'`
>   —— Swift 的"省略与参数类型重复的词"规则：参数类型本身就是 `UIImage`，
>   所以 `showCrossImage` 里的 **Image** 被省掉。**按头文件照抄方法名就会编译失败**。
>   修法：写成 `func driveManager(_ d: AMapNaviDriveManager, showCross crossImage: UIImage?)`。
>   （隐藏那个 `driveManagerHideCrossImage:` 不用改 —— 它的参数是 manager，不重复 ✅）
> * ✅ 已在 `_tools\check_swift.py` 里加了**专项检查**（第 4 条）：只要**外部标签**以
>   "参数类型去掉模块前缀后的名字"结尾（比如 `showCrossImage` + `UIImage`），就提示
>   "Swift 里这个名字多半是 `showCross`"。用合成文件验证过能抓到 ✅。
> * ✅ 按要求**关掉牵引线**：`v.showVectorline = false`
>   （头文件：「是否显示牵引线，默认YES」—— 就是起点到终点那根飞线）。

> **2026-09-24 补丁（1.6.15，提交 `251765b`）**
>
> * 🐞 **CI 又挂了一次**（这次是我加资源目录引起的）：
>   `ios/CarDash/Assets.xcassets: error: None of the input catalogs contained a matching
>   stickers icon set, app icon set, or icon stack named "AppIcon"`。
>   原因：**工程里一旦有 `Assets.xcassets`，actool 就会跑**，而 XcodeGen 默认给 App target
>   设了 `ASSETCATALOG_COMPILER_APPICON_NAME=AppIcon` —— 没有这个名字的图标集就**直接 error**
>   （之前一张图都没有、actool 不跑，所以一直没暴露）。
>   已补 `AppIcon.appiconset`（深色底 + 深蓝车标，1024×1024 **不透明** —— App Store 不许带 alpha），
>   生成脚本 `%TEMP%\mkicon.py`。顺带这 App 现在终于有自己的图标了。
> * 🔧 **红色脉冲的语义改了**（用户提问：高德是"会被拍限速的电子眼"才冒红，不是超路段限速就冒）：
>   车机侧开始下发 `camera{dist,type,speed}`（`AmapSignals` 解析 `CAMERA_DIST/TYPE/SPEED`，
>   每条引导广播覆盖一次、没有就置空），iPhone 的 `isOverspeed` 改成
>   **前方 300m 内是测速类电子眼**（高德枚举 `AMapNaviCameraType`：0 测速 / 8、9 区间测速，
>   或该电子眼自带限速值）**且车速超过该电子眼的限速**（它没给就用路段限速）+2km/h 才冒。
>   数据不全一律不冒（宁可不报、绝不误报）。
> * 车机端同步出 **215 / cd20**：根目录 `Deepal-CarDash-215.apk`。

### 更早（iOS 1.6.15，2026-09-24）—— 只动 iPhone，车机仍 cd19

用户模拟导航测完后的七条：

1. **关掉两个收费接口**（用户要求"只用官方 SDK 的免费功能"）：`showTrafficLightView`、
   `showOverSpeedPulse` 不再赋值。⚠️ 代码注释里写清了原因，**别顺手打开"试试"** ——
   没付费权限时它们什么都不画，白开（价格与申请方式见待办 9）。
   * **我们自己那份"超速红光"留着**：数据同样来自原厂（车机高德限速广播），不花钱。
     用户没说不要它；要拆的话删 `DashboardView` 里那段 `EdgeGlow(kind: .over…)` 即可。
2. 🐞 **"地图移动卡卡的、感觉没有 60 帧"** → 两处硬伤：
   * `DashboardModel.poll()` 每 250ms **无条件**写 `self.car / link / carHost` ——
     `@Published` 是"赋值就发通知"，等于整个 HUD（含地图那个 `UIViewRepresentable`）
     每秒重绘 4 次。现在全部**先比后写**（`CarSnapshot` 本来就是 `Equatable`）。
   * `AnchoredDriveView` / `AnchoredMapView` 的 `layoutSubviews` 以前**每次调用**都重算
     锚点/相机，可能和渲染抢时间；现在用 `lastSize` 挡住，**只在 bounds 真变了**才回调。
   * 另外：模拟导航的位移是 SDK 模拟器按自己节奏推的，看着比真导航"跳"属正常，
     真车手感要等实车。
3. 🐞 **"模拟导航先显示北京，然后才从定位开始导航"** → 根因：`NaviCoordinator.update`
   里**先 `feed` 后建引擎**，第一次调用时 `manager` 还是 nil，喂位置等于没做，
   地图就停在高德默认位置（**北京**）。现已改成"**先建引擎 → 再喂位置 → 最后算路**"，
   并且模拟导航**开始前**也允许喂一次（开始后绝不喂，否则会把模拟车拽回真实位置）。
4. **"预计多久到达"文字别灰** → `NavSummaryPanel` 那格从 `white.opacity(0.70)` 改成 `0.9`，
   和旁边两格一个亮度。
5. **非导航态车标改成"苹果查找"那种实心圆**（用户给的两个选项里选了这个）：
   绿 = 在线（能取到当前位置）、灰 = 不在线。用 `UIGraphicsImageRenderer` 现画，
   不依赖图片资源；状态变了要手动换图（`viewFor` 不会为已存在的标注再调一次）。
6. **深蓝车标到位**：新建 `ios/CarDash/Assets.xcassets`，里面 `DeepalLogo`
   是**官网**那张 `share_logo.png` 抠掉白底得到的透明 PNG。启动遮罩的循环动画自动用它
   （`LaunchOverlay.logo` 里的 `UIImage(named: "DeepalLogo")` 分支早就写好了）。
7. **转向灯**：代码已就绪，等用户把车机诊断里那四项原始值发来校准取值形态（见待办 7）。

### 上一轮（iOS 1.6.14 / 车机 cd19，2026-09-24 凌晨）—— **两边都要更新**

用户模拟导航时提的六条，逐条落地：

1. 🐞 **"跑过的路不显示灰色、还是绿的"** → 头文件原话：`showGreyAfterPass`
   「走过的路线是否置灰，**默认为 NO**」—— 我们从来没开过。已开（`NaviKitView.swift`）。
2. **红绿灯倒计时** → `showTrafficLightView`（+ `showTrafficLights` 图标）。
   ⚠️ 头文件写明这是**收费接口**（"开启付费权限时默认为YES"），没买权限它就不画，
   想要得提高德工单。已打开，能不能显示以实测为准。
3. **超速时两边冒红** → 两条路都做了：SDK 的 `showOverSpeedPulse`（**同样是收费接口**，
   先开着）+ **我们自己算的一份**（不依赖收费）：
   车机高德广播里本来就有 `LIMITED_SPEED`，`AmapSignals` 一直在解析但只记诊断，
   现在经 `StateHub.speedLimit` 下发到 iPhone，`DashboardModel.isOverspeed` 比一下就红。
4. **转向灯 → 屏幕两边闪绿光** → 数据真的有（见第 6 节第 38 条），
   车机侧加了采集（`Light/TurnLightStatus` / `Light/TurnLeft` / `Light/TurnRight`），
   下发 `"turn"`（0灭/1左/2右/3双闪），iPhone 用 `EdgeGlow` 两边闪（左灯只闪左、双闪两边）。
5. **启动时循环画车标** → 启动遮罩改成"外圈转 + 车标呼吸"的循环动画。
   ⚠️ 工程里**一张图都没有**（连 `Assets.xcassets` 都没有），现在用箭头占位；
   把深蓝车标 PNG 命名成 `DeepalLogo` 放进 Assets 就自动换上，代码不用动。
6. **帧率想要 120** → SDK 里没有帧率开关，是 iOS 侧缺 plist 键：
   已加 `CADisableMinimumFrameDurationOnPhone = true`（不加的话系统把 App 锁在 60Hz）。
   ⚠️ 只在支持 ProMotion 的机器上有效，非 Pro 机型仍是 60Hz。

### 更早一轮（iOS 1.6.13，2026-09-23 深夜）—— 只动 iPhone，车机不用重装

用户实测 1.6.12 后提的（截图 + 文字）：

- 🐞 **「横竖屏切换后先错位、要等一会车头才归位」**：根因是 **SwiftUI 在旋转时不一定调
  `updateUIView`**（它的输入坐标没变），可视图 bounds 已经变了 ⇒ `screenAnchor` /
  相机偏移还停在旧方向那一档。解法：给两个地图视图各套一层子类
  （`AnchoredDriveView` / `AnchoredMapView`），在 `layoutSubviews` 里回调重算 ——
  同一帧内就摆正，肉眼看不出空档。
- ✅ **加启动遮罩**（`LaunchOverlay`，1.8 秒 + 0.5 秒淡出）：把「地图首帧 / 导航引擎首次
  创建（同步阻塞）/ 第一次定位」这段藏起来。用户原话：「我要在使用软件的时候看不到这些
  等一会才调整到正确位置」。时长在 `CarDashApp.swift` 里改一个数即可。
- ✅ **模拟导航改成官方的**（用户给了文档《实时导航与模拟导航》）：
  在算路成功回调里调 **`startEmulatorNavi`**（而不是 `startGPSNavi`），
  配合 `setEmulatorNaviSpeed(60)`。这样模拟时车会**沿路线自动跑**，能看到路况、
  电子眼 —— 和官方 demo 一致。
  ⚠️ 模拟期间**不喂** `setExternalLocation`（喂了会把模拟车拽回真实位置）。
- 🐞 **顺手修了个隐蔽 bug**：算路重算的 key 里原来含**起点**坐标 ⇒ 车每移动约 5 米
  就重算一次路线（白费流量、还会把导航重置）。现在只按**目的地**（+模拟开关）判重。

### 上一轮（iOS 1.6.12，2026-09-23 深夜）—— 只动 iPhone，车机不用重装

用户装了 1.6.11 后实测：**非导航状态不跟车、直接显示北京、也没有车标**（截图实锤）。

- **原因**：1.6.11 想让两种状态共用同一个引擎，让没目的地时进**巡航模式**
  （`detectedMode`）。但官方文档《智能巡航》写明：巡航只提供**数据**
  （电子眼/道路设施），**地图视图不会跟车**；官方给的巡航 UI 方案是自己建
  `MAMapView` + Annotation。所以那个 view 停在默认位置（高德默认＝北京）。
  ⇒ **巡航是死路，已回退**。
- **改法**：非导航态回到 `AMapNavView`（MAMapView），但把它调成和导航态同一观感：
  1. 底图换成 **`MAMapTypeNaviNight` / `MAMapTypeNavi`**（枚举里就叫"导航视图/导航夜间视图"）
     —— 之前用的 `.standardNight` 是蓝紫底 + 艳丽 POI 图标，跟导航态差很远；
  2. 车标换成**高德官方那张** `AMapNavi.bundle/.../compass_car_256@3x.webp`
     （导航态那个"蓝圈 + 白箭头"，从 SDK 资源包里读，取不到退回自绘）；
  3. 车头定位改成**精确反算**（`centerCoordinate` + `convert` 两次），
     锚点值和导航态共用 `MapAnchor`（横屏 0.64/0.50、竖屏 0.50/0.66）——
     用户要求"两种状态同步"，现在是真的同一组数字。
- 导航态本身没动（静音 / 外部定位 / 预热 / screenAnchor 都保留）。

### 上一轮（iOS 1.6.11，2026-09-23 深夜）—— 已作废的思路，留档

这一轮试了「非导航态也用导航引擎 + 巡航模式」：**结论是不行**（见上面 1.6.12）。
留在这里是因为它解释了第 6 节第 26 条的结论，省得以后又想试一遍。

用户对比四种状态（导航/非导航 × 横竖屏）截图后提的：

- 🐞 **「非导航状态下黑了一片 / 断层」—— 上一版我引入的 bug，已修**：
  1.6.10 为了让车头不居中，搞了个「把地图视图画大 1.35 倍再偏移」的 hack。
  SwiftUI 会把比父视图大的 child 摆在父视图**左上角**（不是居中），结果
  左边（横屏 8%）/ 上边（竖屏 10%）露出一条**没被地图盖住的黑边**，
  而且车头落点算错（竖屏跑到 0.755/0.775）。**hack 整个删掉了**。
- ❌ **「非导航状态改用同一套导航引擎 + 巡航」→ 已作废**（实测巡航不跟车，见 1.6.12）。
- **留下有用的**：锚点从横屏 `x 0.58` / 竖屏 `y 0.60` 调到 `0.64` / `0.66`，
  并且这两个值被提成了**共用常量 `MapAnchor`**（1.6.12 起导航态、非导航态都读它）。

### 上一轮（iOS 1.6.10，2026-09-23 深夜）—— 只动 iPhone，车机不用重装

用户装 1.6.9 上车实测后的六条反馈，全部处理：

- **导航播报声音**：`AMapNaviDriveManager.isUseInternalTTS = false` + `pauseNaviSpeech()`
  （后者是 AMapNaviBaseManager 的方法，DriveManager 继承得到）。仪表盘自己不出声。
- **「点模拟导航会卡一下、地图先闪一下北京」**：两个原因、两个修法 ——
  ① `AMapNaviDriveManager.sharedInstance()` 第一次创建是**同步阻塞**的 → 启动时
  `NaviKitNavView.prewarm()` 空跑一次；② 引擎自己搜星期间地图停在**高德默认位置（北京）**
  → 用 `setExternalLocation(_:isAMapCoordinate:false)` 把当前位置直接喂给它（WGS-84）。
- **自车 / 当前位置不再居中**：导航视图用 `screenAnchor`（横屏 `(0.58, 0.50)` 往右、
  竖屏 `(0.50, 0.60)` 往下 —— 竖屏时车速数字会压住车头前面的路线）；
  普通地图（MAMapView 没有 screenAnchor）用「视图画大 1.35 倍 + 整体偏移」的布局法等效实现。
- **遮罩不够黑**：主渐变浓度上调（横屏 0.72→0.88、竖屏 0.72→0.86），
  并额外压一条**顶部带**（灵动岛/刘海那一带，横竖屏都加）。
- **模拟路线补上「预计多久到达」**：`DashboardModel.mockNav` 塞一份假的
  `42 分钟 / 21.9 公里 / 预计 19:30 到达`，这样没连车机也能看出顶栏排版对不对。

### 待办 / 已知可优化

1. ~~**目的地还没彻底解决**（只有喊语音导航才有）~~ → **2026-09-24 已做**：
   新增「高德车机版广播」这条权威来源（`DestSignals.onAmapBroadcast`），手动点导航也能认。
   ⚠️ **但**字段名（`TO_POI_NAME` / `TO_POI_LATITUDE` / `TO_POI_LONGITUDE` 一族）是在
   `后装导航.apk`（`com.wt.mahjong` 9.5.0.600013）的 dex 字符串池里挖出来的，
   **还没在真车上验证过**。上车先验 §0.0 的 ①；没认出来的话看 `/logcat`：
   - 「高德导航广播」段的 `extrasAll()` 现在能用了（以前因为 `lastIntent` 没赋值一直是空的，
     本轮修了）——**那就是高德每条广播的全部 key**，真名一眼可见；
   - 找到真名后补进 `DestSignals` 的 `NAME_KEYS / LAT_KEYS / LON_KEYS` 即可，
     归一化会自动兼容 `EXTRA_` 前缀和下划线有无。
   - 另一条备选路还在原处：车机原生导航 IPC（第 11 节），`S05Navi` 继续只做探测、不下发目的地。
2. ~~目的地的**新鲜度窗口是 30 分钟**，讲一小时的语音导航中途路线会消失~~
   → **2026-09-24 已分层解决**：权威源（高德广播）**在导航会话期间一直有效**，
   不再走墙钟；只有语音那条兜底源仍保留 30 分钟窗口（它的副作用是已知且可接受的）。
   会话结束由 `AmapSignals.tick()` 的看门狗判定（`navigating()` 连续 90 秒不成立）。
3. pod 可以锁版本了：当前 `~> 11.0` 实际拉到的是 **11.2.100**
   （从 CocoaPods 的 trunk 查到的最新 11.x）。锁法：`pod 'AMapNavi-NO-IDFA', '11.2.100'`。
   锁之前建议先看一次 CI 日志确认。
4. 根目录别攒包：**只留当前底包 + 当前要装的包**，旧包挪 `_obsolete\` 或删掉（规矩见 §3 末尾）。
   两个在用的底包都在 **`_bases\`**（`Deepal+ S05 v260925.apk` / `旧底包-v26.0521.apk`）。
5. 车机端下一版接着 `214 / v26.0521-cd19` 往上，**有代码改动就要涨版本号**，
   不然车上没法核对装的是不是新的。
6. **导航路线被自定义界面遮挡**（用户提过「遮挡地图/路线」）：头文件里有现成机制 ——
   `showUIElements = NO` 时，SDK 会回调
   `- (UIEdgeInsets)driveViewEdgePadding:(AMapNaviDriveView *)driveView;` 问「你的界面挡住多少」
   （文档原话：*保证路线不被自定义界面元素遮挡*；横竖屏切换 / 全览 / 算缩放时都会问）。
   另有 `- (void)updateRoutePolylineInTheVisualRangeWhenTheShowModeIsOverview;`（**仅全览态**有效）
   和旋转回调 `- (void)driveView:didChangeOrientation:`。
   `NaviCoordinator` 已经是 `AMapNaviDriveViewDelegate`，实现那个方法就行 ——
   **但数值必须按 HUD 实际占位调**（竖屏压上/下两条、横屏压左/下），要看着屏幕调，别盲填。
   （1.6.13 没动它：先把编译错误修掉让包先出来，这条留到下一轮看着屏幕调。）
7. **上车要实测两件事**（数据形态只有真车能确认）：
   * ~~**转向灯**~~ → **2026-09-24 已用实车日志校准**：真实别名是
     `vc_alias_turn_left_signal_on` / `vc_alias_turn_right_signal_on`（1/0，双闪 = 两个同时 1），
     已改进 `VendorSignals`（§6 第 71 条），**cd25 / cd2 起生效** ——
     装完打左/右灯、双闪，看 iPhone 两边绿光对不对。
   * **电子眼 + 限速**：导航中看 iPhone 设置页的 `camera` 那一行
   * **电子眼 + 限速**：导航中看 iPhone 设置页的 `camera` 那一行
     （形如 `dist=120 type=0 speed=60`）和 `limit` ——
     确认车机高德到底发不发 `CAMERA_DIST/TYPE/SPEED` 这三个 key、type 编号是不是高德官方那套
     （0 测速 / 8、9 区间测速）。对不上就调 `DashboardModel.isOverspeed` 里的白名单
     `speedCamTypes`。顺带确认：**只有测速类电子眼才冒红**，闯红灯/监控那些不该冒。
     限速的 key 不同版本叫 `EXTRA_LIMIT_SPEED` / `LIMITED_SPEED`，`AmapSignals.pickInt` 两个都试了；
     都没值就去 `/logcat` 看那一串 extras 的真实 key 名。
8. ~~深蓝车标素材~~ **1.6.15 已做**：`ios\CarDash\Assets.xcassets\DeepalLogo`
   （官网车标抠白底，见第 6 节第 46 条）。想换更清晰的版本就把同名 PNG 覆盖掉，代码不用动。
   ⚠️ 这个 `Assets.xcassets` 是 1.6.15 才新建的，**CI 第一次编译它** ——
   出包时留意构建日志里 `actool` 有没有报错（本地没法编 iOS，只能看日志）。
9. **高德收费接口（2026-09-24 查证过，别凭印象说价）**：红绿灯倒计时（`showTrafficLightView`）、
   超速脉冲（`showOverSpeedPulse`）都属「高级能力 / 商务合作」类，**官方没有公开单价**。
   `lbs.amap.com/upgrade` 上公开的只有这些：技术服务许可 基础版 **¥50,000/年**、
   高级版 **¥100,000/年（联系商务）**、QPS 包 **400~1500 元/月/10QPS**、
   基础服务按调用量 **30 元/万次**（阶梯 24/18）。红绿灯倒计时/超速脉冲**不在**公开价目表里。
   申请入口只有一条：控制台 → **创建工单 → 商务合作类工单**（SDK 文档原话
   「您如果申请试用或者正式应用都请通过工单系统提交商务合作类工单」
   ⇒ **可以申请试用**，天数/条件由商务定）。认证等级：未认证配额 0 / 个人认证 15 万次/月 /
   企业认证(乘风) 300 万次 / 技术服务许可 900 万次 —— 个人认证能自助买的是**基础服务**超额配额，
   高级能力/商用一般要**企业主体**。
   ⚠️ 申请时顺便问**红绿灯倒计时的开通 key**：SDK 里有个
   `- (void)setIsOpenTrafficLight:(NSString *)key;`（`AMapNaviDriveManager.h`，10.0.700+），
   光打开 `showTrafficLightView` 不够，得带 key 调它才有用。
   * 红绿灯倒计时：**必须原厂数据**（我们拿不到路口信号），所以这条只能走商务。
     拿不到授权时，HUD 上只能退而求其次显示车机高德广播里的「剩余红绿灯数」
     （`routeRemainTrafficLightNum`，`AmapSignals` 已解析、目前只记诊断）。
   * 超速脉冲：**不用花钱** —— 我们自己做的那份数据同样来自原厂（车机高德的限速广播）。

---

## 5. 关键设计决策（改动前务必知道）

1. **坐标系统一存 WGS-84**，高德要 GCJ-02，每次进出都转（`ChinaCoord.toGcj` / `toWgs`）
2. **车速只用原车数据**，高德的 `CUR_SPEED` 只记诊断不参与显示（用户明确要求）
3. **总里程不写死量纲**：拿缓存值当标尺校准实时值（缓存滞后但数值对）
4. **歌词优先读 D.apk 的 `LyricBroadcastDispatcher`**，联网兜底
5. **导航 SDK 自带地图**，不能再引 `AMap3DMap`（会重复符号，见下方踩坑）

### 高德 Key（在 `ios/CarDash/UI/AMapSupport.swift` 的 `AMapConfig`）

| Key | 用途 |
|---|---|
| `iOSKey` | 地图渲染 + 导航 SDK |
| `webKey` | Web 算路（driving 接口）|

---

## 6. 踩过的坑（血泪经验，改代码前先看）

### Swift / 编译

1. **`CLLocationCoordinate2D` 不遵循 `Equatable`** —— 含它的 struct 不能声明 `Equatable`
2. **memberwise init 参数顺序必须和属性声明顺序一致**，调用点顺序写反会编译失败
3. `MAPolyline(coordinates:count:)` 返回的是 **Optional**，append 到 `[MAPolyline]` 要 `!`
4. `Info.plist` 版本字段必须写 `$(MARKETING_VERSION)` / `$(CURRENT_PROJECT_VERSION)`，**写死就永远不变**（曾导致版本核对失效很久）

### 高德 SDK（iOS）

5. **iOS 导航视图类名是 `AMapNaviDriveView`**，`AMapNaviView` 是 **Android** 的类名（曾因此卡了 3 轮）
6. **`AMapNaviKit` 的 module umbrella 不完整**，必须靠 bridging header（已建 `CarDash-Bridging-Header.h`）显式 import
7. **导航 SDK 内部自带地图 SDK**，与 `AMap3DMap` 同时引入 → `ld: 272 duplicate symbols`
8. 初始化是 `AMapNaviDriveView(frame:)` + `.delegate = self`（**不是** `init(frame:delegate:)`）
9. 算路成功回调：`driveManager(_:onCalculateRouteSuccessWith type: AMapNaviRoutePlanType)`
10. `AMapNaviDriveManager` 用 **`sharedInstance()`**（5.4+ 禁用了 `init`）
11. 枚举名是 `.drivingStrategySingleDefault`（小写开头，不是 `.singleDefault`）
12. **旋转方向**：高德是**逆时针为正**，GPS course 是顺时针 → `rotationDegree = heading` 是对的（不要改成 `360-heading`）

### 导航数据 / 目的地（2026-09-23 新增）

13. **车机日志里到处都是 GPS 上报，形状和"目的地坐标"一模一样**
    （`{"data":{"lat":..,"lon":..}}`、`gps:GPS(lon=..,lat=..)`）。
    拿它们当目的地 ⇒ IPA 画出一条完全不同的路线，而且车一动目的地就变。
    真目的地只出现在**语音语义日志**里：`"destName"` / `"dest"` / `"destNavi"`。
14. **高德 `EXTRA_STATE`（KEY_TYPE=10019）在导航中会 8 ↔ 40 每分钟翻一次**（实测）。
    别拿"看到空闲就清数据"当导航结束的判据 —— 那会让仪表每分钟闪一下。
    用「引导广播断 60 秒」当判据。
15. **`AMapNaviDriveView.mapViewModeType` 默认是 `0 = Day`** ⇒ 晚上也是白地图。
    自动档是 **`2 = DayNightAuto`**（按日出日落切）。枚举在 `AMapNaviCommonObj.h`。
16. **`AMapNaviDriveView.showScale` 只在 `showUIElements = NO` 时才生效**（文档原话），默认 NO。
17. `AMapNaviDriveView.h` 里还有一整套能单独开关的东西：`showTrafficButton`、`showCrossImage`、
    `showEagleMap`、`showWholeTravel`、`showBrowseRouteButton`、`showMoreButton`、
    `showRefreshButton`、`logoEnable`、`autoZoomMapLevel`。要收拾界面先看这张表，别再猜。
18. 🎯 **别靠猜高德 API 名 —— podspec 是公开的，头文件能直接下下来核对**：
    ```powershell
    # 1. 查最新版本和源码 zip 地址（JSON 里的 source.http）
    curl.exe -s 'https://trunk.cocoapods.org/api/v1/pods/AMapNavi-NO-IDFA/specs/latest'
    #    → https://a.amap.com/lbs/static/zip/AMap_iOS_Navi_Lib_V11.2.100.zip
    # 2. 下载解压（约 120MB，本机实测 6MB/s，20 秒）
    curl.exe -s -o navi.zip https://a.amap.com/lbs/static/zip/AMap_iOS_Navi_Lib_V11.2.100.zip
    Expand-Archive navi.zip -DestinationPath C:\Users\Administrator\amapsdk
    #    头文件都在 AMapNaviKit.framework\Headers\*.h
    ```
    本次的 `mapViewModeType` / `showScale` 就是这么确认的。
    **本机没 macOS 也能拿到权威签名** —— 上面第 5~11 条那种"猜名字卡 3 轮"的事，用这招都能避免。

### 导航视图（AMapNaviDriveView / DriveManager）可调项（2026-09-23 晚新增）

19. **`AMapNaviDriveManager.sharedInstance()` 第一次创建是同步阻塞的** —— 用户实测
    「点模拟导航会卡一下」就是它。改法：启动时空跑一次（`NaviKitNavView.prewarm()`）。
20. **静音**：`manager.isUseInternalTTS = false`（头文件说默认就是 NO，但显式写一次）
    + **`manager.pauseNaviSpeech()`**（`AMapNaviBaseManager` 的方法，DriveManager 继承）。
    `resumeNaviSpeech()` 是它的反操作。
21. **`manager.setExternalLocation(_:isAMapCoordinate:)`** —— 喂外部定位。
    ⚠️ 不喂的话，引擎自己搜星那几百毫秒到几秒里地图停在**高德默认位置（北京）**，
    用户看到的就是"先闪一下北京才跳到我的位置"。`isAMapCoordinate: false` = WGS-84。
22. **`view.screenAnchor`** —— 自车图标在屏幕上的位置，`(0,0)` 左上、`(1,1)` 右下。
    头文件原话：**只在 `showUIElements = NO` 时生效**。现值：横屏 `(0.58, 0.50)`、
    竖屏 `(0.50, 0.60)`（在 `NaviCoordinator.anchorLandscape/Portrait`）。
23. **普通地图（MAMapView）没有 screenAnchor** → 用布局法等效：把地图视图
    `frame` 画成 1.35 倍再 `offset`，位移量别超过 0.175 个屏幕（否则边缘露白）。
24. 其它可调：`showMode`（默认 `CarPositionLocked`）、`trackingMode`、`cameraDegree`、
    `mapZoomLevel`、`autoZoomMapLevel`、`showTrafficLightView`、`showTurnArrow`、
    `lineWidth`、`routeStatusColor`（路况配色）…… 要收拾视觉先翻 `AMapNaviDriveView.h`。

### 巡航 / 非导航态地图（2026-09-23 深夜新增，踩过一轮才理顺）

25. **官方文档在这**：《开发指南 → 专业导航 → 智能巡航》
    `https://lbs.amap.com/api/ios-navi-sdk/guide/navigation-map/cruise-navi`（这个 URL 能静态抓）。
    而 podspec 指向的参考手册站 `a.amap.com/lbs/static/unzip/iOS_Navi_Doc/index.html` 是 **JS 空壳**，
    静态抓只能拿到 "Loading..." —— 想核对 API 优先翻 SDK 头文件（见第 18 条）。
26. **智能巡航 = `AMapNaviDriveManager.detectedMode`**
    （`0=None 1=Camera 2=SpecialRoad 3=CameraAndSpecialRoad`），
    它只是"巡航期间返回哪类道路设施**数据**"的开关，**不会让导航视图跟车**。
    官方给的巡航 UI 方案是：**自己建 `MAMapView` + Annotation，用自车位置更新坐标**。
    ⇒ 我们试过「没目的地时也用 `AMapNaviDriveView` + 巡航」：**实测地图停在默认位置（北京）、
    连车标都没有**（用户截图实锤）。**别再走这条路。**
27. **`startGPSNavi` 不能拿来启动巡航**：头文件原话「**必须在路径规划成功的情况下**
    才能够开始实时导航」—— 没算路时直接失败。巡航与导航互斥，官方顺序：
    导航→巡航要**先 `stopNavi` 再设 `detectedMode`**；巡航→导航要**先把 `detectedMode` 设回 `None`**再开始导航。
28. ⛔ **别用「把地图视图画大再 offset」去挪车头位置**（2026-09-23 我这么干过，被用户抓了）：
    SwiftUI 会把比父视图大的 child 摆在父视图**左上角**（不是居中），于是左/上露出一条
    **没被地图盖住的黑边**（用户原话「黑的断层」），车头落点也全错（竖屏跑到 0.755/0.775）。
29. **非导航态（MAMapView）怎么精确挪车头**：MAMapView 也有 `screenAnchor`，但它是**缩放锚点**，
    不是内容锚点（和导航视图的同名属性语义不同）。所以自己反算：
    `centerCoordinate = 车` → `let at = view.convert(屏幕锚点, toCoordinateFrom: view)` →
    `centerCoordinate = 车 + (车 − at)`；偏移量做合理性判断（< 0.02°）防地图没渲染好时拿到垃圾。
    锚点值统一放 `MapAnchor`（横屏 `0.64/0.50`、竖屏 `0.50/0.66`），**导航态与非导航态共用**。
30. **让两种状态长得一样，这两招确实有效**：
    - 非导航态底图用 **`MAMapTypeNavi` / `.naviNight`**（枚举原话"导航视图 / 导航夜间视图"），
      **不要**用 `.standard/.standardNight` —— 后者是蓝紫底 + 艳丽 POI 图标，跟导航态那种灰黑底差很远。
    - 车标用**高德官方那张**：`AMapNavi.bundle/engine/eyrieImage/compass_car_256@3x.webp`
      （就是导航态那个"蓝圈 + 白箭头"）。路径是
      `<App>/Frameworks/AMapNaviKit.framework/AMapNavi.bundle/...`；取不到就退回自绘箭头。见 `AmapBundle`。
    - 顺带：`AMapNaviDriveView` 有 `setCarImage:`（可自定义自车图标），说明车标确实是"一张图"的机制。
31. **导航态默认「车头朝上」**：`trackingMode` 默认 `AMapNaviViewTrackingModeCarNorth = 1`（Head up），
    `0` 才是 `MapNorth` 正北朝上。停车时车头方向不可靠，高德会在车标外画一圈**南东西北罗盘**兜底 ——
    截图里看到那圈字是正常现象，别当成 bug 去"修"。
32. **SwiftUI 横竖屏切换时不一定调 `updateUIView`**（输入没变），但视图 bounds 已经变了 ⇒
    凡是"依赖视图尺寸"的设置（自车图标位置 `screenAnchor`、相机偏移）都会**停在旧值**，
    表现就是「切过去先错位、等下一次定位更新才归位」。解法：给地图视图套一层子类，
    在 `layoutSubviews` 里回调重算（见 `AnchoredDriveView` / `AnchoredMapView`）。
    **以后再遇到"要等一会才到位"，先想这条。**
33. **模拟导航 = 官方 `startEmulatorNavi`**（文档《实时导航与模拟导航》原话：
    「区别就是在路径规划成功的回调函数中调用 startEmulatorNavi 方法开启模拟导航」），
    速度用 `setEmulatorNaviSpeed(60)`（头文件，默认 60）。它会让车**沿路线自动跑**，
    能看到路况/电子眼 —— 就是官方说的"预先了解既定路线"。
    ⚠️ **模拟期间别调 `setExternalLocation`**，否则模拟车会被真实定位拽回去。
34. **算路重算的 key 只能算目的地**：把起点也算进去的话（1.6.12 之前就是），
    车每移动约 5 米就重算一次路线 —— 白费流量，还会把导航重置。
35. **启动遮罩**（`LaunchOverlay`，1.8 秒 + 0.5 秒淡出）：藏住「地图首帧 / 引擎首次创建 /
    第一次定位」。用户明确要求别让他看到"先错位再归位"。时长在 `CarDashApp.swift` 改一个数。
    2026-09-24 起里面的标改成循环动画（外圈转 + 车标呼吸）；车标走 `UIImage(named: "DeepalLogo")`，
    **工程里目前一张图都没有**，所以先显示占位箭头，拿到素材直接换（见待办 8）。
36. **形参别和存储属性同名**（2026-09-23 CI 为此挂过一次，整包没出来）：
    `NaviCoordinator` 里有 `private weak var view: AMapNaviDriveView?`，
    方法写成 `func update(view: AMapNaviDriveView, ...)` —— 形参把**属性盖住**了，
    函数体里 `view` 就变成**非可选**，`if let v = view` 直接编译不过：
    `error: initializer for conditional binding must have Optional type`
    改法：形参改名（现在是 `naviView`）。
    **本地没有 Swift 编译器**（Windows），所以 `_tools\check_swift.py` 里加了静态检查：
    * 非可选形参被 `if/guard/while let` 绑定 → **报错并计入问题文件数**
    * 形参遮蔽同名属性 → 提示（不一定错，但危险）
    push 之前先跑 `python _tools\check_swift.py`。
37. **高德那批「路线上显示什么」的开关，大多默认 NO** —— 用户说"某某没显示"时，
    先去 `AMapNaviDriveView.h` 查默认值，别急着怀疑自己的代码。本次用到的：
    `showGreyAfterPass`（走过的路置灰，**默认 NO** ← 用户说"跑过的路还是绿的"就是这个）、
    `showCameraDistance`（电子眼距离，**默认 NO**）、`showCamera`/`showTrafficLights`（默认 YES）。
38. **两个收费接口**（头文件原话"当前接口为收费接口…请通过工单系统提交商务合作类工单"）：
    `showTrafficLightView`（红绿灯倒计时）、`showOverSpeedPulse`（超速脉冲）。
    打开它们不花钱，但**没买权限就不会有画面** —— 所以超速那条我们自己也做了一份
    （见下条），不依赖它。
39. **限速从车机高德广播来**：`AmapSignals` 里 `LIMITED_SPEED` / `EXTRA_LIMIT_SPEED`
    一直在解析（变量 `limit`），以前只 `setSource("limit", …)` 记诊断，
    现在经 `StateHub.speedLimit` → JSON `"limit"` → iPhone `CarSnapshot.limit` →
    `DashboardModel.isOverspeed`（留 2km/h 容差；限速未知一律不报，宁可不报别误报）。
    另：SDK 也有 `driveManager:onUpdateNaviSpeedLimitSection:`，但它第一个参数在头文件里是
    `_Nullable` ⇒ Swift 实现得写 `AMapNaviDriveManager?`，否则签名对不上、回调永远不来。
40. **转向灯数据确实存在**（2026-09-24 从 D.apk 挖出来的）：别名有
    `Light/TurnLightStatus`（中文名「灯光 / 转向灯状态」）、`Light/TurnLeft`、`Light/TurnRight`、
    `TurnLight/State`（是 TurnLightStatus 的兼容别名）；D.apk 里还有
    `parseTurnSignalValue` / `handleTurnSignalStateChanged` 方法。
    **但取值形态没在真车上验证过**，所以三个别名全读、原始值记进诊断
    （iPhone 设置页的 `turnRaw`），归一化放在 `StateHub.turnValue()` ——
    **必须在最后统一算**：左右灯是两个独立别名、到达顺序不定，
    在采集端就地归一化的话「左灯=0」会把已经收到的「右灯=1」覆盖掉。
41. **ProMotion 120Hz 要 plist 键**：`CADisableMinimumFrameDurationOnPhone = true`，
    不加系统就把 App 绘制锁在 60Hz。SDK 里**没有**帧率开关，别去找。
    只在 ProMotion 机型上有效。
42. **收费开关别"打开试试"**：`showTrafficLightView`（红绿灯倒计时）和
    `showOverSpeedPulse`（超速脉冲）没付费权限时**什么都不画**，打开只会让人以为坏了。
    用户 2026-09-24 明确「只用官方免费功能」⇒ 这两个不赋值（见第 38 条与待办 9）。
43. **高德的默认位置是北京**：地图在拿到定位前会停在默认位置。所以
    `NaviCoordinator.update` 的顺序必须是**先建引擎 → 再 `setExternalLocation` → 最后算路**
    （早期版本先 feed 后建引擎，第一次喂不进去，用户就看到"先显示北京再跳过来"）。
    且模拟导航**开始前**要喂一次位置、**开始后**必须停喂。
44. **`@Published` 是"赋值就发通知"**：`DashboardModel.poll()` 每 250ms 跑一次，
    无条件写 `car / link / carHost` 会让整个 HUD（连同地图那个 `UIViewRepresentable`）
    每秒重绘 4 次 —— 表现就是"地图卡卡的、没 60 帧"。**先比后写**（`if snap != self.car`）
    一行就能明显改善；以后往 poll 里加字段也照这个写法。
45. **`layoutSubviews` 不是"只在旋转时调"**：SDK 内部也可能频繁触发，在里面重算
    锚点/相机会跟渲染抢时间。要**用 bounds 尺寸变化做闸门**（两个 Anchored* 里的 `lastSize`）。
46. **深蓝车标从哪来的**：官网 `https://staticre.deepal.com.cn/20231107-office/office/public/share_logo.png`
    （300×300 白底银标，是官网唯一能直接抓到的品牌图；那站是 SPA，页面里只有这一张）。
    抠白底 + 裁边用 `%TEMP%\mklogo.py` 那个 Pillow 脚本做的 —— 第三方 logo 站
    （motomarks / logodad）抓下来是 HTML 或错误 JSON，**别在那浪费时间**。
    ⚠️ 为此装了 **Pillow**（`python -m pip install pillow`），这台机器上现在有它。
47. **一旦有了 `Assets.xcassets`，actool 就会跑**，而 XcodeGen 默认给 App target 设
    `ASSETCATALOG_COMPILER_APPICON_NAME=AppIcon` ⇒ **必须有 `AppIcon.appiconset`**，
    否则 `error: None of the input catalogs contained a matching … app icon set … "AppIcon"`。
    2026-09-24 就为此挂了一轮 CI。图标要 **1024×1024 且不带 alpha**（App Store 要求不透明）；
    单尺寸图标的 Contents.json 用 `"idiom":"universal" + "platform":"ios" + "size":"1024x1024"`。
48. **"超速冒红"的正确语义（用户 2026-09-24 指出的）**：高德的红色脉冲是
    **会被拍限速的电子眼**才冒，**不是**超了当前道路限速就冒 —— 路上很多电子眼不测速
    （闯红灯/监控/公交道/应急车道）。判断依据是车机高德广播里的
    `CAMERA_DIST/TYPE/SPEED`，类型用官方枚举 `AMapNaviCameraType`（本地头文件
    `AMapNaviCommonObj.h` 里有全表）：
    **0 测速 / 1 监控 / 2 闯红灯 / 3 违章 / 4 公交专用道 / 5 应急车道 / 6 非机动车道 /
    8 区间测速起始 / 9 区间测速终止** ⇒ 只有 0/8/9（或自带限速值的）才算测速类。
    规则：前方 300m 内有测速类电子眼 **且** 车速 >（该眼的限速，没给就用路段限速）+2km/h。
    数据不全一律不冒。
49. **别把"路段限速"当"电子眼限速"**：`LIMITED_SPEED` 是**路段**限速，
    `CAMERA_SPEED` 是**那个电子眼**的限速，两者不是一回事（同一个电子眼可能限 60，
    而路段限 80）。判断要不要冒红优先用后者。
50. **路口放大图（cross image）的两个硬前提**：
    ① `AMapNaviDriveView.showCrossImage` **必须设 false**，否则回调里的图是 **nil**
       （头文件原话，而它默认是 YES —— 2026-09-24 就是靠这句话才拿到图的）；
    ② 图走 `AMapNaviDriveDataRepresentable` 的
       `driveManager:showCrossImage:`（给图、比例固定 25:16）和
       `driveManagerHideCrossImage:`（收图，注意这个方法名**不带** `driveManager:` 的前半段），
       而且要在**算路之前** `addDataRepresentative(self)`，否则第一次进路口收不到。
51. **"只遮 A 不遮 B"的 UI 做法**：让 A 和新内容**共用同一格**（ZStack），
    并把新内容尺寸锁成**不比原内容高**（`SpeedSlot` 里 150*scale vs 速度表约 181*scale）——
    出现/消失都不会让布局抖动，也就绝不会压到别的元素。
    **别用铺满屏的 overlay**（那一定会盖到别的东西）。
52. **ObjC 方法名别照抄到 Swift（这条已经烧了三轮 CI）**：
    Swift 导入 ObjC 时会「**省掉与参数类型重复的词**」，所以头文件里的
    `driveManager:showCrossImage:` 在 Swift 里叫 `driveManager(_:showCross:)`
    （参数就是 `UIImage` ⇒ `Image` 被省），照抄 `showCrossImage:` 直接编译不过：
    `error: 'driveManager(_:showCrossImage:)' has been renamed to 'driveManager(_:showCross:)'`。
    **写完先跑 `python _tools\check_swift.py`** —— 里面第 4 条专门查这个
    （外部标签以"参数类型名"结尾就提示）。同类：`viewForAnnotation:` → `view(for:)`、
    `addDataRepresentative:` → `addDataRepresentative(_:)`。
    ⚠️ 另一个反向坑：`driveManagerHideCrossImage:` **不用**改（参数是 manager，不重复），
    硬改反而对不上。
53. **`MAMapView.convert()` 在"地图还没渲染好"时会返回垃圾坐标**：非导航地图的车头定位
    （`AMapNavCoordinator.applyCamera`）就是靠它反算偏移的。**不能在算不出来时直接放弃** ——
    那样车会停在屏幕正中，用户看到的就是「进软件先居中、几秒后才归位」（横竖屏都有，
    2026-09-24 用户截图报的就是这个）。正确做法：让 `applyCamera` 返回"有没有真偏移成功"，
    失败交给 `reapplyCamera` **排队重试**（0.1~0.8s 退避、最多 8 次，成功即清零）。
54. **导航起步前 `AMapNaviDriveView` 显示的是 SDK 默认位置（北京）**：算路 + 起步要 1 秒左右，
    这一秒它就在北京，用户会看到"闪一下北京"。想不让人看到，就别在这段露出导航视图 ——
    现在的做法是**垫一张非导航地图**顶着，等 `didStartNavi`（`onStarted` 回调）再切过去。
55. **要动效就别用 `MAAnnotationView.image`**：静态图动不起来。改成**自定义 `MAAnnotationView`
    子类** + `CAShapeLayer` + `CAAnimationGroup(repeatCount = .infinity)`（`CarDotView` 就是这么做的）。
    好处：动效跑在**渲染服务**上，不吃主线程、不影响地图帧率。
    ⚠️ **别在子类里重写 ObjC 的 `init(annotation:reuseIdentifier:)`** —— 空值标注写错就编译不过
    （本项目已在这类命名/签名上栽过三次）。走继承来的 init，再用一个 `prepare()` 建层。
56. **`CAShapeLayer` 的 `bounds`/`position` 默认是 0**：只设 `path` 的话，
    `transform.scale` 会绕**父层左上角**缩放 —— 表现就是"光晕在一旁呼吸、没对准圆心"
    （用户 2026-09-24 实测）。要让动效居中：每层都设 `bounds`（图形外接矩形）
    + `position`（中心点），`path` 用**相对自己 bounds** 的坐标画。
57. **用脚本"整块删代码"很危险**（2026-09-24 踩了）：按花括号配对删的时候，
    遇到「同一行里既有 `{` 又有 `}`」「注释行里没有花括号」就会**多删/少删**，
    留下多一个 `}`、孤立的 `.append(...)`、空 `try {}` 这类结构残体。
    **删完立刻跑**：Swift → `check_swift.py`；Java → `java_lint.py` + `tools\build_local.py`（本地真编译）。
    这轮两边的残体**全是自检抓出来的**，没一处是靠眼睛看出来的。
58. ⛔ **删「整块代码」时，光编译绿不算完 —— 必须逐条功能过一遍**（2026-09-24 真踩了，比上一条更狠）：
    删「自绘路线绘制块」时，那段代码**夹在**「相机块」和「目的地 pin」之间，
    而 **车标（`carPin`）的创建代码正好也在同一个区间里**，于是被区间删除一起带走。
    ⚠️ 这种错**编译和静态检查发现不了**：`carPin` 的声明还在、`viewFor` 里还在用它，
    只是**永远不会有这个标注**（没有任何语法/结构错误）—— 花括号配平、命名检查全绿。
    现象：**非导航态看不到那个绿色呼吸圆点**，最后是**用户在自己手机上发现的**。
    ⇒ 规矩：以后整块删代码之后，**挨个功能走一遍**：
    车标在不在？路线还有没有？目的地 pin？路口放大图？限速/转向灯接口还在读吗？
    或者**先让用户在他机器上试一版**再继续删别的。
59. ⚠️ **中文 Windows 控制台（GBK）会把 `check_swift.py` 打崩 —— 而且崩得"看着像跑完了"**
   （2026-09-24 在这台 `d:\Desktop\IPA` 上发现）：第 4 项检查（ObjC→Swift 命名，**救过三轮 CI 的那个**）
   标题里有个 `⇒`（U+21D2），GBK 编不了 ⇒ `print` 抛 `UnicodeEncodeError` 直接中断。
   现象极具欺骗性：**前 3 项（括号配平 / if-let 绑定 / 形参遮蔽）照常打印完**，
   断在第 4 项标题上，粗看就是"检查跑完了、没问题"，其实第 4 项**一次都没跑** ——
   等于最容易让 CI 挂的那项守卫是空的。
   已修（`_tools\check_swift.py` 开场加 `sys.stdout.reconfigure(errors='replace')`，
   只放宽错误处理、**不改编码**，中文照旧可读，遇编不了的符号显示 `?` 而不崩）。
   ⇒ **判断脚本有没有跑完，看最后那行 `问题文件数 =`**，没看到就是没跑完。
60. 🎯 **「动画明明写了却看不见」先算一遍"实际曝光量"，别怀疑图层没建上**
   （2026-09-24 用户报「车标看不见绿色呼吸、只有一个绿点」）：
   `CarDotView` 的呼吸**代码一直在跑**，是数值让它白跑 ——
   * 光晕画在实心圆**下面**（`halo` 先 add），半径 8.25~17.25 里 **小于白圈 11.5 的那段整段被盖住**，
     肉眼可见的行程只有 11.5→17.25 那 ~6pt；
   * 填充色只有 `0.30` 透明度，再乘 `fade 0.75→0`，而 `timingFunction = .easeOut`
     在行程前段就把透明度吃掉大半 ⇒ **露出来时只剩 ~8%**，等于没画。
   ⇒ 改成"起点就从白圈外沿冒出来"（`scale 0.8→1.7`，即 r 12→25.5）+ 填充 `0.50` ⇒ 一眼能看见。
   ⚠️ 对比着看：1.6.18 那版**同样淡**，用户却说看得见 —— 因为当时缩放绕着父层左上角，
   光晕大幅横扫、曝光面积大。**动效"看不看得见"取决于 行程 × 透明度 × 遮挡，三者要一起算。**
   同理：`clip` 也是隐形杀手，所以 `side` 从 34 提到 64（装得下 r=25.5）并显式 `clipsToBounds = false`。
61. ⚠️ **Core Animation 的动画会被系统丢掉，得能自愈**：视图离开窗口/渲染树（高德回收标注视图、
   App 切前后台）时层上的动画就没了 —— 而且**不报错、不崩溃，只是不动了**，最难查。
   现在的做法：`didMoveToWindow()` 里 `window != nil` 就补一次、再监听
   `UIApplication.didBecomeActiveNotification` 补一次，`halo.add(g, forKey:)` **同一个 key 重复 add 是替换**
   （不会叠加速度、不会越转越快），所以可以放心无脑补。
62. ⚠️ **这台机器的网络封了 GitHub 的 22 端口**（2026-09-24 实测：`git push` 报
   `ssh: connect to host github.com port 22: Connection timed out`；同一时刻 443 秒通）。
   ⚠️ 它和 §9 那条「`Permission denied (publickey)`」**是两回事**：
   那条是 `core.sshCommand` 里的路径过期，这条是**网络层直接连不上**（提示词里是 `timed out` 不是 `denied`）。
   修法：`_tools\setup_git.py` 现在固定带上 `-o Hostname=ssh.github.com -o Port=443`
   （remote URL 保持 `git@github.com:…` 不动），并会把 `[github.com]:443` 的 host key 补进 `known_hosts`。
   **换机器后重跑一次 `python _tools\setup_git.py` 就自动带上，不用手动改。**
63. 🎯 **"长途导航会自己退出"的三个根因（2026-09-24 一次全修掉，别只修一个）**：
   用户问的是"连续导航 10 小时会不会乱跳/退出导航"，查下来是**三条独立的链**，
   任意一条都能让导航掉，而且它们还会互相掩盖：
   * **30 分钟墙钟过期**（`DestSignals.fresh()`）：`lastAt` 只在地名/坐标**变化**时刷新，
     导航中你不会再喊一次同样的目的地 ⇒ 30 分钟后 `dest` 从 `/state` 里消失 ⇒
     iPhone `syncDestination` 拿不到 candidate ⇒ `routeDest = nil` ⇒
     `NaviCoordinator` 收到 `to == nil` 立刻 `stopNavi()`。
     **这是必然发生**（>30 分钟的行程一定中），不是"可能"。
   * **单帧 `nav.active:false`**：`navFresh()` 原来只有 **15 秒**窗口，而 iPhone 是
     250ms 轮询、**单帧就认** —— 引导广播断十几秒（高德重算路线/切前后台/隧道）
     就会让某一帧报 false，iPhone 当帧就把整条路线拆掉。现在放宽到 **60 秒**。
   * **目的地被别的日志改掉**：见下一条。
   ⇒ 修法在两边都有：车机侧把"导航会话"当成一个整体（权威源在会话期间一直有效、
   90 秒去抖才收尾），iPhone 侧给"目的地暂时拿不到"加 **20 秒去抖**（不单帧放手）。
   **要点：判断"导航结束"必须去抖，绝不能单帧下结论。**
64. 🎯 **车机手动点导航的目的地从哪来（2026-09-24 新增，别去猜别的办法）**：
   在 `后装导航.apk`（`com.wt.mahjong` 9.5.0.600013 = 车机上的高德地图深蓝专版）的
   dex 字符串池里挖到的字段族 ——
   ```
   TO_POI_NAME / TO_POI_LATITUDE / TO_POI_LONGITUDE   ← 终点（目的地）
   MID_POI_*  ALONG_POI_*  ARRIVE_POI_*               ← 途经点 / 沿途 / 到达
   FROM_POI_NAME / FROM_POI_LATITUDE / FROM_POI_LONGUTUDE  ← 起点（**拼错了，别照抄成 LONGITUDE**）
   ```
   同一份数据**有三套写法**（不同版本/载荷里名字不一样）：
   `TO_POI_NAME` / `toPoiName` / `ToPoiName`，还有 `EXTRA_` 前缀的变体。
   所以 `DestSignals.normKey()` 统一成"大写 + 只留字母数字 + 剥掉 EXTRA 前缀"再比，
   上面四种写法会落到同一个 key 上。
   ⚠️ **两条铁律**：
   * **绝不能认 `FROM_`/`START_` 那一族** —— 那是起点（＝车自己的位置），
     认成目的地就又回到 2026-09-23 那个"目的地＝当前位置"的坑（IPA 路线和车机完全不同）。
   * **绝不能去扫 `EXTRA_POI_RESULT` 那类搜索结果** —— 用户只是搜了个地方、没去导航，
     扫了就会"导航到其他目的地"。只扫路线载荷（`EXTRA_NAVI_INFO` / `EXTRA_PLAN_ROUTE` /
     `EXTRA_ROUTE` …）。这两条都是用户明确要求的「中间不导航到其他目的地」。
   另外：**同地点（100 米内）只刷新时间戳、不动坐标** —— 坐标一变 iPhone 就重新算路，
   用户看到的是"导航闪一下从头再来"。
65. 🐞 **诊断工具自己也会坏：`AmapSignals.lastIntent` 声明了、读了、从没赋值**
   （2026-09-24 发现）—— 所以 `/logcat` 里那段「最近一条引导广播的全部 extras」
   **永远是"（还没收到过广播）"**。而它正是"高德 key 真名到底是什么"唯一的现场证据。
   已修（在 `handle()` 开头 `lastIntent = intent`）。**教训**：给排查用的输出，
   要真的在真机上核对一次它有没有内容，别默认"代码写了就是好的"。
66. ⚠️ **引导广播（KEY_TYPE=10001）巡航时也发** —— 所以「在收引导广播」**不等于**「在导航」。
   巡航（EXTRA_STATE 9/24/25）没有目的地却一直在发引导信息，只看引导广播的新鲜度
   会把巡航误判成导航中。判据要结合模式（`AmapSignals.navigating()`：mode==2 直接判否）。
   反过来，**导航中 EXTRA_STATE 会 8↔40 每分钟翻一次**（40 落到"空闲"档），
   所以模式不是 1 时也不能立刻判否，要看引导广播还新不新鲜（60 秒）。
   两头都踩过，这条判据就是在这两个坑中间走钢丝。
67. 🎯 **「剩余多久/多远/转向」是四路来源抢同一批槽位，必须仲裁**
   （2026-09-24 用户提"我习惯原厂导航和第三方高德**一起开**，会不会重复获取目的地一直跳"）：
   写这几个字段的地方有**四处** —— 高德广播（`AmapSignals.onGuide`）、
   通知栏（`NaviSignals.onPosted`）、读屏（`NaviSignals.onScreenTexts`）、
   车身信号（`VendorSignals` 的 `Navigation/NaviInfo*`）。
   以前**只有一条规则**："通知栏刚给过（5 秒内）就别让读屏覆盖"（`NOTIFY_PRIORITY_MS`），
   其余三路谁也不让谁 ⇒ 两个导航 App 同时开时，顶栏数字会在两条**不同路线**的
   "还有 48 分钟 / 还有 200 公里"之间来回跳。
   现在统一走 `StateHub.navClaim(src)`：优先级 **高德广播(3) > 通知栏/读屏(2) > 车身信号(1)**，
   优先级低的一路在当前来源 5 秒内还推过数据时直接让位；被压住会写进
   `src.navIgnored`，**在真车上一眼就能看出两个导航确实都在推、我们挑了谁**。
   高德没在导航时通知栏 5 秒后自然接手（原厂导航单独用时顶栏照样有数据）。
   ⚠️ 目的地那条**不跟着仲裁走**：它只有两路，而权威源在位时语音那路是**完全锁死**的
   （见第 64 条），被忽略的语音目的地会记进 `src.destVoiceIgnored` 留证据。
68. 🎯 **"按使用习惯自查"抓出来的三个 bug（2026-09-24，iOS 1.6.23）—— 这类 bug 用户不会提醒你**：
   用户的原话是「你要学会自查可能出现的 bug，而不是我去提醒你才查，要结合生活习惯」。
   这三个都是"平时看不出来、在他那种用法下才炸"的，记在这里当模板用：
   * **`Info.plist` 声明了 `UIBackgroundModes: location`，代码里却
     `allowsBackgroundLocationUpdates = false`** —— 声明等于白写。
     用户的用法是"这块手机在车里一直当仪表"，中途切出去看别的 App / 手动锁屏很常见；
     一旦进后台 iOS 就把它挂起 ⇒ **轮询和导航引擎一起停，仪表冻在最后一帧**。
     （`isIdleTimerDisabled = true` 只防"自动锁屏"，防不住"切后台"和"手动锁屏"。）
     **自查方法：`Info.plist` 里的每一项声明，都去代码里找对应的实现，对不上就是 bug。**
   * **`poll()` 只看 `inFlight` 布尔**：它只在完成回调里清 —— 回调万一没执行
     （App 被挂起、网络栈异常），就**永远卡在 true ⇒ 整个仪表再也不刷新**，
     而且不报错、界面看着还"活着"。**自查方法：任何"置位-复位"的状态机，
     复位路径都得有超时兜底**（这里加了 3 秒时间闸门）。
   * **熄火下车后手机还在导航**：`syncDestination()` 只在**拉取成功**时才跑，
     车机一断电就再也收不到任何消息 ⇒ `routeDest` 一直留着、导航引擎空转
     （用户习惯：熄火、走远离开车机热点）。现在看门狗里加了"失联 >150 秒就收掉"
     （150 秒是留给"行车途中热点打嗝"的余量，模拟导航模式不受影响）。
     **自查方法：凡是"依赖外部输入才能变化"的状态，都要问一句
     「那个输入再也不来了，它会怎么样？」**
   * **`DestSignals.report()` 写了却没有任何调用点**（同 §6 第 65 条那类）：
     本轮新增的诊断方法**从来没被 `/logcat`、`/diag` 调过**，等于死代码 ——
     而文档 §0.1 恰恰让用户"去 `/logcat` 看目的地那一段"。
     用户要是照着做，看到的是一片空白，还会以为"是不是没识别到目的地"。
     **自查方法：新增的诊断输出，除了写，还要 grep 一遍调用点，
     并且（有条件时）在真机上确认它真的有内容。**
69. ⚠️ **换机器后必查两件事，而且「整包哈希相同」这个判据本身不成立**（2026-09-24 退回本机复核时发现）：
    * **git 里删掉的文件会被文件夹拷贝带回来**：cd21 删掉的 `MediaSignals.java` / `Lyrics.java` /
      `LauncherLyrics.java` 又出现在工作区（`git status` 里是 **untracked**）。它们引用的是
      **已经被删掉的** `StateHub.mTitle/mCover/mediaError` ⇒ 车机包会**直接编译不过**
      （`javac` 会编 `inject` 下所有 `.java`）。处理：`git status` 看到 untracked 的**源码**文件，
      先想一下"这会不会是上游删过的"，是就删掉，再跑一次本地构建确认干净。
    * **`.git/config` 的 `core.sshCommand` 是上一台机器的绝对路径** ⇒ push 报
      `D:/Desktop/IPA/_env/git/usr/bin/ssh.exe: No such file or directory`。
      修法就是 §7 第 2 步：`python _tools\setup_git.py`（它按当前位置重写并实测认证）。
    * **整包字节比对不能跨机器用**：本地重编 `219/cd24` 与根目录交付包
      **1656 个条目、内容 0 处不同**、解压后总和都是 `17,352,109`，但**整包 SHA-256 不同**
      （体积差约 184KB）。两条原因：① `zipfile` 的默认压缩级别**随 Python 版本变**
      （办公那台 Python 3.14 / 这台 3.12.10 ⇒ 压缩率 61.75% vs 60.69%）；
      ② APK 的 v2/v3 签名块是 ECDSA，每次签名随机数不同。
      ⇒ **"源码与交付包一致"的正确判据**：比**逐条目内容**（`zipfile` 每条 sha256，
      重点 `classes.dex` / `classes2.dex` / `AndroidManifest.xml` / `resources.arsc`）
      或比**解压后大小总和**；**别比整包哈希**。
70. 🎯 **换底包 = 数据层可能重构，动手前先 grep 反射目标**（2026-09-24 移植到 v26.0923 踩过）：
    旧底包 `Deepal+_v26.0521.apk` 换新 `Deepal+ S05 v260924.apk` 时，**包名和系统签名都没变**，
    但内部重构了三处，差点让注入静默失效：
    * `CarS05InfoUtil` → `S05VehicleDataMonitor`（挪到 `com.deepalhome.launcher.util.s05` 包）；
    * `psGetValueSync(String)`（同步返回）→ `psGetValue(String)`（**异步 void**，推给 `onPsAliasChangedListeners`）；
    * 底包从单 dex 变 **multi-dex**（自带 `classes2.dex`），注入脚本原来写死塞 `classes2.dex` 会直接拒绝。
    移植要点：① 用 `_tools\dex_class.py --list` + 字段/方法 dump 逐个核对反射目标是否还在
    （这次 `cacheCarS05Info` / `currentDrivingSpeedKmh` / `onPsAliasChangedListeners` /
    `virtualCarPropertyManager` / `connectPolymericService` 全都在，只换了类名 + 一个方法）；
    ② 注入 dex 改成**自动挑下一个 classesN.dex 槽位**（`inject_manifest.py`，对旧底包也兼容）；
    ③ 同步取值方法没了就置 `psGet=null`，靠缓存 + 实时监听兜底；
    ④ 每次换底包都出**新旧两个包**（`新-…` / `旧-…` 前缀），新包闪退能回退。
71. 🎯 **数值精度要看语义 —— 坐标绝不能走通用舍入**（2026-09-24 实车抓到的）：
    `Json.num` 是给速度/电量那种「2 位小数够用」的字段写的（`Math.round(d*100)/100`），
    目的地坐标也走了它 ⇒ `30.759425` 被舍成 `30.76`，**最大糊掉约 1.1 公里**。两个后果：
    ① IPA 的导航终点和车机不一致（用户截图对比：只是「接近」那个地方，不落点）；
    ② iPhone 的 `routeKey` 拿坐标去重，附近两个目的地舍完相同 ⇒ **切了目的地也不重算路线**。
    修法：坐标专用 `Json.num6`（6 位 ≈ 0.1 米），`routeKey` 把名字也算上兜底。
    同一轮实锤的还有：**转向灯别名错了** —— 照 D.apk 的 Aliases 表猜的
    `Light/TurnLeft` / `Light/TurnLightStatus` 车机根本不推（/state 里 `turn` 恒为 null），
    实车日志的「车机实时推送」列表里真正在推的是 `vc_alias_turn_left_signal_on` /
    `vc_alias_turn_right_signal_on`（值 1/0，双闪 = 两个同时 1）。
    ⇒ **别名/字段名这类东西必须拿实车日志校准，不能照反编译表猜。**
72. 🎯 **换了底包，反射类名可能整个换掉 —— 只用一份代码打两个底包就必须做"候选名单"**
    （2026-09-24 抓到的严重 bug，差点让整包白测）：
    用 dex 逐个核对确认 **两个底包的车身信号取值类是不同的类**：
    * 旧底包 v26.0521 → `com.deepalhome.launcher.util.CarS05InfoUtil`
    * 新底包 v26.0923 → `com.deepalhome.launcher.util.s05.S05VehicleDataMonitor`
    移植新底包时把类名写死成新的那个 ⇒ **旧底包打出来的包装到车上
    `Class.forName` 直接失败，车速/档位/续航/电量/转向灯全空**，而且不报错、
    界面看着还"活着"（`/state` 里 `vendor` 会写 `unavailable`）。
    修法：`UTIL_CANDIDATES` 存两个名字，`connect()` 依次试、哪个存在用哪个，
    失败时把手柄清干净再试下一个，诊断里记下实际连上的类名。
    ⇒ **以后凡是"一份代码要给两个底包打包"，反射目标都要写成候选名单 + 逐个验证。**
73. 🎯 **改包重签 ≠ 改签名身份 —— 用同一个平台密钥重签，证书/公钥摘要是不变的**
    （2026-09-25 用 apksigner 逐项核实过，用来回答"会不会影响插件市场订阅"）：
    原版桌面和我们用 `platform.pk8` 重签的包对比，
    **证书 SHA-256 / 公钥 SHA-256 / SHA-1 / MD5 / DN / RSA2048 全同**，
    连"只有 v3 生效"的验证特征都一样 ⇒ 凡是基于**签名证书或公钥**算的校验
    （`appSignatureSha256` 通常就是这种）**完全不受影响**。
    **真正会变的只有"整包内容哈希"**（我们往里加了 dex 和组件），
    如果对方校验的是整包哈希，那只要改动包就无解 —— 这时只能改成**独立 APK**
    （见 §0.0c 的预案：独立包 + `sharedUserId=android.uid.system` + 平台签名，
    再用 `android:process="com.deepalhome.launcher"` 共用桌面进程以保留反射取数）。
    核查命令：`apksigner verify --print-certs -v <原版.apk>` 与 `<重签后.apk>` 对比。
    验证手段：`_tools\dex_class.py --list` 或直接搜 dex 字节里有没有那个类名。
74. 🎯 **别按属性名字猜真值语义 —— 猜两次错两次（2026-09-25 实车定案）**：
   `vc_alias_turn_left/right_signal_on` 这两个别名，第一次猜错了**别名**（照 D.apk 的
   `Light/Turn*` 表，车机根本不推），第二次猜错了**真值语义**（按名字当成"1=亮"）。
   证据是用户车上的属性全量扫描：**没打转向灯时两个别名都是 1** ——
   按"1=亮"解释就是"双闪"，iPhone 两边绿光从连上车机起一直闪（用户报的 bug）；
   按 **0=亮、1=灭** 解释就全对上了（全灭 1/1、左亮 0/1、右亮 1/0、双闪 0/0）。
   ⇒ 现在语义集中在一个常量 `VendorSignals.TURN_ACTIVE_VALUE`，
   并在诊断里加了一份**原始值变化历史**（带时间，最近 24 条）——
   "打一次左/右/双闪"就能一次定案，不用再猜第三次。
   **教训：真值语义只能靠"我做了 X，值变成了 Y"来定，绝不能靠名字/文档/反编译表。**
   （同类：摄像头 type 白名单、限速 key 名，都是这个套路。）
75. 🎯 **「只在状态变化的那一刻判一次」= 会永久卡住**（2026-09-25 修，用户实测）：
   清导航字段（顶栏的"还有多久/多远/几点到"）原来只挂在
   `AmapSignals.onState` 的**模式变化**分支上（`if (m == mode) return;` 之后）。
   实车现象：车机退出导航 → 那一瞬间引导广播还新鲜（实测 30 秒前）→
   60 秒的新鲜度判定失败 → **之后再也不会重判** → 车机早退出了，
   iPhone 顶栏一直挂着「15 分钟 6.7 公里」（用户截图红箭头指的就是它）。
   ⇒ 修法是把判定搬进**每 10 秒跑一次的看门狗**（`AmapSignals.tick()`），
   判到结束就把**导航字段 + 目的地一起清干净**（少清一套就会出现
   "地图回普通地图了、顶栏还挂着摘要"这种半死状态）。
   **自查关键句：「这个判定只跑一次吗？漏了会不会永远卡住？」**
   （同 §13.2 第 3 条：状态谁负责复位、复位路径断了会怎样。）
76. 🎯 **两边各有一套阈值 ⇒ 总有一套会先误判**（2026-09-25）：
   同一件事（"导航还在不在"）曾经有三套阈值：车机 `/state` 的 15 秒→60 秒新鲜度、
   车机 60 秒的引导断流、iPhone 20 秒的去抖。它们**不一致**，
   所以"顶栏摘要该消失"和"导航视图该退出"会在不同时刻发生，看着就像 bug。
   ⇒ 现在只留**一个权威判据**在车机侧（`AmapSignals.navigating()`），iPhone 只认
   `nav.active` 这个结果 + 10 秒去抖。
   ⚠️ 2026-09-25 第二轮把这**一个**判据本身也换掉了：从"引导广播 60 秒内 + 非巡航"
   换成"**引导广播里还有活路线（`remain/time > 0`）**" —— 因为实测证明前两条都不靠谱
   （见第 77 条）。**教训不变：同一件事只能有一个判据、一个来源。**
   **自查关键句：「同一件事我是不是在两个地方各判了一次？两边的数一样吗？」**
77. 🎯 **EXTRA_STATE 不能当"在不在导航"的判据 —— 停车不导航时它每分钟也发 8**（2026-09-25 第二轮实车）：
   证据：`bridge.log` 里 09-24 19:47~21:14（用户人在办公室、车停着没导航）
   「高德驾驶模式 -> 导航中 (EXTRA_STATE=8)」**每分钟来一对**（8 ↔ 40/315/316/2001 交替）。
   拿它置 `navActive` / 判会话 ⇒ 车机早退出导航了，IPA 还以为在导航。
   **同一份日志还暴露了更狠的一条**：车机没在导航时，那些周期广播的**载荷里夹带着 POI**
   （19:49:44「巷子口郑鸡肉柴火鸡」、21:02:51「鑫画家全屋定制」）⇒ 以前"翻到就采信"的写法
   会把目的地**重新点亮** —— 这正是用户报的「车机退出导航了、IPA 还显示路线，还得我手点退出」。
   ⇒ 现在唯一的判据：**引导广播里 `remain/remainTime > 0`**（`AmapSignals.lastLiveRouteAt`）；
   活路线断 30 秒算结束、驾驶模式变空闲/巡航后 20 秒快收（§6 第 80 条）。
78. 🎯 **目的地必须被"导航会话"背书 —— 但背书不能靠"当前在不在导航"一刀切**（2026-09-25 第二轮）：
   规则：广播里挖到的目的地先当**候选**（存着不发布），只有"带活路线的引导广播"出现才转正，
   8 秒内没等到就丢掉（`DestSignals.confirmPending` / `PEND_TTL_MS`）。
   为什么不能简单地"没在导航就不收"：**手动点导航时，目的地载荷常常比第一条引导广播早到
   几十毫秒** —— 直接挡会把"手动导航也能识别"这个功能弄坏（那是用户 9-24 明确要的）。
   **自查关键句：「这个判断的输入，会不会在'还没开始'和'已经结束'这两头都出现？」**
79. 🎯 **转向灯属性是跟着灯泡闪的（1↔2，两三赫兹）—— 真值语义只能实测，而且下发前必须先"保持"**
   （2026-09-25 第二轮实车定案）：`/logcat` 的「转向灯（原始值变化历史）」里，用户 11:47:30
   打灯时 `left/right` 在 **1 ↔ 2** 之间反复跳；而第一轮**没打灯**时两个别名都是 **1**。
   ⇒ **1 = 灭、≥ 2 = 亮**。上一版按"0 = 亮"实现，而打灯时值是 2 ⇒ 判成"什么都不亮"
   （用户报「打左转、右转、双闪都没生效」）。
   ⇒ 下发前必须**保持 2.5 秒**（`StateHub.TURN_HOLD_MS`）：iPhone 250ms 轮询会抽到随机的
   on/off，直接发就是"抖"。**同一个功能已经猜错两次（先猜别名、再猜真值）——
   凡是"属性名看着像布尔"的，一律先上真车把原始值变化历史抓回来再写逻辑。**
80. 🎯 **"结束"类事件往往根本没有广播 —— 只能自己用"数据停了多久"推**（2026-09-25）：
   高德**没有**给"退出导航"的专用广播（这一版没观察到）。所以两个 bug 的收尾都靠时间窗：
   活路线断 30 秒（模式变空闲/巡航后 20 秒快收）+ iPhone 侧 10 秒去抖。
   定这种阈值**必须先有实测的最大断流**（本轮实测 7 秒 ⇒ 取 20/30 秒，留 3~4 倍余量）。
   ⇒ `/logcat` 新加了【高德广播类型统计】（每种 KEY_TYPE 的条数 / 最近 / 字段名）：
   下一份日志就能确认"停车时 10001 还发不发""有没有现成的结束广播"，
   有的话可以把 30 秒缩到秒级。
81. 🎯 **"保持窗口"要贴着物理节奏取，别凭感觉留余量**（2026-09-25 第四轮，用户实测打回来的）：
   转向灯属性是**跟着灯泡闪的**（1↔2，实测每秒两三次、灭相约 0.3 秒），所以需要一个
   "保持"把"灭"相糊过去 —— 但上一版给了 **2.5 秒**，于是两个毛病一起出现：
   ① 关灯后还要亮一两秒才收；② 右转切左转时右边还在窗口里 ⇒ **两边同时亮几秒**。
   ⇒ 现在保持 **0.7 秒**（只够跨过灭相），换边另外用**互斥**解决：一侧亮起时，
   另一侧"最近一次亮"超过 **300ms** 就立刻判灭。
   **300ms 不是拍脑袋**：双闪时两侧是**交替**推值的（间隔比它小 ⇒ 不会被误杀），
   而真正换边时另一侧早就停了（间隔远大于它）。
   **自查关键句：「这个窗口是为了糊住什么？那个东西的周期是多久？」**
82. 🎯 **"多久没数据了"这种判据，实际延迟 = 阈值 + 检查频率**（2026-09-25 第四轮）：
   第三轮把退出判据换成"活路线断 30 秒"方向是对的，但**检查只挂在"驾驶模式变化那一刻"**
   ⇒ 真实延迟是"30 秒 + 等到下一次模式变化"（最长 40 秒），用户体感就是"根本没自动退"。
   ⇒ 改成**每一条高德广播都跑一次这个检查**（12205/10019 这些心跳广播每秒 2~3 条，
   正好当免费定时器）⇒ 延迟≈阈值本身；再把阈值按实测收到 **15 秒**
   （导航中 10001 是 2 条/秒、最长断流 12 秒；而退出导航后 31 秒里一条都没有）。
   **自查关键句：「我这个检查挂在什么事件上？那个事件多久来一次？」**
   ⚠️ 顺带一条：`/logcat` 现在会捞**高德自己打的"退出/结束导航"日志** ——
   一旦拿到原文，就能用"关键字触发"把 15 秒变成秒级（时间窗永远是最后的兜底手段）。
83. 🎯 **换底包 = 先核对"反射目标清单"，再编**（2026-09-25 第三次换底包，这次很顺）：
   我们和底包是**反射耦合**（没有编译期依赖），所以换底包**编得过 ≠ 跑得起来** ——
   类被挪包/改名/改签名，**编译和签名校验全都发现不了**，只有上车才暴露
   （历史上就吃过一次：新底包把取值类从 `CarS05InfoUtil` 换成 `S05VehicleDataMonitor`，
   装上车后车速/档位/续航/转向灯全空）。
   ⇒ 固定动作（5 分钟，`_tools/dex_class.py` 就够）：
   1. `aapt2 dump badging <新底包>`：包名必须还是 `com.deepalhome.launcher`；
   2. `apksigner verify --print-certs`：**证书 SHA-256 必须还是 `c8a2e9bc…92ab8`**
      （否则系统 uid / 覆盖安装都会失败）；
   3. 解出 dex，`python _tools/dex_class.py <classes.dex> --list <关键字>` 逐个查**下面清单里的类**；
   4. `python _tools/dex_class.py <classes.dex> <类名>` dump 成员，确认**方法/字段名没变**；
   5. `aapt2 dump xmltree --file AndroidManifest.xml`：看新底包的 provider authorities
      （别和我们注入的 `com.deepalhome.launcher.cardashboot` 撞），以及它的无障碍
      `meta-data`（我们要复用那个配置资源 ID，取不到 `inject_manifest.py` 会直接 die）。
   **反射目标清单（改注入代码时同步维护）**：
   * `com.deepalhome.launcher.util.s05.S05VehicleDataMonitor` —— `INSTANCE`、`cacheCarS05Info`、
     `virtualCarPropertyManager`、`psGetValue(String)`、四个 `*Listeners` 列表、`ALIAS_LOW_SPEED_SOUNDS`
     （老固件那条是 `com.deepalhome.launcher.util.CarS05InfoUtil`，见 `UTIL_CANDIDATES`）；
   * `com.deepalhome.launcher.carinfo.CacheCarS05Info` —— 只在 `connectOne` 里被
     `Class.forName(CACHE)` 确认"类在"，**它挪过包，就是靠这一行暴露的**；
   * `com.deepalhome.launcher.util.s05.navi.NaviIpcBridge` —— `INSTANCE`、
     `access$queryS05CurrentDestination(NaviIpcBridge, String, String)`；
   * `com.deepalhome.launcher.hud.s05eta.HudNaviManager` / `HudEtaOverlayController`；
   * `com.deepalhome.launcher.util.AssistantUtil`（语音 NLU 正则的出处）；
   * `com.openos.virtualcar.VirtualCarPropertyManager#getValue(int,int)`。
   ✅ **2026-09-25 换成 `v26.0925.beta` 的核对结果：上面全在、签名一致、provider 不冲突、
   无障碍资源可复用 ⇒ 注入代码零改动，只重编**（`新-230`/v26.0925-cd6）。

---

## 7. 换电脑后第一步（按顺序做一遍）

前提：`IPA` 文件夹是**整个拷过来**的（含 `_env`、`_tools`、
`_bases\`（两个底包：新固件 v260925 / 老固件 v26.0521）、`CarDash\keys`）。

### 拷走之前（在**当前这台**机器上先做一遍，2026-09-25 夜已做）

1. **git 工作区必须是干净的** —— 不然新机器上 `git status` 会混着两台机器的改动：
   ```powershell
   cd <IPA>\CarDash
   & '..\_env\git\cmd\git.exe' status --porcelain     # 无输出 = 干净
   ```
2. **确认该推的都推了**：本地 `HEAD` 的 hash 要与 GitHub 上 `main` 的最新 commit 一致
   （`github.com/FanXia5280/cardash`）。
3. **这四样绝不能落下**（丢了在新机器上补不回来 / 很麻烦）：
   `_env\`（992MB：便携 JDK17 / build-tools / platform / git）、`_env\ssh\cardash_ed25519`
   （推仓库的私钥）、`CarDash\keys\platform.pk8` + `platform.x509.pem`（车机同款系统签名，
   **不入 git**）、`_bases\` 两个底包（26.7MB，构建输入）。
4. **可不带**（只为省体积，缺了不影响构建）：`_refs\` 432MB（反编译素材，§15/§16 的证据）、
   `_obsolete\` 389MB（历史包）。
   ⚠️ 但 `_lantern_src\`（0.4MB）**必须带** —— 氛围灯的 `Res.java` 靠它重新生成，
   没了就只能回 `D:\Desktop\huancai` 找原工程（见 §15.1）。

0. **本机（`d:\Desktop\IPA`，2026-09-24）已复核一遍，结论留档** —— 下次再换机器照这份清单走：
   * 用户是 `梵夏`（不是文档里提过的 `Administrator`），**路径又回到了 `d:\Desktop\IPA`**；
   * 机器上已装 **Python 3.14.5**（`C:\Users\梵夏\AppData\Local\Programs\Python\Python314`，
     不是 3.12，但现有 4 个自检脚本 + `build_local.py` **实测都能跑**），
     依赖 `cryptography / pyaxmlparser / pyyaml / pillow` 齐全；
   * **`CarDash\.git\config` 的 `core.sshCommand` 又是旧机器的绝对路径**（这次残留的是
     `C:/Users/Administrator/Desktop/IPA/…`）⇒ 跑 `python _tools\setup_git.py` 修好，
     认证实测通过（`ls-remote` 拿到 `refs/heads/main`）；
   * `_env`（jdk / build-tools android-14 / platform android-34-ext12 / git / ssh 私钥 + known_hosts）、
     `CarDash\keys\platform.pk8|.x509.pem`、`_bases\` 里两个底包 **全都在**；
   * **本地真编了一次车机包**（`216 / v26.0521-cd21`）验证链路：
     `ROUNDTRIP_OK` ✅、证书 `SHA-256 = c8a2e9bc…92ab8` **与车机系统签名一致** ✅、
     产物与根目录 `Deepal-CarDash-216.apk` **逐字节相同** ✅ ⇒ 工具链可用、源码与交付包同步；
   * `adb` 有（`%LOCALAPPDATA%\Android\Sdk\platform-tools\adb.exe`，37.0.1，没进 PATH）；
   * ⚠️ 这把 `check_swift.py` 曾因 GBK 控制台崩在第 4 项检查上，**已修**（见第 6 节第 59 条）。
  * **2026-09-24 又回到本机（`C:\Users\Administrator\Desktop\IPA`，用户 `Administrator`，
    Python **3.12.10**）再复核一次**，结论：
    跑 `_tools\setup_git.py` 修好 ssh 路径（认证通过，`ls-remote` 拿到 `4e88ee5` = 本地 HEAD ✅）；
    删掉被文件夹拷贝带回来的三个音乐源码文件（见 §6 第 69 条，它们会让车机包编不过）；
    本地重编 `219 / v26.0521-cd24`：`ROUNDTRIP_OK` ✅、证书 `c8a2e9bc…92ab8` 与车机系统签名一致 ✅，
    与根目录交付包做**逐条目内容比对：1656/1656 完全相同**（整包哈希不同是压缩级别 + 签名随机，
    见 §6 第 69 条 —— 上一台机说的"逐字节相同"只在**同一台机器**上成立）。
  * **2026-09-25 夜（又回到本机 `d:\Desktop\IPA`，用户 `梵夏`，Python 3.14.5）拷走前最后一次复核**：
    * `python _tools\setup_git.py` 跑通（认证 OK，`ls-remote` 拿到 `refs/heads/main`）；
      当前 `core.sshCommand` 写的是 `D:/Desktop/IPA/...` ⇒ **到新机器上必然失效，
      第一件事仍然是重跑这个脚本**；
    * 第九轮改动 + iOS 1.6.32 **已提交并推送**（`d85e7db`，工作区干净），
      CI run #72 成功 → Release `CarDash IPA 1.6.32 (build 72)`；
    * `_bases\` 两个底包、`_lantern_src\`（87 文件 / 438KB）、`CarDash\keys` 全在；
    * 自检 `check_swift.py`（花括号配平全 OK）、`verify_ios_ver.py`（全部通过）；
    * ⚠️ 新机器上**别指望 `D:\Desktop\huancai`**（氛围灯原工程不在新电脑），
      但只要 `_lantern_src\` 跟着走了，`make_lantern_res.py` 就能照常生成 `Res.java`。
  * **2026-09-25 夜（回到本机 `C:\Users\Administrator\Desktop\IPA`，用户 `Administrator`，
    Python 3.12.10）本机复核** —— 结论：**本地链路全通，GitHub 推不上去**：
    * ⚠️ **网络把 GitHub 整个掐了（不是断网、也不是私钥问题）**：
      22 端口 `Connection timed out`；`ssh.github.com:443` / `github.com:443` 建连成功但
      立刻 `Connection reset`（SSH 版本交换都没开始）；HTTPS 同样 —— `git ls-remote
      https://github.com/FanXia5280/cardash.git` 报 `Recv failure: Connection was reset`，
      `api.github.com` 也连不上。而 `baidu.com` / `registry.npmmirror.com` / `gh-proxy.com`
      都是 **200** ⇒ 是**针对 GitHub 的协议级拦截**，比 §9 记的"只封 22 端口"更狠
      （那次 443 还能用）。⇒ **要 push 得先换网络 / 开代理 / 手机热点**，
      或者等一会儿重试（这网络历史上就抽风过）。私钥本身没问题：
      `_env\ssh\cardash_ed25519(.pub)` 在，`core.sshCommand` 已重写成本机绝对路径，
      `known_hosts` 里 `[github.com]:443` 也在。**已给 `_tools\setup_git.py` 加走系统代理
      （新增 `_tools\proxy_connect.py`）**：重跑 `python _tools\setup_git.py` 会自动检测
      浏览器用的代理（Clash 那类）并让 ssh 走它。
    * 老规矩照做：那三个被文件夹拷贝带回的音乐源码（`MediaSignals.java` / `Lyrics.java` /
      `LauncherLyrics.java`，§6 第 69 条）又在 `git status` 里 untracked ⇒
      这次**没直接删，挪到 `_obsolete\_untracked_2026-09-25\` 留档**（要彻底删再删）。
    * 自检四项**全绿**：`check_swift.py`（只有"形参遮蔽同名属性"的提示，无 error）、
      `verify_ios_ver.py`、`validate_all.py`、`java_lint.py`。

1. **装 Python** —— 一次性，`_env` 里没有便携 Python，必须本机装（见第 10 节）。
2. **配 git 推送** —— 每台电脑都要跑一次（它按当前位置重写 `core.sshCommand`，
   并固定走 GitHub 的 **443 备用入口**，见第 6 节第 62 条）：
   ```powershell
   cd <IPA>
   python _tools\setup_git.py     # 输出里出现 refs/heads/main 就是认证通了
   ```
   > 如果它报 `Connection timed out`，先别怀疑私钥 —— 那是**网络封了 22 端口**，
   > 重跑本脚本就会带上 `-o Hostname=ssh.github.com -o Port=443`（2026-09-24 已内置）。
3. **自检**（都在 `<IPA>` 下跑；脚本自己找路径，不依赖盘符）：
   ```powershell
   cd <IPA>
   python _tools\check_swift.py        # Swift 括号配平 + 顶层声明
   python _tools\verify_ios_ver.py     # iOS 版本号链路（Info.plist / project.yml / CI / Swift 四方一致）
   python _tools\validate_all.py       # 工程结构总检
   python _tools\java_lint.py          # 车机 Java 括号配平
   ```
4. **车机端**（改过 `inject\` 之后）：跑第 3 节的 `build_local.py`，
   必须看到 `ROUNDTRIP_OK` **且** `✅ 与车机系统签名一致` 才算过。
5. **iOS 端**（改过 `ios\` 之后）：push → 网页看 Actions → Releases 下 IPA。
   CI 约 3~5 分钟，装了导航 SDK 后久一点。

> ⚠️ `_tools\verify_final.py`、`verify_nav.py`、`verify_map.py`、`verify_todos.py` 是**当时那一轮改动的
> 断言脚本**（断言的是 1.5.x / 1.6.7 的状态），现在跑会报「缺失」—— 那是过期检查，**不是故障**，别去"修"。

### 本机没有 macOS，iOS 只能靠 CI 验证

所以改 Swift 时要格外小心类型/签名。上面第 6 节列的坑都是真金白银换来的 —— **改之前对照一遍**。

---

## 8. 怎么核对装的是哪版

- iPhone：设置 → 最下面「版本」
  - `App 版本` = IPA 版本（要和 Release 名字对得上）
  - `桥接版本` = 车机 APK 版本（对应关系：`210`=`v26.0521-cd15`、`211`=`cd16`、`212`=`cd17`、`213`=`cd18`，依次递增）
- 车机：诊断 → 车机运行状态 → 第一行 `桥接版本`

---

## 9. Git

仓库在 `<IPA>\CarDash`，remote = `git@github.com:FanXia5280/cardash.git`（SSH 私钥在 `_env\ssh`）。

```powershell
cd <IPA>\CarDash
$git = '..\_env\git\cmd\git.exe'     # 便携 git；用系统 git 也一样，反正 sshCommand 指向 _env
& $git status
& $git log --oneline -5
# 提交推送
& $git add -A; & $git commit -m "…"; & $git push origin main
```

**push 报 `Permission denied (publickey)` 怎么办**：九成是 `CarDash\.git\config` 里的 `core.sshCommand`
还存着上一台机器的绝对路径。这个坑 2026-09-23 踩过一次 —— 里面残留
`C:/Windows/System32/OpenSSH/ssh.exe -i D:/Desktop/IPA/_env/ssh/cardash_ed25519`（那台机器早就没这个路径了）。
修法：`python _tools\setup_git.py`（按当前位置重写并实测认证）。

> ⚠️ **这个坑会反复复发，因为文件夹在两台机器之间来回拷**（2026-09-25 已是第三次：
> 昨晚在 `C:\Users\Administrator\Desktop\IPA` 那台干的活，今早整个文件夹拷回 `d:\Desktop\IPA`，
> `core.sshCommand` 就又是 `C:/Users/Administrator/...` 了）。
> **症状有个更隐蔽的版本：不报 `denied`，而是命令直接没输出**（比如 `git ls-remote --tags`
> 打印空白）—— 因为它是 ssh 启动失败、"拒绝访问"被吞掉了。
> ⇒ **规矩：只要换过机器/拷过文件夹，动手第一件事就是 `python _tools\setup_git.py`**，
> 它最后会打印 `OK <hash> refs/heads/main` 才算通。

**push 报 `ssh: connect to host github.com port 22: Connection timed out` 怎么办**：**不是私钥问题**
（`denied` 才是），是这台机器的网络封了 22 端口。2026-09-24 踩过一次：
15 分钟前 `ls-remote` 还好好的，转眼就 timeout —— 同一个网络里 22 端口被中途掐掉是常态。
修法：`python _tools\setup_git.py`（脚本现在固定带 `-o Hostname=ssh.github.com -o Port=443`，
并自动补 `[github.com]:443` 的 host key）。想手动看它到底通不通：
```powershell
cd <IPA>
& '_env\git\usr\bin\ssh.exe' -p 443 -i '_env\ssh\cardash_ed25519' `
    -o UserKnownHostsFile='_env\ssh\known_hosts' -T git@ssh.github.com
# 出现 "Hi FanXia5280! You've successfully authenticated" 就是通的
```

⚠️ push 改 `inject/**` 或 `tools/**` **不会**触发 iOS 打包（workflow 只监听 `ios/**` 和它自己）。

最近几次提交（从新到旧）：

```
d85e7db  iOS 1.6.32：删掉「设置 → 测试」三项模拟功能（模拟路线/转向灯/超速）+ 高德模拟导航（startEmulatorNavi），为发布正式版（run #72 成功 → `build-72`）；顺带把此前未提交的车机 238 那批一起补提（内置氛围灯 lantern/ + LanternEntry、退出导航快判 QUICK_END_MS=4s、回退卡片自定义 App）
924ec97  车机 cd26(224)/cd3(225) + iOS 1.6.30：修转向灯一直闪（真值语义 0=亮/1=灭，实车证据）+ 车机退出导航 IPA 不退出（清导航字段只跑一次 + 双阈值错位）
f2d4a63  iOS 1.6.29 + 车机 cd25/cd2 重建：修车身信号取值类只写新底包名（旧底包装车后车速/档位/续航/转向灯全空）→ 两个候选类依次尝试；加一键复制全部诊断
7cb3ef3  iOS 1.6.28：退出导航清掉残留路口放大图；模拟双闪升级为模拟转向灯（关/左/右/双闪）+ 新增模拟超速红光开关
d22bc86  iOS 1.6.27：修模拟导航下点退出导航要等 20 秒去抖才收路线（立刻收）+ stopNavi 推到下一 runloop（不再卡那一帧）
dca0285  模拟器版修复：桌面自己的服务在 Android 15 缺前台服务类型导致 MissingForegroundServiceTypeException 崩进程 → --emu 包补权限 + foregroundServiceType=dataSync
fe9fe1e  iOS 1.6.26：退出导航不再重建地图（治车标跳回定位点卡一下）+ 设置里加模拟双闪开关；build_local 加 --emu
88aaf16  iOS 1.6.25 + 车机 cd25：修目的地坐标被舍到 2 位小数（约 1km 误差 / 切目的地不重算）、转向灯别名改成实车在推的 vc_alias_turn_*_signal_on、加退出导航按钮、导航视图拆除时停引擎
5e63f32  车机移植到新底包 v26.0923：CarS05InfoUtil→S05VehicleDataMonitor（psGetValueSync 变异步）+ 底包已 multi-dex，注入改自动挑 classes3.dex
4e88ee5  车机 cd24（219）+ iOS 1.6.24：把「目的地」诊断真正接到 logcat/diag 上（DestSignals.report() 之前是死代码），App 诊断增加「状态快照（JSON）」按钮
ce8ab55  iOS 1.6.23：按使用习惯自查出的三个 bug —— plist 声明了后台定位、代码里却关着（切后台/锁屏后整个仪表冻住）；poll() 的 inFlight 没有复位兜底（卡住就永远不再刷新且不报错）；熄火下车后 routeDest 一直留着（失联 >150 秒才收）
d4e026a  车机 cd23（218）：原厂导航 + 第三方高德同时开时顶栏数字来回跳 —— 加导航字段来源仲裁（高德>通知/读屏>车身信号，5 秒让位），四处写入点全部接上；destVoiceIgnored 留证据（只动 inject）
5a1ae2f  车机 cd22（补）：目的地上锁收紧 —— 直接 extra 只认确切字段名，不再对任意 key 做 JSON 递归（那会把 EXTRA_POI_RESULT 搜索结果也翻一遍）；递归只从路线载荷 key 进入，并支持嵌套 Bundle（只动 inject，不触发 iOS CI）
adf3faa  车机 cd22 + iOS 1.6.22：修长途必掉导航（30分钟墙钟/单帧active:false/目的地被别日志改掉）＋ 支持车机手动导航被 IPA 识别（从高德广播里取目的地，只读不干扰）
e1d451b  iOS 1.6.21：修「车标看不见绿色呼吸」——光晕半径小于白圈那段全被实心圆盖住、露出时 easeOut 已把透明度吃到 ~8%，等于没画；改成从白圈外沿起扩散(0.8→1.7)、填充 0.30→0.50，并补动画自愈(切前后台/视图重挂后自动续上)
2b09c41  iOS 1.6.20：修复非导航态车标消失（1.6.19 删自绘路线时把夹在中间的 carPin 创建代码一起删了）
17650b7  iOS 1.6.19 + 车机 cd21：删掉自绘路线（导航已用高德官方）与车机音乐整块；修车标呼吸光晕没对准圆心
1bb1bba  iOS 1.6.18：非导航车标改成苹果「查找」那种会呼吸的实心圆（CarDotView + CAShapeLayer 呼吸动效）
180c600  iOS 1.6.17：修进软件绿点先居中后归位（非导航地图 convert 未就绪时排队重试，横竖屏同一处）；模拟导航不再闪北京（起步前垫非导航地图，didStartNavi 后再切导航视图）
270706d  修 CI：showCrossImage 回调在 Swift 里要写 showCross（参数类型重复的词会被省）；按要求关掉牵引线 showVectorline=false
4027ae6  iOS 1.6.16：接入路口放大图（showCrossImage=false 才拿得到图 + addDataRepresentative(self)），盖在速度那一格上，只遮速度不挡别的元素
251765b  修 CI：补 AppIcon（actool 要求图标集）+ 车机 cd20 下发电子眼；红色脉冲改成只在【会拍限速的电子眼(0/8/9) + 超速】才冒
63ad59b  iOS 1.6.15：关掉两个收费接口；修模拟导航先显示北京；非导航车标改绿/灰实心圆；修地图卡顿（poll 去重 + layout 尺寸闸门）；ETA 不再灰；加入深蓝车标(Assets 官网抠图)
75b4116  iOS 1.6.14 + 车机 cd19：走过的路置灰、电子眼距离、红绿灯倒计时、超速脉冲；限速下发 + 自算超速红光；转向灯采集 + 绿光闪烁；120Hz；启动循环车标动画
500bcc8  修 CI 编译错误：update 的形参 view 遮蔽了同名属性 view，导致 if let v = view 编不过（改名为 naviView）
a7092f8  iOS 1.6.13：旋转/启动时车头位置即时归位（layoutSubviews 补位）+ 启动遮罩藏住初始化 + 模拟导航改用官方 startEmulatorNavi + 只有目的地变化才重算路线
af67cd1  iOS 1.6.12：巡航不跟车（官方文档证实），非导航态回退 MAMapView 但调成与导航态同观感——导航样式底图 .naviNight + 高德官方车标 + 共用 MapAnchor 精确定位
1b3b857  iOS 1.6.11：非导航状态改用同一套导航引擎的巡航模式（不再两套地图）+ 去掉会露黑边的地图偏移 hack + 锚点横屏 0.64/竖屏 0.66
0399d10  iOS 1.6.10：导航静音 + 外部定位(治先闪北京) + 引擎预热(治点导航卡一下) + 自车图标位置(横屏右/竖屏下) + 顶部遮罩压更黑 + 模拟路线补预计到达
bec6fd7  CI：全步骤日志汇总 + 已装则跳过 brew/gem + pod install 重试 3 次 + 失败统一定位（以前只打 build.log，pod install 挂了什么都看不到）
c0ecf55  修 IPA 目的地乱跳（车机侧把 GPS 上报当成了目的地）+ 导航界面改版：去转向卡/比例尺、终点图标、日夜模式、竖屏总里程挪位
5049b9b  iOS 1.6.8：MARKETING_VERSION 1.6.7 -> 1.6.8（上一提交只改了 Swift，版本号忘了跳）
41dda3e  构建脚本去掉写死的 d: 盘路径，改成按脚本位置定位（换机器后不用改）
2f7cb89  iOS 1.6.8：导航只留路线 + 地图改白天样式
5a987cd  去掉 AMap3DMap，解决 MAMapKit 重复符号
d12c1dd  按官方文档改 AMapNaviDriveView 初始化与回调签名
b808fcd  iOS 导航视图类名是 AMapNaviDriveView，不是 AMapNaviView
883ba1a  bridging header 显式 import AMapNaviView.h
f3fdbde  加 bridging header
d2f249e  按 CI 报错修正 NaviKit 4 个 API 名
9049b05  接入高德官方导航 SDK
8ae32ae  路线改成高德原版单层画法
```

---

## 10. 本机环境清单（Windows 要有什么）

| 组件 | 位置 / 版本 | 用途 |
|---|---|---|
| JDK 17 | `_env\jdk\jdk-17.0.13+11`（便携） | javac 编译车机代码 |
| Android build-tools 34 | `_env\build-tools\android-14`（便携） | `d8.jar` / `zipalign.exe` / `apksigner.jar` / `aapt2.exe` |
| Android platform 34 | `_env\platform\android-34-ext12\android.jar`（便携） | javac / d8 的 classpath |
| Git（MinGit） | `_env\git\cmd\git.exe`（便携，自带 `usr\bin\ssh.exe`） | 推仓库 |
| SSH 私钥 | `_env\ssh\cardash_ed25519`（+ `known_hosts`） | 已登记在 GitHub 账号里 |
| 签名密钥 | `CarDash\keys\platform.pk8` + `platform.x509.pem` | 车机同款 system 签名（不入库，别丢） |
| **Python** | **必须本机装**（`_env` 里没有便携 Python）。文档原定 3.12；**本机 2026-09-25 实测装的是 3.14.5，现有脚本全都能跑**（能跑就别折腾降级） | 所有 `.py` 脚本 |
| 氛围灯素材 | `_lantern_src\`（87 文件 / 438KB，见 §15.1） | 重新生成 `Res.java` 的源；**原工程 `D:\Desktop\huancai` 不在新电脑也能干活** |

工具链丢了：`python CarDash\tools\setup_env.py`（约 260MB）；Git 丢了：`python _tools\setup_git.py`。

> **换电脑时体积参考（2026-09-25 实测，整个 `IPA` 约 2.2 GB）**：
> `_env\` 992MB · `CarDash\` 263MB · `_refs\` 432MB（可不带）· `_obsolete\` 389MB（可不带）·
> `_apk\` 18MB · `_tools\` 18MB · `_bases\` 27MB · `_lantern_src\` 0.4MB。

### Python 装法（本机 2026-09-23 装的是 3.12.10）

```powershell
winget install --id Python.Python.3.12 -e -s winget --scope user `
  --accept-package-agreements --accept-source-agreements
# 开个新终端验证
python -V
python -m pip install cryptography pyaxmlparser pyyaml
```

依赖：`cryptography`（`verify_key.py` / `keycheck.py` / `inspect_apk.py`）、
`pyaxmlparser`（`dump_manifest.py`）、`pyyaml`（`validate_all.py`），其余全是标准库。

> **踩坑**：`AppData\Local\Microsoft\WindowsApps\python.exe` 是 0 字节存根，敲 `python` 可能反而命中它 ——
> 它**不报错、只静默什么都不干**。所以"脚本跑完没输出/输出是空的"，先 `Get-Command python` 看是不是这个存根。
> 本机已把真 Python 排在用户 PATH 前面，但 **IDE 内置终端继承旧 PATH，要重启 IDE 才生效**。

### 可选

- `adb`：`%LOCALAPPDATA%\Android\Sdk\platform-tools\adb.exe`（没进 PATH；只在模拟器/车机调试时用）
- 模拟器测试包：`build_local.py` 加 `--no-shared-uid`（Android 14+ 禁止非预装应用加入 `android.uid.system`，
  正式包装不上模拟器；**这个包别装车机**）

---

## 11. 车机导航数据从哪来（挖 D.apk 的结论）

> **2026-09-24 补：车上其实有两个导航 App，别搞混**（两个 APK 都在根目录）：
>
> | APK | 包名 | 是什么 | 我们怎么用它 |
> |---|---|---|---|
> | `后装导航.apk` | `com.wt.mahjong`（高德地图（深蓝专版）9.5.0.600013）| **用户日常用的那个**，后装的高德车机版 | 它的**广播**给导航数据，现在还给出**目的地**（见 §6 第 64 条）|
> | `原车导航.apk` | `com.tinnove.wecarnavi` | 原厂导航（tinnove＝下面那条 IPC 链路的同一家）| 只通过 D.apk 的 IPC 暴露；`S05Navi` 在探测 |
>
> ⚠️ 别把 `AmapSignals` 注释里那句「这台车上的高德包名是 `com.wt.mahjong`（深蓝定制版），
> 不是 amapauto」理解成"不是高德"——它就是高德车机版，只是深蓝定制包名。

**结论：车机原生导航的目的地和路线是走 IPC 拿的，跟语音没关系。**
（用户提的「D.apk 不用语音也能拿到目的地，怎么实现的」= 这一段。）

链路全在 D.apk 自己的类里，**同进程反射就能用**：

```
HudNaviManager    com.deepalhome.launcher.hud.s05eta.HudNaviManager      （Kotlin object → 静态 INSTANCE）
  │  字段：isNavigating / lastPrimaryLine / lastSecondaryLine / lastRenderedText / overlayController
  └─ NaviIpcBridge   com.deepalhome.launcher.util.s05.navi.NaviIpcBridge
       ├─ 绑服务       com.tinnove.skill.service-new
       ├─ AIDL 调度器  com.tinnove.navi.skill.IPCSkillDispatcher（运行时混淆名 m8/IPCSkillDispatcher）
       │               queryInfo(String):String / sendRequest(String, IPCResponseObserver)
       │               sendResultRequest() / calculateRoute() / showPoi() / startNavigation() / search()
       ├─ AIDL 回调    com.tinnove.navi.skill.observer.IPCGuideInfoObserver（运行时 n8/…）
       │               onNaviStart / onNaviStop / onNaviProgress / onNaviRemainDistance /
       │               onNaviInfo / onNextRoadName / onRemainInfo / onRouteInfoChanged / onTurnInfo /
       │               onLaneLineInfo / onElectronicCameraInfo / onTrafficInfo / onSpeedLimitInfo …
       ├─ 主动查终点   queryS05CurrentDestination(...)（日志：「主动查询当前路线终点 trigger=」）
       └─ 解析结果     parseS05BasicPoiJson(String)
                       → NaviIpcBridge$S05PoiResult { String name; Double latitude; Double longitude; }
                         ← **这就是目的地名称 + 坐标**
```

| 类 | 关键成员（`_tools\dex_class.py` dump 的原文） |
|---|---|
| `NaviIpcBridge` | 字段 `INSTANCE` / `ipcSkillDispatcher` / `bound` / `started` / `lastPoiSignature` / `activeGuidePoiSignature` / `pendingSystemStartedPoiSignature` / `lastRouteQueryKey`；方法 `dispatchS05Poi` / `handleS05PoiFromSystem` / `requestS05ResponseCommand` / `resetS05GuideState` / `queryS05CurrentDestination` |
| `NaviIpcBridge$S05PoiResult` | `name` / `latitude` / `longitude` |
| `HudNaviManager` | `isNavigating` / `lastPrimaryLine` / `lastSecondaryLine` / `lastRenderedText` |
| `HudEtaOverlayController` | `primaryLine` / `secondaryLine` |

D.apk 里**其它**取数路子（都试/查过，别重复走）：

- 语音 NLU 日志（`AssistantUtil`，正则 `"destName"` / `"dest"` / `"destNavi"`）——
  就是现在用的那条，**只覆盖"喊语音导航"**
- 无障碍读屏（清单里的 `AutoNaviAccessibilityService`）—— 注入侧已有服务，但实测连接次数 = 0
- 通知监听 —— 实测 listener 未连接、收到通知 0 条
- `content://` provider：D.apk 里只有厂商 OAID 那几条，**没有导航 provider**
- 高德广播 `AUTONAVI_STANDARD_BROADCAST_SEND`：D.apk 只用它收发**驾驶状态和日夜模式**；
  `ROUTE_REMAIN_DIS` / `SAPA_DIST` / `CUR_ROAD_NAME` 这些键**在 D.apk 字符串池里根本不存在**
  （是从作者另一个项目 S05HudTrafficLight 移植的 —— 但那套在真机确实收得到引导信息，
  现在导航卡片的数据就是它给的）

复现命令：

```powershell
cd <IPA>
python _tools\dex_class.py _apk\classes.dex com.deepalhome.launcher.util.s05.navi.NaviIpcBridge
python _tools\dex_class.py _apk\classes.dex 'com.deepalhome.launcher.util.s05.navi.NaviIpcBridge$S05PoiResult'
python _tools\dex_class.py _apk\classes.dex com.deepalhome.launcher.hud.s05eta.HudNaviManager
```

⚠️ **别急着照这张图写代码**：`IPCGuideInfoObserver` 的回调要自己实现 AIDL 的 `$Stub`
（必须是个真 Binder，动态代理不行）。所以现在的做法是先用 `S05Navi.java` 那个探测
把真实字段格式采回来（`/diag` → 「车机原生导航 IPC」段），再决定走 `queryInfo` 同步查、
还是复用 D.apk 已经注册好的 observer。

---

## 12. 文档维护约定

- **每做完一轮改动就更新本文档**，第 4 节（进度）、第 6 节（新踩的坑）、第 9 节（提交记录）至少动一处。
- 版本、产物这类事**写实测结果**，不写"应该"。第 4 节的「版本实况」就是这么核出来的。
- **不写死盘符**：路径一律用 `<IPA>` 或相对路径。2026-09-23 项目从 `d:\Desktop\IPA` 搬到
  `C:\Users\Administrator\Desktop\IPA`，所有写死 `d:\` 的地方（`_tools` 22 个脚本 + `CarDash\tools` 2 个）
  当时全都静默失效或报错，才全部改掉。

### 变更记录

| 日期 | 改了什么 |
|---|---|
| 2026-09-26（五十四） | **最后一次自检：把「主动查终点」导航 IPC 物理删除（不再只是默认关）**。用户关键信息：**「没做 Cardash 时，用没内置服务的 D 桌面 + 内置 HUD 的高德，从不出这个 bug」⇒ 问题必在 Cardash 代码**。系统性重扫 inject 全部对外动作，结论**唯一**：会碰车机导航的只有 `S05Navi` 的 `queryS05CurrentDestination`（原 316/318 行 `mQDest.invoke`）。其余：`VendorSignals` 的 4 个 `invoke`（`startMonitor`/`bindVirtualCarPropertyManager`/`registerVirtualCarCallbacks`/`connectPolymericService`）是**车辆数据采集挂钩**（读车速/档位/续航，不碰导航）、`CarSignals` 读车辆属性、`Settings.Secure` 只写无障碍/通知授权、`startService` 只起我们自己的服务、`AmapSignals` 纯接收广播（`EXTRA_STATE=8` 不可信，见上条）。⇒ 修法升级：`probeDestination` 改成**空实现**（不再 invoke，连 `probeEnabled` 被误开都不可能查），`/setprobe` 路由提示已废弃；dex 验证「S05Navi 主动查终点」日志字符串已消失。产物 **`旧-Deepal-CarDash-249.apk`（`v26.0521-cd41`）**，提交 `58aae8e` |
| 2026-09-26（五十三） | **iPhone 加「导航探针」开关按钮（用户不用碰浏览器了）**。① `HttpServer`：`/setprobe` **放行局域网**（它是 iPhone 用的调试开关，默认关、进程重启即复位），其余 `/setnav /setfull /setwrite` 仍**只允许车机本机**；② `ios/SettingsSheet.swift`：车机诊断区加「打开导航探针（对照测试）」「关闭导航探针（默认）」两个按钮，点一下即请求 `/setprobe?on=1/0`；③ 产物：**`旧-Deepal-CarDash-248.apk`（`v26.0521-cd40`）**、iOS `1.6.38 → 1.6.39`（提交 `a785876`）。⚠️ **重要排查认知（本轮核对日志得出的）**：车机 `AmapSignals` 注释明确 —— **`EXTRA_STATE=8`（"导航中"）不可信**（停车不导航时也每分钟发一次 8，还会 8↔40 每分钟翻一次），所以日志里海量「高德驾驶模式 -> 导航中(8)」**不代表真的在导航**；真正代表导航的只有 **「目的地(高德广播)=xxx」** 和 **「目的地已随导航会话结束清空」**。判断"是否又自动导航了"要看目的地广播，不能看 EXTRA_STATE。 |
| 2026-09-26（五十二） | **四个 bug 修复（车机 247 / iOS 1.6.38）**。① **氛围灯渐变跑十几二十分钟后变成固定颜色** —— 根因：`GradientPlayer.tick` **没有 try/catch**，`LedOutput.sendRawColor()` 一旦抛异常（蓝牙断连/写失败，车上跑久了必然碰到一次），后面的 `handler.postDelayed` 就不执行 ⇒ 循环**彻底停摆**，灯停在最后一帧 = 固定色。修：tick 整体包 try/finally，保证下一帧一定排上；② **上车不会自动恢复上次渐变** —— 两处：`restoreGradient` **只在 `onStateChanged`（蓝牙刚连上那一瞬）调用**，桌面进程起来时蓝牙若已连着就不再触发；且 `isPlaying()` 只看 `item != null`，循环死了它仍返回 true ⇒ 恢复逻辑以为在播而永不重播。修：加心跳 `lastTickAt`（3 秒内 tick 过才算在播）+ `start()` 里无条件恢复一次、2 秒后再兜底补一次（防 `reapplyDeviceConfig` 覆盖）；③ **进地下室无 GPS 时速度乱飙** —— 根因：`LocalSensors.derivedKmh()` = 位移÷时间，**无任何上限**；地库定位漂移一秒几百米 ⇒ 算出上千 km/h。**修（iOS）**：上一帧也要可信（`horizontalAccuracy < 100`）、`dt < 1s 且位移 > 80m` 判漂移丢弃、速度上限 300km/h（异常大值不采信也不写回 `lastGoodSpeedKmh`）；④ **iPhone 电量百分比一直显示上车时的值** —— 根因：`VendorSignals.poll` 里 `if (hub.soc == null && ...)` ⇒ **只在第一次算出来，之后永不更新**；而且用的是「续航÷满电续航」折算，没读缓存里现成的真实值。修：`readCache` 里优先读 `cacheCarS05Info.socPercent`（日志/截图中确实是 84）并每轮刷新，读不到则把 `hub.soc` 清空让折那段**每轮重算**；⑤ **胎压要动态** —— 缓存本来就 250ms 读一次（`startPolling` ⇒ `readCache` ⇒ `readTire`），天然是动态的，无需改动；⑥ 产物：**`旧-Deepal-CarDash-247.apk`（`v26.0521-cd39`）**；iOS 版本号 `1.6.37 → 1.6.38`（提交 `e432ab2`，改了 `ios/` 会触发 iOS CI） |
| 2026-09-26（五十一） | **只读范围纠正 + 胎压真相 + 端口安全**（用户三条反馈：①"只读是不要给高德下发任何指令，不能影响正常功能，只是修复退出导航后自动重开，别把其他功能阉割了"；②"确保同网络下别的设备不能改车机下发的数据——之前把包给其他 AI 做逆向，发现了端口 8765 并指出别人可通过浏览器下发指令"；③截图实锤胎压字段）。① **只读收窄**：上一轮把无障碍/通知使用权自动启用也禁了，**过头了** —— 那两个只是写我们自己的授权，不影响高德，还是功能前提 ⇒ 已恢复；`ReadOnly` 现在**只管 `S05Navi.probeDestination`**（给导航 IPC 查终点，默认关 = 本轮 bug 的修复本体）。⚠️ 排查原则记牢：**改"只读/安全"这类横切开关时，逐个点过一遍再禁，禁之前先问"这个动作会不会碰高德"**；② **胎压真相**：截图是 `/diag` 里 `CarS05InfoUtil.cacheCarS05Info` 的 dump —— `tirePressureFrontLeft = 2.59` 等四个字段**一直都在**（String，已格式化 bar），说明不是"字段是数组"，而是**新/旧底包各有一个 util、各挂各的缓存**，当前绑的 util 缓存里没胎压。修法：`readTire` 读不到时遍历 `UTIL_CANDIDATES` 换另一个 util 的 `cacheCarS05Info` 再读一次；③ **端口安全**：`HttpServer.serve()` 里加校验 —— **`/set*`（/setnav /setfull /setwrite /setprobe）只允许 `isLoopbackAddress()` 的请求**，局域网其它设备访问一律 403；`/state /diag /` 等只读路由保持开放（iPhone 要用）；④ 产物 **`旧-Deepal-CarDash-246.apk`（`v26.0521-cd38`）**，根目录只留这一个；提交 `0799183` |
| 2026-09-26（五十） | ⚠️ **根目录只放「下次要装的那一个 apk」**（用户提醒：「你忘记了根目录只放我下次需要安装的 apk」）。历史包一律移到 `_obsolete\_apk_<日期>\`，根目录永远只保留最新一个待装包 |
| 2026-09-26（四十九） | **移除 D+ 桌面内置的 HUD 红绿灯**（用户：「桌面设置里的氛围灯设置保留，那个 HUD 可以移除了，把内置的相关代码也移除，我还是用原来内置到高德里的 HUD 红绿灯显示」）。① **日志结论（看 `E:\log.txt` / `E:\log2.txt`，各 401 行）**：内置 HUD **没有下发路线** —— 日志里全是**收到**的广播（`[Amap] 引导信息 10001` / `[Amap] 红绿灯广播 60073` / `其它广播 12205/13011/13022`），**没有任何下发路线或设置目的地的记录，也没有 `sendAmapHudStart`**；**但它确实在频繁向车机发指令**：`[HudWakeHelper] sendDipStatus(51)/(10)/(9)` 反复出现（22:04:40、22:05:07、22:05:21、22:05:41、22:05:59…），那是往车机 DIP 服务发的 HUD 状态指令 —— 不是路线，但属于"下发命令"，与只读要求冲突；② **改动**：删 `inject/HudEntry.java`；`BootProvider` 去掉 `HudEntry.install`；`build_local.py` 编译树去掉 `('hud','src')`；`hud/` 整个移到 **`_obsolete/_hud_builtin_20260926`**（留档、不再编译）；`LanternEntry` 剥掉 HUD 那半（去掉 `import HudPanel`、`HudRuntime.bootstrap` 调用、侧栏「HUD 红绿灯」项、`KIND_HUD` 面板分支）⇒ 桌面设置里**只剩「氛围灯设置」一项**；③ 产物 **`旧-Deepal-CarDash-245.apk`（`v26.0521-cd37`，10.3MB）**，dex 里已核对 `hudtraffic` 出现次数 = **0**、`HudEntry` 不在、`LanternEntry` 与 `ReadOnly` 都在 |
| 2026-09-26（四十八） | ⚠️ **出包规矩变了：从今天起只编「旧底包」，新底包停更**（用户原话：「现在我们只需要更新旧包就行了，新包就不更新了，我不会装新包了」）⇒ 以后每轮只跑 `python tools/build_local.py "..\=_bases\旧底包-v26.0521.apk" ..\_env <号> v26.0521-cd<号>`，根目录只交付 `旧-Deepal-CarDash-<号>.apk` |
| 2026-09-26（四十七） | **严重 bug 修复（一）：退出导航后定制高德自动重开 + 改成「只读」模式**。① **时间校准（重要排查常识）**：用户说 bug 发生在晚上 22 点，而日志里 bridge.log 全是 `18:05~18:16` —— 因为**车机系统时钟比真实时间慢约 4 小时**（`/state` 里 `ts=1790432050357` 换算成北京时间 = **2026-09-26 22:14**，与用户描述吻合）⇒ 看车机日志要先 +4 小时；② **根因**：`S05Navi.probeDestination()` 每 30 秒（= `PROBE_MIN_GAP_MS`）反射调 D.apk 的 `queryS05CurrentDestination("diag","cardash")` 去 IPC 问「当前路线终点」——**这不是只读查询**，会触发车机导航 skill 把挂着的 POI 重新 start ⇒ **深蓝定制版高德（com.wt.mahjong）隔几十秒重新开始导航上次目的地**（239/241 都复现；原车导航不受影响，因为调度层是梧桐 tinnove、执行层才是定制高德）。咱们 inject 里**一条 `sendBroadcast` 都没有**，不存在直接叫高德导航的代码；③ **改成只读（用户要求：「只从 D 桌面做到只读文件，不下发命令」）**：新增 **`inject/ReadOnly.java`** 总开关（默认 `enabled=true` 只读），并接到所有会写车机/下发指令的点 —— `AccessibilityHelper`（不再写 `Settings.Secure` 自启用无障碍）、`NotificationHelper`（不再写通知使用权）、`S05Navi.probeDestination`（默认关）、`HudRuntime.wakeHudLater/bringFrontLater/bounceHudSwitch`（不再补发 HUD 指令）、`S05HudWakeHelper`（不再 invoke HUD 设置 / 发广播）、`AccessibilityKeepAlive`（不再写 Settings / 跳设置页）。**保留**：读车机字段、注册监听、起我们自己的桥接服务、`/state` `/diag` 等 HTTP 接口、用户在界面上主动点的操作；④ 需要临时放开用 **`/setwrite?on=1`**（重启桌面回到默认只读）；hud 侧用 `allowWriteSoft()` **软引用** ReadOnly（`Class.forName` 找不到就保持原行为），这样拷给公司的那份源码不受影响；⑤ **胎压修复**：iPhone 胎压页四个轮子一直 `--` —— 根因是车机缓存里胎压是**数组**（`/diag` 里看到 `[Ljava.lang.Integer;@368d9a0`），而 `VendorSignals.readTire` 只按 `tirePressureFrontLeft/…` 四个 **String** 字段读 ⇒ 全 null ⇒ `/state` 里 `tire:null`。改成：先读四个 String，读不到就 `findTireArray()` 遍历缓存对象找名字带 tire/press 的数组（`int[]/float[]/double[]/long[]/Object[]`）取前四个，并按「≥100 当 kPa 除 100」折成 bar 字符串（iPhone 端 `TireInfo` **不做换算**，只原样显示 + 固定标 `bar`）；同时把缓存里带 tire/press 的字段名/类型写进 `src.tireFields` 供下次核对；⑥ `tools/build_local.py`：javac 与 d8 都改走 **`@argfile`** —— 源码变多后命令行撞 Windows 32K 上限（`FileNotFoundError: [WinError 206] 文件名或扩展名太长`）；⑦ 修了合并远端后两处编译断点：`HudRuntime.ensureAlive()` 补上（远端那版没有）、`HudEntry` 改用 `bootstrap(app)`（不是 `start`）；⑧ 本轮产物 **`旧-Deepal-CarDash-244.apk`（`v26.0521-cd36`，10.9MB）**，新包 242/243 作废不再更新 |
| 2026-09-26（四十六） | **逆向对比新包 vs 9/25 平台签名版，确认无回退（用户要求："对比你打包好的软件和之前的 HUD红绿灯_平台签名版_20260925.apk 差异，逆向一下，确保新打包的 apk 不会出现其他 bug"）**。工具：新增 **`_tools\diff_hud_apk.py`**（androguard 4.1.4 解析 `classes2.dex`，比对类清单/方法签名/每方法指令条数；⚠️ androguard 4.x 用 loguru，必须 `logger.remove()` 否则 DEBUG 日志刷屏）。**结论：零差异** —— ① 类数 126 = 126，无新增/删除类；② 方法级 0 增 0 删；③ 18 个核心类（``）指令条数**逐条相同（差 +0）**；④ Manifest 完全一致（包名 `com.s05.hudtraffic`、version 1.0/1、12 项权限、activity 2 / service 4 / receiver 3，**无 `sharedUserId`**）；⑤ 资源（assets/res/resources.arsc/AndroidManifest.xml）字节数完全一致。过程中发现并清掉的残留：`Prefs.java` 里 7 个 `NaviMap*` 死方法（`isNaviMapEnabled/setNaviMapEnabled/getNaviMapX/Y/setNaviMapPos/getNaviMapSize/setNaviMapSize`，无任何调用点），已从 `D:\S05` 与 `D:\S05_HUD_PURE` 两份源码删掉并重编 ⇒ 复检 0 差异。**签名**：用 `D:\S05\tools\keys\platform.pk8/.x509.pem`（apksigner 需先设 `JAVA_HOME`，否则报 "JAVA_HOME is not set"）重签 ⇒ `D:\S05\HUD红绿灯_平台签名版_20260926.apk` = **209,987 B，与 9/25 那份字节数完全相同**，证书 SHA-1 `27196e386b875e76adf700e7ea84e4c6eee33dfa`（= 车机系统签名）⇒ 可直接覆盖装车。注：`WRITE_SECURE_SETTINGS` 只在 `AccessibilityKeepAlive` 用且有权限判断（无权限时降级"需手动开启无障碍"），debug 包不会崩 |
| 2026-09-26（四十五） | **还原源码给用户拷去公司做内置（用户叫停今晚的 2D 地图方向：「我现在想废弃今晚和你的聊天，把源码拷到公司让他直接把 HUD 红绿灯内置进去，你已经有点跑偏了」）**。① **诊断**：`D:\S05\S05HudTrafficLight` **本身已编不过**（不是今晚弄坏的，是 9/25 夜里加的自绘 2D 半成品：`Navi2DRenderer` 调 `MapTiles.get/tile2lon/tile2lat` 与 `proj.totalM`，这些成员在 `MapTiles`/`NaviRoute` 里根本不存在）；今晚改的一直是副本 `IPA\_hud_src\`，**没动 `D:\S05`**；② **还原**：备份 `D:\S05\S05HudTrafficLight_bak_20260926`（42 个 java，原样留存）后，用纯净版覆盖 `D:\S05\S05HudTrafficLight` ⇒ **36 个 java（纯红绿灯，无 2D）**，`gradle assembleDebug` **BUILD SUCCESSFUL**，apk `206,768 B ≈ 202KB`（与你 9/25 那份 `HUD红绿灯_平台签名版_20260925.apk` = 209,987 B 基本一致）；另存等价副本 **`D:\S05_HUD_PURE`**（英文路径，gradle 不认中文路径）；③ ⚠️ **apk 体积坑（重要）**：今晚加的 `implementation 'com.amap.api:3dmap:latest.integration'` 即使把 `NaviMapOverlay/RoutePlanner/LocationFeed` 三个 java 全删掉，**`.so` 照样打进包** ⇒ `lib/arm64-v8a/libAMapSDK_MAP_v10_0_600.so` 19.1MB + `armeabi-v7a` 12.8MB = **31.9MB / 共 37MB**。必须**同时从 `app/build.gradle` 删掉这行依赖**并重编 ⇒ 37MB → **202KB**。以后任何"加地图 SDK"的动作，回退时记得连 gradle 依赖一起删；④ 交付给公司的要点：包名 `com.s05.hudtraffic`，链路 `AmapTrafficReceiver`（高德广播）→`TrafficLightBus`→`HudTrafficManager`（DisplayManager 副屏 + `TYPE_APPLICATION_OVERLAY`）→`TrafficLightPanel`，入口 `MainActivity` + 常驻 `HudTrafficService`；UI 全部**纯代码构建无 res 布局**，所以合进宿主不用搬资源；权限见 `AndroidManifest.xml` 12 项（`SYSTEM_ALERT_WINDOW`、前台服务、`RECEIVE_BOOT_COMPLETED`、定位、`WRITE_SECURE_SETTINGS` 等） |
| 2026-09-26（四十四） | **第十一轮：独立版加「2D 导航地图」+ 高德 Key 申请/填装**。① 独立版（`_hud_src\S05HudTrafficLight`，39 个 java）新增 `NaviMapOverlay`（可拖动/可调大小的高德 2D 地图悬浮窗，主界面开关「2D 导航地图（按住顶部蓝条拖动位置）」）、`RoutePlanner`（调高德 Web 驾车 API `restapi.amap.com/v3/direction/driving` 算路 → polyline 画到地图）、`LocationFeed`（定位/模拟定位喂给地图）；**原有红绿灯面板一行没动**；② **高德 Key 两份**（一个 Android Key 只能绑一个包名）：独立版 `115b8004…`（包名 com.s05.hudtraffic）/ 内置版 `4a77cd6b…`（包名 com.deepalhome.launcher），`NaviMapOverlay.amapKey()` 按 `getPackageName()` 自动选 —— ⚠️ 控制台「安全码」框**只填 SHA1**（写成 `SHA1;包名` 会报「安全码信息只能包含数字，字母，下划线，中划线，冒号」），包名填在下面单独的 PackageName 框；**改安全码不会改变 Key 值**（Key 字符串不变，改的只是校验规则）；③ ⚠️ **内置进 D+ 的那份不能带地图**（本次实测结论）：车机包 classpath 只有 android.jar，**没有高德地图 SDK**（jar + .so）⇒ 把 `NaviMapOverlay/RoutePlanner/LocationFeed` 拷进 `CarDash\hud\` 编译直接失败（`RoutePlanner.java: 找不到符号 LatLng`）⇒ **已回退**，内置版仍是纯红绿灯 HUD（39 个 java，`build_local.py` 编译通过、签名 `c8a2e9bc…` 一致）。要让内置版也出地图，必须把 AMap 地图 SDK 打进车机包（jar 编进 classes3.dex + .so 塞进 `lib/`，包体 + 几 MB，需改打包流程并在车机上验 GL 渲染）—— 用户尚未决定是否做；④ **验证**：MuMu（`adb -s 127.0.0.1:16384`）装独立版 debug apk，`input tap` 打开 2D 开关 ⇒ A/B 截图（`_shots\A_on.png` / `B_off.png`）对比定位到地图悬浮窗在 x≈200-500 / y≈200-400，区域内 959 种颜色、含白色道路/标注像素 ⇒ 地图引擎确实渲染了；日志里的 `NoSuchMethodException: android.view.accessibility.AccessibilityManager.isHighTextContrastEnabled` 是**高德 SDK 内部反射警告**（新系统移除了该方法，SDK 自己 catch，非崩溃、非鉴权失败），同一时刻 gralloc 已分配纹理内存；**Web 算路 Key** `14f2efc0…` 在 PC 上 curl 实测 `status=1`（14.3km / 1194s / 6 个红绿灯，polyline 完整）✅ ⇒ 无需配 IP 白名单 |
| 2026-09-23 | 项目从 `d:\Desktop\IPA` 搬到 `C:\Users\Administrator\Desktop\IPA`；本机装 Python 3.12.10 + `cryptography/pyaxmlparser/pyyaml`；`_tools\*.py`(22 个) 与 `CarDash\tools\{build_local,setup_env}.py` 去掉写死盘符；`_tools\setup_git.py` 增加"重配 `core.sshCommand` + 验证认证"；修掉仓库 `.git/config` 里残留的旧机器 ssh 路径（它导致 push 全部 `Permission denied`）；**iOS 版本号补跳到 1.6.8**；提交 `41dda3e`、`5049b9b` 并 push |
| 2026-09-23 晚 | 用户回传车机实测日志（942 行）+ 两张截图 → 定位「IPA 目的地乱跳」根因（车机把 GPS 上报当地目的地）并修掉；新增 `S05Navi.java` 探测车机原生导航 IPC；修「导航每分钟闪一次」；**iOS 1.6.9**：去转向卡、去比例尺、终点旗图标、竖屏总里程挪位、日夜模式自动、算路安全阀；**车机 213 / `v26.0521-cd18`** 本地构建并交付到 `<IPA>\Deepal-CarDash-213.apk`；下载高德 SDK 头文件核对 API（新增第 6 节第 18 条）；新增第 11 节「车机导航数据从哪来」；提交 `c0ecf55` 并 push |
| 2026-09-23 深夜 | CI 曾在 Build 之前失败（`build.log` 都不生成）→ 工作流改成全步骤日志 + `pod install` 重试 + 统一失败诊断（提交 `bec6fd7`）；用户装 1.6.9 实车反馈六条 → **iOS 1.6.10**：导航静音、外部定位治「先闪北京」、引擎预热治「点导航卡一下」、自车图标位置（横屏右/竖屏下）、顶部遮罩压更黑、模拟路线补「预计到达」；补第 6 节第 19~24 条（导航视图可调项）；提交 `0399d10` 并 push。**本轮没动车机端，APK 仍是 cd18** |
| 2026-09-23 深夜（二） | 用户对比四种状态截图：非导航状态有「黑断层」+ 车头位置错乱（**1.6.10 的地图偏移 hack 是我引入的 bug**），且要求「别用两套系统」→ **iOS 1.6.11**：非导航状态改用同一 `AMapNaviDriveView` 的**巡航模式**（`detectedMode`），删掉偏移 hack，锚点横屏 0.64 / 竖屏 0.66；提交 `1b3b857`。**车机端仍未动，APK 还是 cd18** |
| 2026-09-23 深夜（三） | 用户提供官方《智能巡航》文档 URL + 实测截图：**巡航不跟车**（地图停在默认位置＝北京、无车标）⇒ 1.6.11 的思路作废。**iOS 1.6.12**：非导航态回退 `MAMapView`，但底图换 `MAMapTypeNavi/NaviNight`（导航样式）、车标换高德官方 `compass_car_256@3x.webp`、车头定位改成 `convert` 精确反算，锚点提成共用常量 `MapAnchor`；第 6 节第 25~31 条整段重写（巡航的真实语义、`startGPSNavi` 硬约束、MAMapView 的 screenAnchor 是缩放锚点不是内容锚点、导航态默认车头朝上+停车时罗盘兜底）；提交 `af67cd1` 并 push。**车机端没动，APK 仍 cd18** |
| 2026-09-25（四十二） | **iOS 1.6.32：移除「设置 → 测试」三项模拟功能（准备发正式版）+ 首次提交推送 + 换电脑文档**（用户："给github推送 我等着下ipa更新测试" → "更新一下交接文档 我准备把整个文件夹拷走去另外台电脑上工作了"）。**① 删 UI**：`SettingsSheet` 里整个「测试（正式版会移除）」Section —— 模拟一条导航路线 / 清除模拟路线 / 模拟转向灯 / 模拟超速。**② 删状态**：`DashboardModel` 的 `mockDest/mockNav/mockTurn/mockOverspeed/isSimulating` 与 `sendMockDestination()/clearMockDestination()`；`displayTurn`、`isOverspeed`、`displayNav`、`syncDestination()` 的目的地候选、`watchdogTick()` 的失联判定、`endNavigation()` 全部回到**只认车机真实数据**（`endNavigation` 不再分"模拟/真导航"两支）。**③ 删模拟导航**：`NaviKitNavView.simulate` 参数、`NaviCoordinator` 的 `simulate`/`plannedSimulate`、`feed()` 里"模拟中停喂真实定位"的特判、`onCalculateRouteSuccess` 的 `startEmulatorNavi` 分支 ⇒ **只剩 `startGPSNavi`**（用 `startedMode` 挡住重复启动，因为算路成功回调可能来多次）。**④ 版本** `1.6.31 → 1.6.32`（`ios/project.yml` 的 `MARKETING_VERSION`）。**⑤ 提交推送**：`CarDash` 从第七轮起一直未提交 ⇒ 这次**连同车机那批一起补提**（`d85e7db`：`lantern/` 27 个 java、`LanternEntry`、`AmapSignals` 快判、`tools/` 五个脚本等），push 后 **CI run #72 成功** → Release **`CarDash IPA 1.6.32 (build 72)`**（65.4MB，下载见 §4）。⚠️ **副作用**：转向灯绿光（⑩）与超速红光（⑨）**只能上路实测**，屋里没法预览了。**⑥ 文档（换电脑）**：§2 目录树包号更新到 `新-238`/`旧-239`、§4 现状 + 版本表、§7 新增**「拷走之前」4 条清单**与本机最后一次复核留档、§9 提交记录补 `d85e7db`、§10 补 `_lantern_src` 一行与**各目录体积**、§0.0b 改成「已移除」留档（写明怎么捡回来）。**本地自检**：`check_swift.py` 括号配平全 OK、`verify_ios_ver.py` 全部通过；真机编译只能靠 CI（本机 Windows 无 Swift 工具链）⇒ #72 成功即为准 |
| 2026-09-25（四十一） | **第九轮：回退"卡片自定义显示 App" + 修氛围灯入口的图标**（用户："这个功能作用并不大了 卡片不用自定义显示其他软件了 把桌面设置氛围灯设置前面加个图标 和之前的布局相同 现在这样看着有点突兀"）。**① 回退**：删掉 `inject\...\CardAppEntry.java`（卡片上的「＋」按钮）与 `inject\...\AppWindowTest.java`（桌面设置里的「应用小窗」验证件）；`LanternEntry` 回到**只挂「氛围灯设置」一项**；`inject_manifest.EXTRA_PERMISSIONS` **收回到空**（已核对：新包里只剩 `INJECT_EVENTS`，那是**原版底包本来就有**的，我们加的 `CAPTURE_VIDEO_OUTPUT`/`INTERNAL_SYSTEM_WINDOW` 确已移除）。**② 修图标**：`LanternEntry.lanternIcon()` —— (a) **尺寸照抄原厂**：量出模板项（`RadioGroup` 里最后一个原厂 RadioButton）左侧图标的 `intrinsicWidth/Height`，按同样 bounds 贴上去，量不到按 24dp 兜底；(b) **保证一定有图标**：`Res.get(ic_palette)` 若返回 null（vector 解析失败），就用 `GradientDrawable` 画一个紫色渐变圆点顶上 —— 之前"只有文字、和旁边几项一比很突兀"就是这里没兜住。**构建**：53 源文件真编全绿 → `新-238`(cd10)/`旧-239`(cd33)，系统签名一致。文档：§0.0（包号 + 回退说明 + 第 3 条改看图标）、§4。**未提交** |
| 2026-09-25（四十） | **第八轮：卡片嵌 App 改成"在卡片上直接选"**（用户："不能直接让我在那三个卡片那里选择我要显示的app吗 不一定非得是抖音"）。**新增 `inject\...\CardAppEntry.java`**：① 桌面主界面（`com.deepalhome.launcher.MainActivity`）resume 后延迟 1.5s 遍历 decorView 认卡片 —— 判据"包名 `com.deepalhome.launcher.` + 类名以 `AppWindow` 结尾 + 宽高>220px"（卡片就是 `MapAppWindow`/`MusicAppWindow`/`CarInfoAppWindow` 实例本身，`extends BaseAppWindow`）；② 每张卡片（限 ViewGroup）右上角加一个 34dp 蓝色圆形「＋」按钮（用 `setX/setY` 定位，不依赖卡片内部是 ConstraintLayout 还是别的），它是卡片的子 View ⇒ 跟着卡片走；③ 点它弹**全部可启动 App** 列表（按名排序，不止抖音；已嵌入时列表第一项多一个「✕ 关闭，还原原卡片」）；④ 选中后在**该卡片原位**加一个 holder（SurfaceView + 状态小字）→ `createVirtualDisplay` → `setLaunchDisplayId` → 启动目标 App → 触摸按比例 `injectInputEvent` 回注（与 `AppWindowTest` 同源）。**为什么不做"塞进三个点菜单"**：那是原厂 Kotlin 的原生 PopupWindow，拿不到菜单对象 ⇒ 加按钮是最稳的等价方案（已向用户说明）。**构建**：55 源文件真编全绿 → `新-236`(cd9)/`旧-237`(cd32)，系统签名一致。文档：§0.0 清单（包号 + 第 3 条改成卡片原位选）、§16.7（卡片入口做法 + 第一版已知不足）。**未提交** |
| 2026-09-25（三十九） | **第七轮：实车反馈后的三处修改**（用户发来 16:47 一键诊断 + 3 张照片，反馈"颜色错/退出导航还等十多秒/插件市场没有自定义卡片功能"）。**① 氛围灯挂载链路验证成功**：日志里 `保活已挂上（跟随桌面进程）` / `入口已插入桌面设置（11 项）` / `面板已打开（容器 2037x1075px）` 三行全中 —— §15 那套做法在真机上完全跑通。**② 线序/颜色 bug（用户报"调成红色显示绿色、改设置也没用"）**：`LedCommand.java` 与原始工程**逐字节相同**（不是移植引入）—— 根因是**下发时机**：线序/灯带点数是**存在灯固件里**的配置、灯掉电/重连会复位，而 App 只在"用户手动改设置那一刻"发过一次、**连接时从不补发** ⇒ 重连后颜色必错，而设置页显示的还是改过的值（存的是 App 侧）⇒ "改了也没用"的错觉；内置版还额外踩到"`Prefs` 与独立 APK 不共享 ⇒ 一上来就是默认线序"。**修**：新增 `LedOutput.reapplyDeviceConfig()`（连上后按 线序→点数→开关→亮度→颜色 全部补发，发色走 Raw 不打断渐变），`LanternPanel` + `LanternBootstrap` 两个连接回调都接上。**③ 退出导航 15 秒 → ~4~5 秒**：诊断里【高德自己的"退出/结束导航"日志】**仍只有我们自己的那行**（高德没打原话）⇒ 改走组合判据：新增 `QUICK_END_MS=4000`，条件是"活路线断 ≥4 秒 **且** 最近一次 `EXTRA_STATE` 是空闲值（非 8/9/24/25）"（导航中引导 2 条/秒，连停 4 秒本身异常；15 秒那份保留兜底），`onState` 里新增记录 `lastStateValue/lastStateAt`，`maybeEndIfStale` 加快判分支 + `stateLooksIdle()`。**④ 卡片嵌任意 App：验证件落地**（用户实测插件市场没有该功能 ⇒ 自己做）。新增 `inject\...\AppWindowTest.java`（桌面设置第二项「应用小窗」：`createVirtualDisplay` + `ActivityOptions.setLaunchDisplayId` + `InputManager.injectInputEvent` 触摸回注，状态文字全程输出供拍照判读）；`LanternEntry` 改成挂两项（`KIND_LANTERN`/`KIND_APPWIN`，tag 用值槽 `TAG_PANEL` + key 槽 `KEY_KIND` —— 踩坑：`View#setTag(int,Object)` 的 key 必须是资源 id 型 int，传 String 编译不过）；`inject_manifest.EXTRA_PERMISSIONS` 放开三条**签名**权限（`CAPTURE_VIDEO_OUTPUT`/`INTERNAL_SYSTEM_WINDOW`/`INJECT_EVENTS`，与文件里"别乱加权限"的警告不冲突，理由写在注释里）。**构建**：54 个源文件真编（javac 全绿）→ `新-234`(cd8) / `旧-235`(cd31)，系统签名一致，权限已核对进清单。文档：§4 现状改成第七轮 + 待回报三样、§0.0 清单、§15.6（线序坑）、§16.6（验证件 + 判读表）。**未提交** |
| 2026-09-25（三十八） | **氛围灯素材归档 + 卡片嵌 App 可行性调研落档**（用户："把氛围灯用到的相关代码放进 IPA 文件夹方便我打包带到另外台电脑…并更新交接文档，我要开启新的对话了"）。**① 归档**：新建 **`<IPA>\_lantern_src\`**（87 文件 / 438KB）= `MagicLantern\{src,res,AndroidManifest.xml,build.ps1}` + `ios\`（iOS 版源码，**删掉了内嵌的 `.git`** 避免嵌套仓库；内容为纯源码不影响重建）；`tools\make_lantern_res.py` 的默认源改成**优先 `_lantern_src`、回退 `D:\Desktop\huancai`** ⇒ 换电脑只带 IPA 文件夹就能重跑；**等价性已实测**：用归档素材重新生成的 `Res.java` 与现有那份 **SHA256 逐字节一致**（`2E7C26E3…`）。**② 卡片嵌 App 调研（只读、未改一行代码）**：用户问"卡片三个点里能不能选任意 App（抖音）嵌进去"，反编译底包得到确定结论 —— 桌面**自带两套现成机制**：`customapp.CustomAppWindow`（选应用 `showAppMenuPopup/selectApp/isSelectableApp` + 虚拟显示 `pip.VirtualDisplayHost` + 嵌入式输入 `WindowEmbeddedInputAppWindow` + 现成选择器 `PipAppPickerDialog`）与 `VideoFloat`/`CheyuVideo`（视频浮窗）；但被**三道闸**控制：插件市场的**权益**（`VideoFloatPackManager.isEntitlementGranted` / `CustomAppAuthorizationEnforcer`）、**行车安全**（`isCustomAppAllowedByDrivingSafetyLimit`+按档位）、**前提检查**（`hasVirtualDisplayViewPrerequisites`）；且核心判断是 **native 方法**而该 apk 里**没有 .so**（实现在系统分区）⇒ 可调用、难改白名单。全部证据与"自己做"的评估写进新增 **§16**（含 `_refs\jadx_card\` 已反编译类清单、`_tools\scan_names.py` 等素材）。**③ 文档**：顶部更新说明、§2 目录树补 `_lantern_src\`/`_refs` 子项、§4 现状补"⏳ 等用户回报三样 + 新对话接手路径"、§15 补归档位置与**从零重建内置版的标准流程**（含"4 个手写文件要先备份"的坑）、§12 本行。**本轮未改代码、未提交** |
| 2026-09-25（三十七） | **第六轮：把用户的氛围灯 App（MagicLantern）内置进「桌面设置」**（用户需求："把之前我写的另外一个氛围灯控制的软件内置到 D+ 桌面的桌面设置里…这样也能确保保活，我也不用单独装 apk 了；红框那里加一个氛围灯设置，蓝色框内显示我之前那个 app 的界面，功能也不能移除"）。**做法（在"不能加资源 + 不能改 smali"两个硬约束下的正解）**：① 源码拷进仓库（`CarDash\lantern\src\com\magiclantern\rgb\`，27 个 java），**原工程 `D:\Desktop\huancai` 一个字节没动**；② **资源去表化** —— 新脚本 `tools\make_lantern_res.py` 把 31 个 drawable 的 `@color` 内联、selector 的 `@drawable` 展开，生成 `Res.java`（运行时 `Drawable.createFromXml` + `ConstantState` 缓存），`tools\port_lantern.py` 再批量改写 62 处 `R.drawable` / 16 处 `setBackgroundResource` / 3 处 `setImageResource` / 1 处 `compoundDrawables`；③ **面板化** —— 新增 `LanternPanel extends FrameLayout`（原 `activity_main.xml` 整段改成代码构建），`Page` 基类改"双字段"：`host`（导航/状态）+ `activity`（Context，**沿用旧名** ⇒ 7 个子页里原有调用几乎没动），`refactor_lantern.py` 统一换掉构造参数（7 处）与宿主方法调用（18 处）；`MainActivity` 缩成壳、独立 apk 仍可构建；④ **入口** —— 新增 `inject\...\LanternEntry`：`BootProvider` 里装一次 + `registerActivityLifecycleCallbacks` 盯 `SettingActivity`，`onResume` 时**往原厂 `RadioGroup` 末尾插一个复刻外观的真 `RadioButton`**（自留 id `0x0C4D0001`；id 先按名字查、查不到用本基座数值 `0x7f0b0551` 兜底），点它 → 面板塞进原厂 `settingsViewContainer`（id 兜底 `0x7f0b05f2`）；点回原厂项时**原厂自己 `removeAllViews`** 把面板摘掉，面板在 `onDetachedFromWindow` 解绑（**蓝牙连接与渐变播放不动**）；⑤ **尺寸**：新增 `Ui.setScaleForContainer()` —— 原来按整屏算缩放，嵌进右侧容器会溢出；⑥ **保活**：`LanternBootstrap` 补**动态注册的定时广播接收者**（内置版清单里没有 `TimingReceiver` ⇒ PendingIntent 改成 action + `setPackage` 投递）、`BleController` 单监听改**多监听**（`addListener/removeListener`，界面与保活互不覆盖）。**安全兜底**：挂载全程包在 try 里、失败只写一行日志、绝不影响原车功能；已反编译确认原厂 `OnCheckedChangeListener` 是 if-else 链、**末尾没有 else 兜底**（收到未知 id 什么都不做）⇒ 插一项不会打断它的分支。**构建实测**：53 个源文件真编（javac 全绿）→ `新-Deepal-CarDash-232.apk`（`v26.0925-cd7`，新增 `classes3.dex` 332KB，`ROUNDTRIP_OK` + 系统签名一致 `c8a2e9bc…`）与 `旧-Deepal-CarDash-233.apk`（`v26.0521-cd30`），并已从产物 dex 里核对到 `LanternPanel`/`Res`/`LanternEntry`/`LanternBootstrap`。**顺带**：用户 15:49 的诊断确认**转向灯（打灯语义/延迟/换边）和退出导航（自动退，等 ~15 秒）都已正常**，结论写进 §4。文档：§4 现状/§0.0 清单/版本表/版本实况、新增 **§15**（实现原理 + 上车怎么验 + 排查日志表 + 下一轮可选项）。**本轮未提交** |
| 2026-09-25（三十六） | **定下"每轮两个包"的交付规矩 + 补出老固件的包**（用户原话："旧版底包还是要改 还是同时改两个，一个新版底包做好的 APK 和一个旧版底包做好的 APK，还是前面加新和旧来区分，都输出到根目录"；"要留档就挪 `_obsolete\`"）。**做了**：① 建 **`_bases\`** 把两个**在用的**底包从根目录/`_obsolete\` 收进去（`Deepal+ S05 v260925.apk` 新、`旧底包-v26.0521.apk` 老）—— 构建输入不再摊在根目录；② **用老底包补编了这一轮的 `旧-` 包** ⇒ `旧-Deepal-CarDash-231.apk`（`v26.0521-cd29`，`classes2.dex`，系统签名一致，产物里已核对到第四轮全部改动）；③ 上一轮的 `旧-228` 留档在 `_obsolete\`。**规矩（写进 §3 末尾）**：每轮**两条线都要编**、都用 `新-`/`旧-` 前缀、**都输出到根目录**；版本号是一条共享序列（谁后编谁号大）；上一轮的包装进 `_obsolete\`；在用的底包在 `_bases\`；参考包在 `_refs\`。文档同步：§2 目录树、§3 构建命令（两条线各一条）与交付表、§4 版本表（拆成"新固件/老固件"两行）/版本实况（底包三版的现状）、§6、§7、§0.0 清单。**本轮不改代码、未提交**；根目录现在只有 `新-230` + `旧-231` 两个能装的包 |
| 2026-09-25（三十五） | **清根目录 + 定下"以后每轮都清"的规矩**（用户要求："把根目录不用的包移除掉，每次出新包把旧包删除，不用的底包也删除"）。**做了**：① 删 `新版D.apk`（和 `Deepal+ S05 v260925.apk` **哈希完全相同**，纯重复）、删 `Deepal+ S05 v260924.apk`（作废的上一版新底包）；② 老固件那套（底包 `旧底包-v26.0521.apk` + 产物 `旧-228`）**挪进 `_obsolete\`** —— ⚠️ 没直接删：底包是**用户给的原始 APK、删了不可再生**（文档里也专门写过"别删"），留着并**已问用户要不要一起删**；③ `Deepal-CarDash-219.apk`、`独立包A/B` 挪 `_obsolete\`；④ 新建 **`_refs\`**，把 `D.apk`、`原车导航.apk`、`后装导航.apk`（420MB 参考包）挪进去；⑤ 改掉 3 个脚本里对 `D.apk` 的硬编码路径（`check_resmap.py` / `inspect_axml.py` / `keycheck.py` → `_refs\D.apk`）和 `inspect_apk.py` 的默认底包路径（→ 当前底包），**三个脚本实测还能跑**。⑥ **发现并清掉 420MB 重复**：`_nav_apk\` 里原本就有那两个导航包（和 `_refs\` 里的**哈希完全相同**，且没有任何脚本/文档引用 `_nav_apk\`）⇒ 删掉 `_nav_apk\`；⑦ 空壳 `CarDash-unsigned.ipa`（141KB，占位假产物）挪 `_obsolete\`。结果：**根目录只剩 2 个 APK —— 当前底包 + 当前要装的包**（外加 `CarDash\ _apk\ _env\ _obsolete\ _refs\ _tools\` 六个目录和交接文档）。规矩写进 §3 末尾（出包即删旧包、没用到的底包删掉、原始底包先问、参考包进 `_refs\`、历史产物进 `_obsolete\`），§2 目录树/§6/§7/§14.4 的被移动路径全部同步。**本轮不改代码、未提交** |
| 2026-09-25（三十四） | **第五轮：换新底包（`新版D.apk` → `Deepal+ S05 v260925.apk`）**。用户下午给来 `新版D.apk`（`com.deepalhome.launcher` **v26.0925.beta**，versionCode 220，16.58MB，签名 `c8a2e9bc…` = 车机系统签名），并说明**之前那个新底包（v260924）不再用**。**先做移植核对、再构建**（这就是 §6 第 83 条那套固定动作）：① 包名/签名/`sharedUserId`/HOME/BOOT_COMPLETED 全对；② 解出 dex 逐个查反射目标 —— `util.s05.S05VehicleDataMonitor`（含 `INSTANCE`/`cacheCarS05Info`/`virtualCarPropertyManager`/`psGetValue`/四个 `*Listeners`/`ALIAS_LOW_SPEED_SOUNDS`）、**`carinfo.CacheCarS05Info`（挪了包，我们的 `CACHE` 常量已经是对的）**、`util.s05.navi.NaviIpcBridge`（含 `access$queryS05CurrentDestination`）、`hud.s05eta.HudNaviManager`/`HudEtaOverlayController`、`util.AssistantUtil`、`openos.virtualcar.VirtualCarPropertyManager` **全部在**（`CarS05InfoUtil` 如预期已移除，`UTIL_CANDIDATES` 兜底）；③ 新底包 provider 是 `fileprovider/musiccustom.bridge/targetruntime/systembarstate/luckProvider/utilcode.fileprovider`，和我们注入的 `…cardashboot` **不冲突**；它的无障碍服务 `AutoNaviAccessibilityService` 带 `meta-data android.accessibilityservice` ⇒ 配置资源可复用。**结论：注入代码零改动，只重编** ⇒ `新-Deepal-CarDash-230.apk`（`v26.0925-cd6`），`classes3.dex` 注入（base 是 2 个 dex）、系统签名一致、5 个组件齐全、产物里已核对到第三/四轮全部改动（换边互斥、活路线判据、退出日志取证等）。`旧-228`（v26.0521/cd28，功能同步）保留给老固件机器。文档：§2 目录树、§3 构建命令与底包说明、§4 现状/清单/版本表/版本实况（底包换过三次的区分）、§6 新增第 **83** 条（换底包必做的反射目标核对清单）。**本轮未改动代码、未提交** |
| 2026-09-25（三十三） | **第四轮：修第三轮留下的两个尾巴**（用户实测报告 + 1 份一键诊断 + 1 张截图）。**① 转向灯「关了还亮一两秒 / 右转切左转两边同时亮几秒」** —— 根因就是第三轮那个 **2.5 秒保持**：关灯后要等 2.5 秒才收；换边时另一侧还在窗口里。原始值历史（12:10:34-36 只有 `right` 在跳、12:10:41-44 两侧一起跳）正好对得上。⇒ 保持 **700ms**（只够跨过实测约 0.3 秒的"灭"相）+ 新增 **`turnSwitch()` 换边互斥**（一侧亮起时，另一侧"最近一次亮"超过 **300ms** 就立刻判灭；300ms 取在双闪交替间隔与真正换边间隔之间，不会误杀双闪）。**② 车机退出导航 IPA 还是不退出** —— 第三轮判据方向对了但窗口太长、而且检查只挂在"驾驶模式变化那一刻"。本轮拿到两条硬实测（导航中 10001 **2 条/秒**、最长断流 **12 秒**；而车机**退出导航后 31 秒里一条 10001 都没有**）⇒ 窗口 **30 秒 → 15 秒**，检查改成**每一条广播都跑**（拿每秒 2~3 条的心跳广播当定时器）⇒ 延迟≈15 秒（原来最长 40 秒）；`endSession` 现在把"活路线已断 N 秒"写进 bridge.log（下次能分清是判早/判晚/压根没判）。**③ 取证增强**：`/logcat` 的【高德广播类型统计】上限 12→**24 种**（结束广播可能藏在少见类型里）；**新增**【高德自己的"退出/结束导航"日志】（`LogcatSignals` 每行喂给 `AmapSignals.noteLogLine`，关键字 = 高德/mahjong/amap + 导航 + 退出/结束/停止/取消）—— 一旦拿到原文就能把 15 秒压到秒级。两个包本地真编 + 系统签名一致（`c8a2e9bc…`）+ 新代码已核对进产物，`java_lint` 全绿。提交 **`b78ca91`** 并 push（只动 `inject/` ⇒ 不触发 iOS CI）。§4 现状/§0.0 清单/版本表/版本实况、§6 新增第 **81、82** 条 |
| 2026-09-25（三十二） | **第二轮实车收工 + 修两个 bug**（用户发来 1 份一键诊断 + 独立包 A 的输出 + 报两个 bug）。**① 独立包 A 实测：关卡② ✅ 过了** —— `myUid()=1000`、`sharedUserId=android.uid.system`、`READ_LOGS=有`、`logcat -d` 真能读（而 `FLAG_SYSTEM=false` ⇒ **它不是系统应用，只是加入了 system uid**）⇒ **README 那句"Android 14 禁止非预装应用加入 android.uid.system"在本车不成立**（那是模拟器 + 调试签名的结论）。关卡③：`getService("polymeric_service")` **直接有 binder**、`com.wt.tinnovecoreservice` 在（uid=1000）、bind `wecarnavi/…SkillProviderService` 成功并拿到 `com.tinnove.skill.api.ISkillProviderFactory` / `com.tinnove.navi.skill.IPCOwner` 描述符 ⇒ 通路探明。用户另问保活：**D.apk 靠"系统桌面(HOME) + 3 个 BOOT_COMPLETED 接收器 + MY_PACKAGE_REPLACED + 前台服务(0x2/0x8) + 系统 uid"，没有 `persistent`**；A 包只是探测器（无 Service/BootReceiver）⇒ **不会常驻**，将来真做独立包照抄那份清单。**② 修两个 bug（车机 cd4/227 + cd27/226，iOS 未改）**：**转向灯"打了没反应"** —— 真值语义第二次猜错：原始值历史里打灯时 `left/right` 在 **1↔2 反复跳**、没打灯时都是 **1** ⇒ **1=灭 / ≥2=亮**（上一版按 0=亮 ⇒ 值 2 被判成不亮）；又因为**属性跟着灯泡闪**，下发前加 **2.5 秒保持**（`StateHub.TURN_HOLD_MS`），`turnRaw` 改成同时显示左右两个原始值。**"车机退出导航 IPA 不退出"** —— 两个根因：`EXTRA_STATE=8` 停车不导航时**每分钟也发**（09-24 19:47~21:14 人还在办公室）、且周期广播载荷**夹带 POI**（19:49「巷子口郑鸡肉柴火鸡」、21:02「鑫画家全屋定制」）把目的地重新点亮 ⇒ 判据换成 **引导广播里 remain/time > 0 = "活路线"**（`lastLiveRouteAt`；断 30 秒算结束、模式变空闲/巡航后 20 秒快收），目的地改成**候选 + 活路线背书才转正**（8 秒 TTL），`tick()` 加"本次开机从没导航过就不刷日志"的闸门。`/logcat` 新增【高德广播类型统计】+【目的地闸门/候选/最后命中（含 KEY_TYPE、字段名、含不含起点）】。两个包本地真编 + 系统签名一致（`c8a2e9bc…`）+ 新代码已核对进产物（`classes3.dex` / `classes2.dex`），`java_lint` 全绿。提交 **`3403c13`** 并 push（只动 `inject/` ⇒ 不触发 iOS CI）。§4 现状/§0.0 清单/版本表/版本实况、§6 新增第 **77~80** 条、§14.4b/§14.4c/§14.6 全部更新 |
| 2026-09-25（三十一） | 用户「**交接文档先做一下，我现在去车上测试**」（第二轮上车，去之前先落文档）。**出货前对账（实测，不是"应该"）**：用 `aapt2 dump xmltree` 逐包验 —— `独立包A-系统uid.apk` **确实带** `android:sharedUserId="android.uid.system"`（原文 `0x0101000b`）、`独立包B-普通.apk` **没有**（对照包）、`新-225`/`旧-224` **也有**（继承桌面）；四者证书全是 `c8a2e9bc…92ab8`。**顺手记下一条判据（防后人误读）**：注入包一直有 system uid **不能**当作关卡②已过的证据 —— 桌面是**预装**系统应用、我们做的是同签名**覆盖升级**，与"一个**新装**的 App 能否加入 system uid"是两件事；能作证的只有独立包 A 报告里的 `Process.myUid()` 是不是 `1000`（已写进 §14.4）。**文档同步**：§4 顶部改成"这一趟装三样（车机 `新-225`/`旧-224` + iOS `1.6.31` + 独立包 A/B）"；§0.0 清单把**独立包测试提到第一条**（装 A → 把那段文本发回来 → 装不上就装 B，附判读要点）；修掉清单里两处过时（`1.6.30`→`1.6.31`）与编号错位（④⑤→⑩⑪）；§4 版本表补「独立包测试件」一行、iPhone 行更新到 1.6.31；§4「版本实况」那条标注清楚是 9-23 的历史快照、共享序列补到 `224`/`225` 并声明"独立包 A/B 不是车机包、别拿去覆盖装"；§0.0c 的"明天的动作"改成这一趟的动作（**插件市场能不能订阅至今未回报**，别当已验过）；§14.4 补对账表 + 那条判据。**本轮无代码改动、未提交** |
| 2026-09-25（三十） | 用户两件事：① **App 名字改成「深蓝仪表盘」**（`CFBundleDisplayName`，原来叫"车载仪表"）——提交 `eac44ad`，iOS **1.6.31** 已出包（`build-71`）。② **问独立包可不可行**：「完全独立出来、不靠 D.apk，不反射桌面内部类，改成自己绑厂商 SDK（polymeric/virtualcar），先做个测试出来我去车上测」。**做了可行性调研（只读，没动任何现有代码）**：①签名这一关**确定可行**（车机自身签名=AOSP platform，钥匙在 `CarDash\keys`）；②`sharedUserId=android.uid.system` 在 Android 14 能不能加入**必须真机实测**（README 那句结论来自模拟器+调试签名，不能推论）；③车身数据那条路**已探明**——反汇编 D.apk 得到：`com.openos.virtualcar.VirtualCar.createVirtualCar(listener,handler)`（**无 Context 参数**）→ `getVirtualCarManager()` 内部要子服务 **`virtualcar_property_service`** → `VirtualCarPropertyManager.getValue(int id, int area)` / `register(int[], callback)`；AIDL 描述符、`com.tinnove.polymericservice.IPolymericService`（聚合服务/别名取值）、以及 `com.wt.tinnovecoreservice` 疑似宿主包全部记录在 **§14**。为此装了 **smali/baksmali 2.5.2**（新脚本 `_tools/get_smali.py` → `_env\tools\smali-lib\`，Maven Central 通的）——**下一步就是把厂商 SDK 的类直接搬进我们的独立包，从而不必自己逆 AIDL**。**产出两个测试包**（新目录 `CarDash/solo/` + 新脚本 `CarDash/tools/build_solo.py`，与 `build_local.py` 完全独立、不影响注入链路）：`独立包A-系统uid.apk`（平台签名 + sharedUserId）、`独立包B-普通.apk`（对照）。测试包会报告身份（uid 是不是 1000）/厂商服务清单（名字·exported·权限）/逐个 bindService 的结果与接口描述符/logcat 能不能读 ⇒ 拿回来就能判定关卡②③。踩坑：XML 注释里不能出现连续两个减号；正则按 `count=1` 替换会先命中**注释里**那句同名文字（两个包做出来一模一样），已改成只在 `<manifest>` 标签内替换 |
| 2026-09-25（二十九） | 🚨 **CI 被计费挡住**：用户贴来 GitHub 原话「The job was not started because recent account payments have failed or your spending limit needs to be increased」—— 说明 1.6.30 那一轮 **job 根本没启动**（所以 tag 不涨，别误判成"构建失败"）。根因：**私有仓库的 macOS runner 按 10 倍扣分钟**，免费额度 2000 分钟 ≈ 200 分钟 macOS，而我们已经跑了 68 轮 ⇒ 额度/账单到顶。**用户把仓库转成公开**（公开仓库 Actions 免费）后，我推了一个空提交 `8fa9c61` 重新触发。**公开前先做了安全核查并写进文档**：仓库只跟踪 50 个文件、**没有任何密钥入库**（`CarDash\keys\platform.pk8` 与 `_env\ssh` 都不在其中），所以公开是安全的；但要提醒用户：`ios\CarDash\UI\AMapSupport.swift` 里的两个高德 Key 现在成了公开信息 —— `iOSKey` 必须绑定 Bundle ID，`webKey`（自绘路线删掉后已无用）建议在高德控制台重置或删除。另外**公开后可以直接读 Actions API 了**（`api.github.com/repos/.../actions/runs`，不用 PAT），以后不必再靠 tag 反推 CI 状态 —— §4「版本实况」里补了命令和说明。**补充（当天下午）**：我第一次推的**空提交** `8fa9c61` **没触发 CI**（workflow 有 `paths: ios/**` 过滤，空提交没有文件变更 ⇒ 一次 run 都没有，这点我自己文档里写着却忘了）；改用**真实改动** `700cf33`（顺手删掉零调用点的 `webKey` —— 仓库公开后不该留 Key）触发 ⇒ **run #70 成功**，出 `CarDash IPA 1.6.30 (build 70)`。顺带确认：**run #69（`924ec97`）是 6 秒就 failure**，正是计费那条（job 没启动但 run 记录照样建、结论记失败）。本机 `api.github.com` 超时 ⇒ 改用**工具侧 web_fetch 抓 API** 拿到了这些数据（不走本机网络），已写进 §4 |
| 2026-09-25（二十八） | **实车第一轮**：用户发来两份完整诊断（`日志一.txt` 10:35 / `日志二.txt` 10:26）+ 一张截图，报两个 bug（另附"手动/语音导航都成功、切目的地也成功"）。**日志把两个根因都钉死了**：① **转向灯绿光从连上车机起一直闪** —— 属性全量扫描里，没打灯时 `vc_alias_turn_left/right_signal_on` **都是 1**，而我们按"1=亮"解释 ⇒ `turnValue()=3`（双闪）⇒ 两边一直闪；真值语义是 **0=亮、1=灭**（和别名名字相反）。② **车机退出导航后 iPhone 顶栏摘要还挂着**（用户截图红箭头指的就是它）—— 清导航字段只挂在"驾驶模式变化那一刻"，而那一刻引导广播还新鲜（实测 30 秒）⇒ 60 秒判定失败后**再也不重判** ⇒ `navActive/navEta/navRemain` 一直挂着；同时还有"车机 60 秒 / iPhone 20 秒 / 旧的 15 秒"三套阈值错位的问题。**修法**：车机 `VendorSignals.TURN_ACTIVE_VALUE = 0`（只有一处）+ 转向灯**原始值变化历史**（带时间，最近 24 条）进 `/logcat`；`AmapSignals.tick()` 改成**每 10 秒周期性判**"导航结束"（`navigating()` = 引导广播 60 秒内有 && 非巡航），判到就 `endSession()` 把**导航字段 + 目的地一起清**；`StateHub.navFresh()` 不再自己按窗口猜（只认 `navActive`，留 10 分钟兜底）；iPhone 侧手点「退出导航」后**顶栏摘要也一起收**（`displayNav` 认 `suppressedDestKey`）+ 收路线去抖 20→10 秒；另加「引导广播最长断流」统计用于把 60 秒阈值继续调低。车机两个底包都重建（`新-225`/cd3、`旧-224`/cd26，签名一致）、三项自检全绿、提交 `924ec97` 并 push（iOS 1.6.30）。§6 新增第 74~76 条；§4 更新现状/版本表/§0.0 加 ⑩⑪ 两条复测项。⚠️ 踩了个操作坑：`git commit -m` 里带中文引号会被 PowerShell 拆坏（会被当成 pathspec）→ 改用 `git commit -F 文件` |
| 2026-09-25（二十七） | 用户开工前让"先读一遍文档、准备好再说"。复查发现两件事：① **`core.sshCommand` 第三次被写成另一台机器的路径**（昨晚在 `C:\Users\Administrator\Desktop\IPA` 干的活，今早整个文件夹拷回 `d:\Desktop\IPA`）—— 而且这次的症状是**命令静默无输出**（`git ls-remote --tags` 打印空白），不是报 `denied`，最容易误判成"没有 tag / CI 挂了"。已重跑 `_tools\setup_git.py` 修好（远端 main = `f2d4a63` = 本地 HEAD，昨晚的提交都在）。② 文档里 `1.6.26 / ⏳ CI 打包中` 已过时：实测远端 tag **`build-64`(1.6.25) / `build-65`(1.6.26) / `build-66`(1.6.27) / `build-67`(1.6.28) / `build-68`(1.6.29 = `f2d4a63`)** 全部出包 ⇒ **今天要装的 1.6.29 已经可以下载**。另外核对了两个车机包：`旧-222` = `v26.0521-cd25`、`新-223` = `v26.0923-cd2`，签名都是 `c8a2e9bc…`，源码树四项自检全绿。§4 一句话现状与版本表改成实测结果；§9 补"这个坑会因文件夹来回拷而反复复发" |
| 2026-09-25（二十六） | 用户担心"改包重签后 `appSignatureSha256` 不匹配 → 插件市场不下发 pack"。**用 apksigner 逐项核实**：原版底包与我们重签的包，证书/公钥的 SHA-256、SHA-1、MD5、DN、RSA2048 **全部一致**，验证方式也同为"仅 v3 生效" ⇒ **基于证书的 `appSignatureSha256` 不受影响**（我们用同一个 platform.pk8）。唯一变的是整包哈希。文档新增 **§0.0c**（明天的动作：装 旧-222 时顺手确认插件市场能否订阅；并写好**独立 APK 预案**：独立包+系统签名+`android:process` 共用桌面进程，桌面本体不动）。§6 第 73 条。**本轮无代码改动，未提交**。用户随后把文件夹拷进 U 盘带走（换机器时记得跑 `python _tools\setup_git.py`，见 §7） |
| 2026-09-24（二十五） | 用户问「上车测什么 / 文档写好 / 诊断能不能一键复制 / 两个包都能装车吗 / 还有没有 bug」。**抓出一个我自己埋的严重 bug**：移植新底包时把车身信号取值类写死成 `S05VehicleDataMonitor`，而用 dex 逐个核对确认 **旧底包 v26.0521 只有 `CarS05InfoUtil`、没有 `S05VehicleDataMonitor`**（两个底包类名不同）⇒ `旧-222` 装到车上会 Class.forName 失败、**车速/档位/续航/电量/转向灯全空**。修法：`UTIL_CANDIDATES` 两个类名依次尝试（`connectOne`），一份代码兼容两个底包，诊断里记下实际连上的类名。**两个包都重建**（`旧-222` / `新-223`，签名都一致）。iOS 1.6.29 新增**「一键复制全部诊断」**：串行抓 /state→/logcat→/diag→/log→/scan 五段 + 版本/链路元信息头，抓完自动进剪贴板（车机 HTTP 是单线程，必须串行）。文档 §0.0 重写成**上车测试清单 9 条** + 0.0b 没车也能验的项，§0.1 改成一键诊断。提交 `f2d4a63` push（⚠️ PowerShell 会把 git 写往 stderr 的进度当成报错、exit 码也失真，判断成功要看有没有 `A..B main -> main`）。§6 第 72 条 |
| 2026-09-24（二十四） | 用户看最新版实测后反馈三点（带 4 张截图；**截图能读，别再说看不到**）：① 退出模拟导航后**路口放大图**残留在"速度"那一格 —— SDK 的 hide 回调在导航视图被拆掉后不再来，已在 `DashboardView` 的 `onChange(of: routeDest == nil)` 里 `crossImage = nil`；② 确认超速红光**已实现**（`DashboardView.swift:129` + `DashboardModel.isOverspeed`），但需要电子眼+车速数据才触发，没车看不到 → 新增**模拟超速**开关；③ "模拟双闪"升级成**模拟转向灯**（设置里 Picker：关/左转/右转/双闪）+模拟超速 Toggle。版本跳 **1.6.28**（提交 `7cb3ef3`，⏳ CI）。另确认用户当时装的是 **1.6.25**（设置里没有模拟双闪、但有退出导航按钮）⇒ 两个症状都是版本落后 |
| 2026-09-24（二十三） | 用户三点：①「点退出导航车标先回定位点、卡一下才出绿标」—— 根因是退出导航时 `routeDest→nil` 切到另一支、`MAMapView` 重建有初始化空档。改成非导航地图**始终挂载**在底层，进/退导航只切换盖在上面的导航视图 ⇒ 无断层。② 加「模拟双闪」开关（设置→测试，强制 turn=3，没车也能预览两边绿光）。③ 让 APK 能装 MuMu：`build_local.py` 新增 `--emu`（去 `sharedUserId=android.uid.system` + 换调试签名，不校验车机系统签名）。**已在用户运行中的 MuMu（Android 15 x86_64，带 arm64 转译）实测**：装得上，但首版一启动就崩 —— 桌面自己的 `LogcatMonitorService` 在 targetSdk 34 + Android 15 上 `startForeground` 没类型，抛 `MissingForegroundServiceTypeException` 把整个进程带崩。修法（提交 `dca0285`）：`--emu` 包里补 `FOREGROUND_SERVICE_DATA_SYNC` 权限 + 给**所有** `<service>` 加 `foregroundServiceType=dataSync`（车机包不受影响，逻辑 gate 在 `--no-shared-uid` 后面）。修后实测：进程存活、`MainActivity` 前台 resumed、桥接照常跑、没车环境优雅降级。iOS 版本跳 **1.6.26**（`fe9fe1e`）。产物：`新-Deepal-CarDash-223-emu.apk`（模拟器专用，别装车上） |
| 2026-09-24（二十二） | 用户三次导航实测 + 截图反馈四件事。**修了四个**：① `Json.num` 把目的地坐标舍到 2 位小数（≈1.1km 误差）⇒ IPA 终点「接近但不准」+ 切目的地 routeKey 相同不重算 —— 新增 `Json.num6`（6 位）给坐标用，iOS 的 `routeKey` 加名字兜底；② 转向灯别名照反编译表猜错了，实车日志证明真正在推的是 `vc_alias_turn_left/right_signal_on`（1/0）—— 已改，双闪绿光这次应该能亮；③ iOS 新增「退出导航」按钮（竖屏总里程上面 / 横屏原海拔位置、横屏移除海拔），逻辑：停 iPhone 侧导航 + 记住被按掉的目的地 key 不让 poll 算回来，换目的地/导航结束/失联自动作废；④ 导航视图拆除时 `dismantleUIView` 停引擎 + `removeDataRepresentative`（不后台空转、不泄漏）。**旧底包丢了** → 新写 `_tools\strip_inject.py` 从 cd24(219) 剥离注入还原出 `旧底包-v26.0521.apk` 同款干净底包，出 **cd25（`旧-Deepal-CarDash-222.apk`）** 和 **新底包 cd2（`新-Deepal-CarDash-223.apk`）**（两包修复相同，versionCode 222/223，注意从新退回旧要先卸载）。手机推送路线那条待复测（多半也是坐标精度引起的）；若还不行，推完路线立刻抓 logcat 看 extras。§6 第 71 条；待办 7 转向灯标完成。提交 `88aaf16` push，⏳ CI 出 1.6.25 |
| 2026-09-24（二十一） | **移植到新底包 `Deepal+ S05 v260924.apk`**（`com.deepalhome.launcher` v26.0923.beta，versionCode 220，签名仍是 `c8a2e9bc…`）。旧底包 `Deepal+_v26.0521.apk` 已被换掉。车机数据层重构，做了三处改动：① `VendorSignals.java` 的 `UTIL` 从 `CarS05InfoUtil` 改成 `S05VehicleDataMonitor`（`psGetValueSync` 变异步 `psGetValue`，置空走缓存+实时监听）；② `inject_manifest.py` 注入 dex 改**自动挑下一个 classesN.dex 槽位**（新底包已 multi-dex，塞成 `classes3.dex`，对旧包也兼容）；③ `build_local.py` 临时 dex 改中性名。本地真编出 **221 / `v26.0923-cd1`**（`ROUNDTRIP_OK` + 签名一致 + 43 个类注入 classes3.dex + 5 个组件齐全）；提交 `5e63f32` 并 push。**产物出两份**：`新-Deepal-CarDash-221.apk`（装这个）+ `旧-Deepal-CarDash-219.apk`（cd24 回退包，新包闪退就装回去）。导航 IPC / 车机高德广播那套反射目标经核对没变；`S05VehicleDataMonitor` 是否还被桌面启动、`cacheCarS05Info` 是否还刷新需上车确认。§6 新增第 70 条 |
| 2026-09-24（二十） | **换回本机（`C:\Users\Administrator\Desktop\IPA`，用户 `Administrator`，Python 3.12.10）后的复核**，按 §7 清单走一遍：① `git status` 看到 cd21 已删掉的三个音乐源码（`MediaSignals/Lyrics/LauncherLyrics`）**被文件夹拷贝带了回来**（untracked，且引用已删除的 `StateHub.mTitle/mCover/mediaError`，车机包会编不过）→ 删掉；② `.git/config` 的 `core.sshCommand` 残留办公机路径 `D:/Desktop/IPA/…` → 跑 `_tools\setup_git.py` 修好、`ls-remote` 认证通过（拿到 `4e88ee5` = 本地 HEAD）；③ 本地重编 `219/cd24`（`ROUNDTRIP_OK` + 证书与车机系统签名一致），与根目录交付包**逐条目内容比对 1656/1656 完全相同、解压后总和都是 17,352,109** ⇒ 源码/工具链/交付包三方一致。同时纠正一个判据误区：**整包字节比对不能跨机器用**（Python 版本决定 `zipfile` 压缩级别 + APK v2/v3 签名随机），§6 新增第 69 条、§7 第 0 步补记本次结论。未动任何代码、未出新包 |
| 2026-09-24（十九） | 用户问「要抓的 log 我能直接在 App 里把车机诊断四项整合发给你吗？」—— 顺着这个问题自查，**发现一个我自己的失误**：`DestSignals.report()`（目的地的来源/名称/坐标/命中规则/命中的原始日志行）**写了却没有任何调用点**，是死代码 ⇒ 文档 §0.1 让用户"去 `/logcat` 看目的地那一段"，而那一段**从来没出现过**（用户照做只会看到空白，还可能误判成"没识别到目的地"）。**车机 cd24（219）**：`logcatReport()` 新增【目的地（高德广播=权威/语音=兜底）】段，`heartbeatText()`（`/diag`）的实时数据里加一行「目的地 名称 坐标 [来源] 可下发/不下发」；**iOS 1.6.24**：诊断按钮从 4 个加到 5 个（新增「状态快照（JSON）」= `/state` 原始 JSON），footer 写明"排查导航问题发 logcat + state 两份"。本地真编 cd24 通过（`ROUNDTRIP_OK` + 签名一致）、三项自检全绿；提交 `4e88ee5` 并 push。§4 的 §0.1 改成"App 里点按钮复制"的实际用法（旧浏览器做法折叠留档）；§6 第 68 条补第 4 条自查项（"新增的诊断输出要 grep 调用点、并在真机上确认有内容"）|
| 2026-09-24（十八） | 用户提出方法论要求：「**你要学会自查可能出现的 bug，而不是我去提醒你才查，要结合生活习惯**」。于是按他的用法做了一轮主动自查（不看编译绿、只问"他这么用会怎么翻车"），抓到三个**平时看不出来、在他的用法下才炸**的 bug，出 **iOS 1.6.23**：① `Info.plist` 声明了 `UIBackgroundModes: location`，而 `LocalSensors` 里 `allowsBackgroundLocationUpdates = false` —— 声明等于白写，切后台/手动锁屏后 iOS 会挂起 App，**轮询和导航引擎一起停、仪表冻在最后那一帧**（`isIdleTimerDisabled` 只防自动锁屏，防不住切后台）；改成 `true` + 显式关掉蓝条。② `poll()` 只看 `inFlight` 布尔，而它只在完成回调里清 —— 回调万一没执行就**永远卡在 true ⇒ 整个仪表再也不刷新**，且不报错、界面看着还活着；加 3 秒时间闸门兜底。③ `syncDestination()` 只在拉取成功时才跑 ⇒ **熄火下车、离开热点后 `routeDest` 一直留着**（人都进楼了手机上还在导航）；看门狗加"失联 >150 秒就收掉"（150 秒是留给行车途中热点打嗝的余量，模拟导航模式不受影响）。自检三项全绿；提交 `ce8ab55` 并 push。**文档新增 §13「习惯驱动的自查清单」**（13.1 由习惯反推的 12 条要求 / 13.2 每次必答的五个问题 / 13.3 两条硬规矩），§6 新增第 68 条 |
| 2026-09-24（十七） | 用户补充习惯：「**原厂导航和第三方导航一起开**，会不会重复获取目的地一直跳」。查出来一个真问题：写「剩余多久/多远/转向」的有**四路**（高德广播 / 通知栏 / 读屏 / 车身信号 `Navigation/NaviInfo*`），以前只有"通知栏压读屏"一条规则，其余谁也不让谁 ⇒ 两个导航同时开时顶栏数字在两条不同路线的数据之间来回跳。**车机 cd23（218）**：新增 `StateHub.navClaim(src)` 统一仲裁（高德广播 3 > 通知栏/读屏 2 > 车身信号 1；当前来源 5 秒内还推过就让位；被压住写进 `src.navIgnored` 留证据），四处写入点全部接上，删掉原先那段只压读屏的局部规则；`DestSignals` 增加 `src.destVoiceIgnored`（权威源在位时把被忽略的语音目的地记下来，用来回答"两个导航一起开会不会被改目的地"）。目的地本身**不参与**这个仲裁 —— 只有两路，且权威源在位时语音那路完全锁死。本地真编通过（`classes.dex` 与基座一致、`classes2.dex` 158464→158968 字节，改动确实进了产物）、`ROUNDTRIP_OK` + 签名一致；提交 `d4e026a` 并 push（只动 `inject/`，不触发 iOS CI）。§6 新增第 67 条；§4 新增 **§0.1「上车要抓的 log」**（A~F 六条，A 是手动点导航时抓 `/logcat` 看高德广播的真实 key）；待办改成"待实车抓 log 验证" |
| 2026-09-24（十六） | 用户两件事：①「把已知和有风险的 bug 都改掉」②「车机端**手动导航** IPA 也要识别（不是只语音），最终目的：**中间不中断导航、不导航到其他目的地**」。**先做了一轮只读排查**（发现长途必掉导航是必然，不是可能），然后一次改完：**车机 cd22（217）** —— `DestSignals` 新增「高德车机版广播」权威目的地源（在 `后装导航.apk` 的 dex 里挖到 `TO_POI_NAME/LATITUDE/LONGITUDE` 一族字段与三套命名，归一化比对；只扫路线载荷、绝不扫搜索结果；绝不认 FROM_/START_），权威源在导航会话期间一直有效（不再 30 分钟墙钟过期）、同地点 100 米内不动坐标、权威源在场时语音那条完全不参与、只有名字没有坐标一律不下发；`AmapSignals` 修 `lastIntent` 从未赋值（诊断 dump 一直是空的）、新增 `navigating()`（巡航也发引导广播，不能用它当"在导航"）与 **90 秒去抖的会话看门狗**；`StateHub` 的 `navFresh` 15s→60s（单帧 false 会让 iPhone 直接 `stopNavi()`）；`/state` 的 `dest` 增加 `src`。**iOS 1.6.22** —— `Dest.isUsable` 改成必须有合法坐标（去掉地理编码路径）、`routeKey` 只按坐标去重、目的地暂时拿不到时**去抖 20 秒**才收路线、设置页显示「目的地 + 来源 + 坐标」。本地真编 cd22 通过（`ROUNDTRIP_OK` + 签名一致）、三项自检全绿；提交 `adf3faa` 并 push。**随后自查发现一处必须收紧**：`classify()` 会对**任何**未命中的 key 做 JSON 递归 ⇒ 等于又去扫了 `EXTRA_POI_RESULT` 搜索结果载荷（用户只搜过、没去导航也会被当成目的地）—— 已改成"直接 extra 只认确切字段名、递归只从路线载荷 key 进入、并支持嵌套 Bundle"，提交 `5a1ae2f` 并 push（只动 `inject/`，不重复触发 iOS CI）。§6 新增第 63~66 条；§11 补「两个导航 App 别搞混」；§4 新增 §0.0「上车就验这三件事」；待办 1、2 标记为已做（待实车验证） |
| 2026-09-24（十五） | 用户报「**车标那圈绿色呼吸看不见，只有一个绿点**」。根因**不是没画**，是数值把它淡没了：光晕画在实心圆**下面**，半径 8.25~17.25 里小于白圈（11.5）的那段**整段被盖住**，露出白圈之后 `easeOut` 已经把透明度吃到 ~8%（填充本来就只有 0.30）。**iOS 1.6.21**：`scale 0.55→1.15` 改成 **0.8→1.7**（起点就从白圈外沿冒出来、一路扩到 r≈25.5），填充 `0.30→0.50`，`side 34→64` + 显式 `clipsToBounds = false`（装得下扩到最大的光晕，不怕被裁）；顺手把三层位置从写死 `side/2` 改成按**当前 bounds** 现算（`layoutDots()`，视图尺寸是高德管的，写死会偏）并补**动画自愈**（`didMoveToWindow` + `didBecomeActive` 各补一次 —— 切前后台/高德回收视图会把 CA 动画丢掉，且不报错）。CI 前自检全绿。**车机端没动，仍是 cd21**。§6 新增第 60、61 条（"动画看不见先算曝光量"、"CA 动画会被系统丢掉"）。**CI 已出包：`build-60` → `e1d451b`，Release `CarDash IPA 1.6.21 (build 60)`，用户已下载** |
| 2026-09-24（十四-补） | 🐞 **push 报 `connect to host github.com port 22: Connection timed out`**：这台机器网络封了 22 端口（15 分钟前 `ls-remote` 还通，转眼就 timeout；同刻 `ssh.github.com:443` 秒通、认证成功）。修：`_tools\setup_git.py` 固定带 `-o Hostname=ssh.github.com -o Port=443`（remote URL 保持 `git@github.com:…` 不变），并自动把 `[github.com]:443` 的 host key 按 github.com 那把密钥补进 `known_hosts`（不盲信首连）。已 push `2b09c41..e1d451b`。§6 新增第 62 条；§7 第 2 步、§9「push 报错怎么办」都补了这一条 |
| 2026-09-24（十四） | **换机器后的环境复核**（本机 `d:\Desktop\IPA`，用户 `梵夏`，不是文档里的 `Administrator`）：`CarDash\.git\config` 的 `core.sshCommand` 又残留旧机绝对路径 ⇒ 跑 `_tools\setup_git.py` 修好，认证实测通过（拿到 `refs/heads/main`）；机器上是 **Python 3.14.5**（非 3.12）+ `cryptography/pyaxmlparser/pyyaml/pillow` 齐全，4 个自检脚本实测都能跑；**本地真编一次车机包 `216 / v26.0521-cd21`** 验证链路 ⇒ `ROUNDTRIP_OK` + 证书 `SHA-256 c8a2e9bc…92ab8` 与车机系统签名一致 + 产物与根目录 `Deepal-CarDash-216.apk`（1656 条目、dex/清单/arsc）**逐字节相同**；用 `git ls-remote --tags` 核对远端确认 **iOS 1.6.20 CI 已出包**（`build-59` → `2b09c41`）。🐞 **修 `_tools\check_swift.py` 的 GBK 崩溃**：第 4 项检查标题里的 `⇒` 在中文控制台编不了 ⇒ `UnicodeEncodeError`，而前 3 项照常打印完 —— **看起来跑完了，实际最关键的 ObjC→Swift 命名检查一次都没跑**；开场加 `sys.stdout.reconfigure(errors='replace')`（不改编码、不损中文）。第 6 节新增第 59 条；第 7 节新增「第 0 步 本机已复核结论」；§4 现状与版本表改成实测结果 |
| 2026-09-24（十三） | 用户实测 1.6.19 反馈「非导航态看不到绿点」⇒ 定位到 1.6.19 删自绘路线时**删多了**：车标（`carPin`）创建代码夹在路线绘制块与目的地 pin 之间，被区间删除一并带走（**编译与静态检查都发现不了**，因为声明与 `viewFor` 都还在）。补回并出 **iOS 1.6.20**（提交 `2b09c41`）。§6 新增第 58 条：整块删代码后必须**逐条功能过一遍**（车标/路线/目的地 pin/放大图/接口读取），别只看编译绿；1.6.19 那一轮加了补丁说明 |
| 2026-09-24（十二） | 用户今晚最后三条。**iOS 1.6.19 + 车机 cd21**：① 删掉自绘路线整块（`AMapNavView` 的 polyline 绘制、`NavMapView` 兜底路线、`DashboardModel` 的 `amapRoute`/`appleRoute`/`parsePolyline`/`statusIndex`/`routeBearing`/`route`/`routeSegments`、`RouteSegment`；`syncDestination` 只留"解目的地坐标"）—— 导航已用高德官方，白画还费电；② 删掉车机音乐整块（删 `MediaSignals.java`/`Lyrics.java`/`LauncherLyrics.java`，`StateHub` 去掉音乐字段与 `"music"` JSON、`mediaError`，`BridgeRuntime`/`StatusPage` 同步清理；iOS 去掉 `MusicState`/`displayMusic`/`MusicPanel`）；③ 修「呼吸光晕没对准圆心」（`CAShapeLayer` 的 bounds/position 默认 0 ⇒ 缩放绕了父层左上角）。第 6 节补第 56~57 条（含"脚本整块删代码要立刻自检"的教训）；`Deepal-CarDash-216.apk` 已在根目录；提交 `17650b7` 并 push |
| 2026-09-24（十一） | 用户要求车标要有苹果「查找」那种呼吸动效，并要求把文档整理成"明天读一遍就能开工"。**iOS 1.6.18**：车标改成自定义 `MAAnnotationView` 子类 `CarDotView`（三个 CAShapeLayer：光晕做放大+淡出无限循环的呼吸、白圈、实心圆；绿=在线/灰=不在线；动效走 Core Animation 不占主线程）。**文档**：§4 顶部新增「下一步（2026-09-25，办公室/车上）」—— 装什么、车上逐条验什么（含"车机导航终点 → iPhone 自动开始导航"这个重点项）、不对时发回什么、还剩哪些欠账、干活方式与老坑；第 6 节补第 55 条；提交 `1bb1bba` 并 push |
| 2026-09-24（十） | 用户带截图反馈两点。**iOS 1.6.17**：① 修「进软件车标先居中、几秒后才归位」—— 非导航地图靠 `MAMapView.convert()` 反算偏移，地图未渲染好时返回值是垃圾，老代码直接放弃偏移 ⇒ 车停在正中；现在改成"返回成功与否 + 排队重试"（`reapplyCamera`，0.1~0.8s 退避最多 8 次），横竖屏同一处一并修好。② 修「模拟导航闪一下北京」—— 导航起步前 `AMapNaviDriveView` 显示 SDK 默认位置，改为起步前**垫一张非导航地图**、`didStartNavi`（新增 `onStarted` 回调）后再切导航视图，目的地清空时重置。第 6 节补第 53~54 条；顺手把进度节里两个过时的"最近一轮"标题改成"更早"；提交 `180c600` 并 push |
| 2026-09-24（九） | 用户给官方《导航实时数据获取》文档 + 一张路口放大图的参考图，要求接入并规定"盖在速度上、只遮速度"。**iOS 1.6.16**：`NaviKitNavView` 设 `showCrossImage = false`（头文件：设 YES 则回调里图为 nil）、`NaviCoordinator` conform `AMapNaviDriveDataRepresentable` 并 `addDataRepresentative(self)`，实现 `showCrossImage:`/`driveManagerHideCrossImage:` 两个回调；新增 `SpeedSlot` 把速度表与放大图做成同一格（图高 150*scale < 速度表 181*scale ⇒ 布局不抖、不挡别的元素），竖屏居中/横屏贴左。第 6 节补第 50~51 条；提交 `4027ae6` 并 push。车机端无改动，仍 cd20 |
| 2026-09-24（八） | 用户反馈 CI 失败 + 提问"红色脉冲到底按什么触发"。**修 CI**：资源目录一旦存在 actool 就会跑，而 XcodeGen 默认要求 `AppIcon` 图标集 ⇒ 补 `AppIcon.appiconset`（深色底+深蓝车标，1024×1024 不透明，`%TEMP%\mkicon.py` 生成），App 从此有图标。**改红色脉冲语义**：用户指出高德是"会被拍限速的电子眼"才冒红，不是超路段限速就冒 ⇒ 车机侧 `AmapSignals` 解析并下发 `CAMERA_DIST/TYPE/SPEED`、`StateHub` 加 `cameraDist/cameraType/cameraSpeed` 与 JSON `"camera"`；iPhone 侧加 `CameraInfo`、`DashboardModel.displayCamera`，`isOverspeed` 改为"前方 300m 是测速类电子眼（官方枚举 0/8/9 或自带限速）且超过该眼限速 +2"。车机同步出 **215 / cd20**（`Deepal-CarDash-215.apk`）。第 6 节补第 47~49 条；待办 7 补电子眼实测项；提交 `251765b` 并 push |
| 2026-09-24（七） | 用户模拟导航实测后提七条。**iOS 1.6.15**：`showTrafficLightView`/`showOverSpeedPulse` 两个收费接口按要求关掉（只用官方免费功能）；修「模拟导航先显示北京」（`NaviCoordinator.update` 改成先建引擎再喂定位，且模拟开始前允许喂一次）；非导航车标改绿/灰实心圆（`AMapSupport.Coordinator.carDot`，不需要图片资源）；修地图卡顿（`poll()` 先比后写 + `layoutSubviews` 加 `lastSize` 闸门）；`NavSummaryPanel` 的 ETA 文字从 0.70 提到 0.9；新建 `Assets.xcassets` 并放入官网抠图的深蓝车标 `DeepalLogo`。装 Pillow 用于抠图。第 6 节补第 42~46 条；待办 8 标记完成；提交 `63ad59b` 并 push |
| 2026-09-24 凌晨（六） | 用户模拟导航后提六条。**iOS 1.6.14**：`showGreyAfterPass`（走过的路置灰——默认 NO 所以一直绿的）、`showCameraDistance`、`showTrafficLightView`、`showOverSpeedPulse` 开关；限速经车机高德广播下发 + `DashboardModel.isOverspeed` + `EdgeGlow` 红色边缘光（不依赖收费接口）；转向灯 `EdgeGlow` 绿色闪烁；Info.plist 加 `CADisableMinimumFrameDurationOnPhone` 放开 120Hz；启动遮罩改"外圈转 + 车标呼吸"循环动画（`DeepalLogo` 素材槽）。**车机 cd19**：`VendorSignals` 采集 `Light/Turn*`（新方法 `applyTurn`/`turnBool`）、`AmapSignals` 下发 `LIMITED_SPEED`、`StateHub` 加 `speedLimit`/`turnLeft`/`turnRight`/`turnStatus`/`turnValue()` 与 JSON `"limit"`/`"turn"`。第 6 节补第 37~41 条；待办补 7~9 条；提交 `75b4116` 并 push；车机包 `Deepal-CarDash-214.apk`（cd19）已本地构建并放在根目录 |
| 2026-09-23 深夜（五） | CI 挂了一次（`a7092f8` 编译失败，整包没出来）：`NaviCoordinator.update` 的形参 `view` 遮蔽了同名属性，`if let v = view` 编不过。修 `500bcc8` 并 push（形参改名 `naviView`）；`_tools\check_swift.py` 增加静态检查专门抓这类错（非可选形参被 `if/guard let` 绑定 = 报错），第 6 节补第 36 条；待办加第 6 条（`driveViewEdgePadding:` 防遮挡机制，需看屏幕调）；版本仍是 `1.6.13`（失败的那次没出包，不重复跳号） |
| 2026-09-23 深夜（四） | 用户实测 1.6.12：横竖屏切换后车头「先错位、等一会才归位」，并要求加启动动画 + 模拟导航照官方做。**iOS 1.6.13**：给两个地图视图套子类在 `layoutSubviews` 里即时重算位置（根因：SwiftUI 旋转不调 `updateUIView`）；加 `LaunchOverlay` 启动遮罩（1.8s+0.5s 淡出）；模拟导航改用官方 `startEmulatorNavi`+`setEmulatorNaviSpeed(60)`，且模拟期间不喂 `setExternalLocation`；顺手修掉"算路 key 含起点 ⇒ 每 5 米重算一次"；第 6 节补第 32~35 条；提交 `a7092f8` 并 push。**车机端仍没动，APK 仍 cd18** |

---

## 13. 习惯驱动的自查清单（每次改动后自己过一遍）

> 用户 2026-09-24 的原话：「**你要学会自查可能出现的 bug，而不是我去提醒你才查，要结合生活习惯**」。
> 下面这份清单是从他的实际用法反推出来的 —— 每次改完代码对着过一遍，别等他在车上撞见了再说。
> **新踩到的"习惯型"bug 也要补进这张表**（这才是它的用法）。

### 13.1 用户是怎么用这套东西的（事实，不是猜测）

| 习惯 | 对代码的要求 |
|---|---|
| **原厂导航 + 第三方高德同时开** | 导航数据多路来源必须**仲裁**（§6 第 67 条）；目的地只能认一路 |
| **长途连续开 10 小时+** | 不能有墙钟过期、不能单帧下结论、缓冲必须有界（§6 第 63 条）|
| **车机端手动点导航（全程不说话）** | 目的地不能只靠语音日志（§6 第 64 条）|
| **这块手机在车里一直当仪表** | 后台不能被挂起、屏幕不能自动锁（§6 第 68 条）|
| **中途切出去看别的 App / 手动锁屏** | 同上：`allowsBackgroundLocationUpdates` 必须开着 |
| **熄火下车就走**（走远离开车机热点）| 失联要能自己收尾，不能"人都进楼了手机上还在导航"（§6 第 68 条）|
| **每天多次点火/熄火** | 车机侧要靠 ContentProvider 自启；手机侧要能自动重连、重连后状态要干净 |
| **横竖屏来回切** | 依赖视图尺寸的东西必须用 `layoutSubviews` 补算（§6 第 32 条）|
| **可能在高德里"只搜不走"** | 搜索结果绝不能被当成目的地（§6 第 64 条）|
| **隧道 / 地库 / 弱信号** | 定位丢失不能把地图和导航拆掉（`coord` 拿到后从不清空）|
| **停车怠速（车不动）** | 静止时 GPS 航向是垃圾，不能拿来转地图（已有 `loc.speed > 1.0` 闸门）|
| **在办公室先试、再上车** | 没连车机时不能报错/卡住，得能退回本机 GPS |

### 13.2 每次改动后必答的五个问题

1. **「那个输入再也不来了，会怎么样？」** —— 依赖外部输入才能变化的状态，都要有超时/兜底。
   （实例：车机断电后 `routeDest` 一直留着 ⇒ 熄火后还在导航。）
2. **「声明和实现对得上吗？」** —— `Info.plist` 的每一项声明、注释里的每一句承诺，
   都去代码里找对应实现。对不上就是 bug。
   （实例：声明了 `UIBackgroundModes: location`，代码里 `allowsBackgroundLocationUpdates = false`。）
3. **「这个状态谁负责复位？复位路径断了会怎样？」**
   （实例：`inFlight` 只在完成回调里清 ⇒ 回调没来就永远卡住、整个仪表不再刷新，而且不报错。）
4. **「多路来源会不会抢同一批字段？」** —— 有第二个写者就要有仲裁和优先级。
   （实例：四路都写 `navEta/navRemain` ⇒ 两个导航一起开时数字来回跳。）
5. **「这个改动会不会让『暂时』变成『永远』？」** —— 去抖、粘性、缓存都要有上界。
   （实例：目的地从"30 分钟过期"改成"会话期间一直有效"，就必须配一个会话结束判据。）

### 13.3 两条硬规矩

* **改完不许只看"编译绿"**（§6 第 58 条）：逐个功能在脑子里过一遍 ——
  车标在不在？路线？目的地？放大图？限速/转向灯？切后台回来还在不在？
* **让用户去抓 log 之前，先问自己"他要是我，会怎么用、会在哪一步翻车"**，
  把那个场景写进 §0.1，下次直接照着验。

---

## 14. 独立包路线（不靠 D.apk）—— 2026-09-25 立项

> 用户原话：「我想的是咱们**完全独立出来，不靠 D.apk** 了 —— 不反射桌面内部类，
> 改成自己绑厂商 SDK（polymeric / virtualcar 的 AIDL）。与桌面彻底解耦、最稳，
> 但工作量最大（要逆 AIDL）。这条我们自己绑厂商 SDK 先做个测试出来，我去车上测试。」
> ⚠️ 这一节全部是**新增**的东西，**没有动过任何已有的注入代码/构建链路**。

### 14.1 为什么值得做

| | 现在（注入 D.apk）| 独立包 |
|---|---|---|
| 每次改动要做什么 | 改桌面包 + 重签 + 覆盖安装 | 装个普通 APK |
| 插件市场订阅 | 改了包 ⇒ 有风险（见 §0.0c） | **桌面一个字节不动 ⇒ 100% 安全** |
| 底包更新 | 要跟（类名/数据层变了就得适配）| 跟桌面解耦，但仍要跟**厂商 SDK**（它比桌面内部类稳） |
| 车机重启后 | 靠 ContentProvider 自启 ✓ | 同样能自启 ✓ |

### 14.2 三个关卡（哪些确定、哪些必须实测）

| 关卡 | 内容 | 状态 |
|---|---|---|
| **① 签名** | 独立包用**平台密钥**签 | ✅ **确定可行** —— 车机自身签名就是 AOSP 公开的 platform（README 已核实：md5 `8ddb342f…`），私钥在 `CarDash\keys\`。⇒ 车机管家会同样显示"此应用拥有系统签名" |
| **② 拿到 system uid** | `android:sharedUserId="android.uid.system"` | ⚠️ **必须真机实测**。README 里那句"Android 14+ 禁止非预装应用加入 android.uid.system"是**在模拟器上、同时换了调试签名**得出的，不能推论到车机（签名不符本来就不让加入）|
| **③ 读车身数据** | 不再反射桌面内部类，改绑**厂商 SDK** | ⚠️ 通路已探明（见 14.3），待测试包验证 |

### 14.3 厂商 SDK 的底细（2026-09-25 反汇编 D.apk 得到，权威）

**结论：桌面自己也是"绑一个服务 + 按属性 ID 取值"，这条路我们完全能复制。**

```
com.openos.virtualcar.VirtualCar                      ← SDK 入口
  ├─ static createVirtualCar(VirtualServiceReadyListener, Handler)   ← 只要 listener+handler，**没有 Context 参数**
  ├─ getVirtualCarManager()  → 内部向连接服务要 "virtualcar_property_service" 这个子服务
  │      （日志原话：getVirtualCarManager could not get binder for service:virtualcar_property_service）
  ├─ createVirtualCarManager(IBinder) → VirtualCarPropertyManager
  └─ getVirtualCarService()  → IVirtualCar

com.openos.virtualcar.VirtualCarPropertyManager       ← 真正的取值 API
  ├─ Object getValue(int id, int area)                ← 按属性 ID 取
  ├─ void   setValue(int id, int area, Number)
  ├─ void   register(int[] ids, VirtualCarPropertyCallBack)
  ├─ void   unRegister(int[] ids, VirtualCarPropertyCallBack)
  └─ boolean isSupport()

AIDL（都在 D.apk 里，有 Stub/Proxy，描述符如下）
  com.openos.virtualcar.IVirtualCar
  com.openos.virtualcar.IVirturalCarProperty            ← 注意拼写：Virtural（少了个 u）
  com.openos.virtualcar.IVirtualCarPropertyEventListener
  com.tinnove.polymericservice.IPolymericService        ← 聚合服务（按别名取值，就是 "ps"）
  com.tinnove.polymericservice.IPolymericListener / IPolymericEventListener

桌面的封装（我们现在反射的就是它）
  com.deepalhome.launcher.util.CarS05InfoUtil            （旧底包）
  …S05VehicleDataMonitor                                 （新底包）
  com.tinnove.comlib.virtual.VirtualCarManagerImpl       ← 它构造时就调 createVirtualCar(...)
```

其它线索：
* 日志里出现过 `com.wt.tinnovecoreservice.virtualcar.policy.property.VirtualCarPropertyServiceAdapterPolicy`
  ⇒ **`com.wt.tinnovecoreservice` 很可能就是那个"连接服务"的宿主包**（测试包会把它列出来）。
* `android.car.*`（AOSP 车控 API）在这台车上是**没有的**（`ClassNotFoundException`），别再往那边找。

### 14.4 测试包（已做好；**2026-09-25 起挪到 `_obsolete\`**，根目录只留当前要装的包）

| 文件 | 内容 | 用途 |
|---|---|---|
| **`独立包A-系统uid.apk`** | `com.cardash.solo` + **平台签名** + `sharedUserId=android.uid.system` | 先装这个：验关卡②③ |
| **`独立包B-普通.apk`** | 同上，但**去掉 sharedUserId** | A 装不上/装上没效果时的对照 |

两个都是 20 KB 左右，**不注入任何东西、不碰桌面**，装坏了直接卸载。
（文件位置：`_obsolete\独立包A-系统uid.apk` / `_obsolete\独立包B-普通.apk` ——
2026-09-25 清根目录时挪进去的，结果已经拿到，平时不用管它们。）

**2026-09-25 出货前对账（`aapt2 dump xmltree` 的实测输出，不是"应该"）**：

| 包 | `sharedUserId` | 证书 SHA-256 |
|---|---|---|
| `独立包A-系统uid.apk` | ✅ 有 —— 原文 `A: android:sharedUserId(0x0101000b)="android.uid.system"` | `c8a2e9bc…92ab8` |
| `独立包B-普通.apk` | ❌ 没有（对照包，故意去掉）| 同上 |
| `新-225` / `旧-224`（当时的包）| ✅ 有 —— 但**这是继承桌面的，不能当作关卡②的证据**（`新-227`/`旧-226` 同理）| 同上 |

> ⚠️ **别把"注入包一直有 system uid"当成关卡②已经过了**：`com.deepalhome.launcher` 是车机**预装的
> 系统应用**，我们做的是**同签名覆盖升级** ⇒ 系统让它继续待在 system uid 里。这跟"一个**新装**的
> App 能不能加入 `android.uid.system`"（A 包要回答的）**是两件事**。
> 真机实测里**唯一能作证的**是 A 包报告里的 `Process.myUid()` 是不是 `1000`。

**它做了什么**（打开 App 就自动跑，界面上一段文本，可复制，也可 `http://<车机IP>:8766/solo` 看）：
1. **身份**：包名、`Process.myUid()`（**是不是 1000**）、`sharedUserId`、`FLAG_SYSTEM`、
   READ_LOGS 有没有给 ⇒ 直接回答关卡②
2. **环境**：`android.car.*` / `com.openos.virtualcar.*` 这些类在不在（注意：**不在**是正常的，
   因为 SDK 在桌面包里；这条只是确认）；`ServiceManager.getService("virtualcar")` 等名字有没有 binder
3. **厂商服务清单**：列出所有包名含 tinnove/openos/virtualcar/polymeric/deepal 的应用，
   以及它们的 `<service>`（**名字 / exported / 需要什么权限 / process**）⇒ 这就是关卡③要绑的目标
4. **绑定探测**：对名字像 `virtual*/polymeric/vehicle/vcar/skill` 的服务逐个 `bindService`，
   报告能不能连上、`binder.getInterfaceDescriptor()` 是什么（**能连上就等于这条路通了**）
5. **logcat**：`logcat -d -t 3` 能不能出东西 ⇒ 只有 system uid 能过（关卡②的试纸）

**结果怎么读**（用户跑完把那段发回来即可）：

| 现象 | 说明什么 |
|---|---|
| A 装得上 + `myUid()=1000` | ✅ 关卡②过了，独立包能拿系统权限（logcat 也能读）|
| A 装不上（提示 shared user incompatible 之类）| ❌ 关卡②不过 ⇒ 只能用 B（普通应用），再想办法（push 进 /system、或放弃 READ_LOGS 那条）|
| 服务清单里能看到 `com.wt.tinnovecoreservice` 的 virtualcar 服务 | ✅ 关卡③的目标找到了 |
| 绑定探测里出现"连上了！+ 描述符" | ✅✅ **关卡③通了** ⇒ 下一步就把 SDK 类搬进独立包，直接 `getValue(id)` |
| 绑定全是"发起失败/没回调" | 目标不对或需要权限 ⇒ 换 B 包再试 / 按清单里的 `permission=` 去申请 |

### 14.4b ★ 真车实测结果（2026-09-25 11:42，用户把 A 包的输出整段发回）

**关卡② ✅ 通过 —— 这台 Android 14 允许"新装应用 + 平台签名 + `sharedUserId=android.uid.system`"**

| 检查项 | 实测值 |
|---|---|
| `myUid()` | **1000（system）** ← 关键 |
| `sharedUserId` | `android.uid.system` |
| `FLAG_SYSTEM` / `FLAG_UPDATED_SYSTEM_APP` | `false` / `false` ← **它不是系统应用**，只是"加入了 system uid" |
| `dataDir` | `/data/user/0/com.cardash.solo`（普通数据目录，没被特殊对待）|
| `READ_LOGS` | **有** |
| `logcat -d -t 3` | 退出码 0、**真的出了日志** ⇒ system uid 才有的权限确实生效 |

⇒ **README 里那句"Android 14+ 禁止非预装应用加入 android.uid.system"在这台车上是错的**
（那句结论来自模拟器 + 调试签名；而签名不符本来就不让加入，所以推不到车机）。
**独立包这条路上最大的不确定项消掉了。**

**关卡③ 通路已探明（方向定了，细节待搬类）**：

| 探索项 | 结果 |
|---|---|
| 类 `com.openos.virtualcar.VirtualCar` | **不在系统里** ⇒ SDK 类必须搬进独立包（或自己写 transaction）|
| `getService("polymeric_service")` | ✅ **有 binder** ← 聚合服务（按别名取值，就是 "ps" 那条）|
| `getService("car_service")` | ✅ 有 binder（AOSP car；但 `android.car.hardware.property` 在本车不可用）|
| `getService("virtualcar")` / `virtualcar_property_service` | `null` ⇒ **不是独立 servicename**，要从 polymeric 那条进 |
| `com.wt.tinnovecoreservice` | ✅ 在，`uid=1000(system)`，v1.8.0.25_beta（含 BootService 等）|
| bind `com.tinnove.wecarnavi/…SkillProviderService` | ✅ **连上了**：`com.tinnove.skill.api.ISkillProviderFactory` 和 `com.tinnove.navi.skill.IPCOwner` |
| bind `com.tinnove.systemservice/…CAVehicleNetworkService` | ✅ 连上了：`com.incall.serversdk.network.ICANetworkInterface` |
| bind `com.wt.tinnovecoreservice/…CommonKeyService` | ✅ 连上了（无障碍接口，不是车身数据）|

⇒ 最有价值的结论：**`com.tinnove.wecarnavi`（原厂导航）的 SkillProviderService 可以绑** ——
它就是注入包里 `S05Navi`（`m8.IPCSkillDispatcher$Stub$Proxy`）已经在用的那条。
独立包可以直接绑它拿原厂导航的目的地，**不用再反射桌面内部类**。

> ⚠️ 顺带一条实车观察（`新-225`/cd3 的诊断）：`s05ipc = dispatcher=…$Stub$Proxy bound=true
> started=true queryKey=cardash`，但 **`POI 指纹 / HUD 第一二行 / 导航中标志` 全是 `--`/false**
> ⇒ **绑上了但取不到值**。下一步要么修查询 key，要么直接调 `ISkillProviderFactory`
>（接口描述符已经拿到，反汇编 `wecarnavi` 就能看到方法表）。

### 14.4c 独立包怎么保活（用户问："这软件会在后台一直跑吗？D.apk 是怎么保活的？"）

**A 包（`com.cardash.solo`）现在只是个"探测器"：只有 Activity，没有 Service、没有
BootReceiver ⇒ 界面一关/被回收就没了，不会常驻。** 这是故意的（探测不需要常驻）。
想确认它在不在：`adb shell ps -A | grep solo`，或在车上重新打开看时间戳是不是新的。

**D.apk（`com.deepalhome.launcher`）的保活手段（2026-09-25 用 aapt2 dump 它的清单核对出来的）**：

| 手段 | 证据 |
|---|---|
| **它是系统桌面（HOME）** | 被系统反复拉起是常态 —— **这是最硬的一条** |
| `RECEIVE_BOOT_COMPLETED` + **3 个** `BOOT_COMPLETED` 接收器 | 开机自启（我们注入的 `BootReceiver` 也走这条）|
| `android.intent.action.MY_PACKAGE_REPLACED` | 覆盖安装后自启（注入包也在用）|
| 前台服务 | 清单里两个 `foregroundServiceType`：`0x2`(dataSync)、`0x8`(connectedDevice)（`LogcatMonitorService` 等）|
| `SYSTEM_ALERT_WINDOW` | 悬浮窗权限（配套手段）|
| 系统 uid | `uid=1000` ⇒ 不在"省电清理第三方"的常规范围 |
| ❌ `android:persistent="true"` | **没有**（而且非系统应用写了也会被忽略 —— A 包实测 `FLAG_SYSTEM=false`）|

⇒ **将来真做独立包，保活就照抄这份清单**：`BOOT_COMPLETED` + `MY_PACKAGE_REPLACED` +
`START_STICKY` 前台服务（`dataSync` 类型）+ 系统 uid。**不要指望 `persistent`。**
（现在注入进桌面的那份代码前 4 条都有 —— 这也是它一直很稳的原因。）

### 14.5 新装的工具链（以后逆 SDK 都用它）

* `python _tools/get_smali.py` ⇒ 下载 **smali / baksmali 2.5.2** 到 `_env\tools\smali-lib\`
  （Maven Central 通的；GitHub 那边超时没关系）
* 用法（只反汇编指定类，**不会**把整个 8MB dex 全铺开）：
  ```powershell
  $cp = (Get-ChildItem '_env\tools\smali-lib\*.jar' | % FullName) -join ';'
  & '_env\jdk\jdk-17.0.13+11\bin\java.exe' -cp $cp org.jf.baksmali.Main d -a 34 `
      --classes "Lcom/openos/virtualcar/VirtualCar;" -o <输出目录> <classes.dex>
  ```
  ⚠️ `--classes` 要写 **`Lcom/xxx/Yyy;` 描述符形式**，写点号形式它**静默什么都不做**（踩过）。
* 已反汇编出来的几份放在 `_tools\sandbox\sdk\`（VirtualCar / VirtualCarPropertyManager /
  ConnectionServiceBase / Monitor / CarS05InfoUtil），**留着当参考，别删**。

### 14.6 下一步（Phase 2 —— 关卡②③都过了，可以开工）

1. **搬厂商 SDK 的类**：baksmali 反汇编 `com/openos/**` + `com/tinnove/**`
   （+ 它们依赖的混淆类），只留需要的，smali 汇编回 dex 塞进独立包 ⇒ 直接调
   `VirtualCar` / `VirtualCarPropertyManager.getValue(id, area)`，不用自己写 transaction。
   ⚠️ `com.openos.virtualcar.*` **不在系统里**（A 包实测）⇒ 只能搬，反射系统类拿不到。
2. **聚合服务那条更省事**：`ServiceManager.getService("polymeric_service")` **直接有 binder**
   ⇒ 反汇编 `IPolymericService` 的 Stub/Proxy 就能自己写代理，按 `vc_alias_*` 取值
   （别名清单我们已有一份，在 `VendorSignals`）。**这条比搬 SDK 类更小、更快见效。**
3. **原厂导航那条**：独立包直接 bind `com.tinnove.wecarnavi/...SkillProviderService`
   （描述符 `com.tinnove.navi.skill.IPCOwner` / `ISkillProviderFactory`）——
   先解决"绑上了但取不到值"（注入包现在 `POI 指纹` 全是 `--`）。
4. **保活**：照 §14.4c 那份清单（`BOOT_COMPLETED` + `MY_PACKAGE_REPLACED` + 前台服务 + 系统 uid）。
5. 再往后：把 HTTP 服务（现在跑在注入代码里的那套）搬进独立包，注入包退休。
   ⚠️ 注意：**车身信号（车速/档位/续航/转向灯）现在还是反射桌面内部类读的**，
   独立包必须自己走 polymeric / virtualcar 才能拿到 —— 这是 Phase 2 最大的工作量。

---

## 15. 内置氛围灯（「桌面设置 → 氛围灯设置」）—— 2026-09-25 第六轮

### 15.1 用户要什么、为什么这么做

用户原话：「把之前我写的另外一个氛围灯控制的软件**内置到 D+ 桌面的桌面设置里**可以吗？
这样也能**确保保活**，我也不用单独装 apk 了」——左侧列表**最后一项**加「氛围灯设置」，
点开在**右侧区域**直接用，**功能一个都不能少**（截图上的红框 = 入口位置，蓝框 = 显示区域）。

原工程在 **`D:\Desktop\huancai`**（`MagicLantern`，包名 `com.magiclantern.rgb`，
27 个 java + 31 个 drawable，纯代码画 UI；之前已经内置进过高德地图一份，见
`work\merge_into_map.py`）。本轮**不直接改原工程**，而是把源码**拷进车机仓库**再改造：

```
CarDash\lantern\src\com\magiclantern\rgb\   ← 内置版（去资源表 + 面板化）
CarDash\tools\make_lantern_res.py           ← 生成 Res.java（drawable 内联）
CarDash\tools\port_lantern.py               ← R.drawable/setBackgroundResource 批量改写
CarDash\tools\refactor_lantern.py           ← 多监听 + 宿主类型/方法替换
CarDash\tools\check_lantern_res.py          ← 自检：31 段内联 XML 的语法（编译查不出，运行时才解析）
CarDash\inject\src\com\cardash\inject\LanternEntry.java   ← 运行时把入口挂进桌面设置
```

> 三个改造脚本都**幂等**（重复跑不会重复改），改完外观/换源工程时按
> `make_lantern_res.py` → `port_lantern.py` → `refactor_lantern.py` 顺序重跑即可。
> `check_lantern_res.py` 的输出：shape 13 / selector 2 / vector 15 / layer-list 1（31 个）。

**⚠️ 氛围灯原始工程已经归档进项目里了**：`<IPA>\_lantern_src\`（87 个文件 / 438KB）
= `MagicLantern\{src,res,AndroidManifest.xml,build.ps1}` + `ios\`（iOS 版氛围灯源码，
**没带 `.git` 历史**）。原始工程本身还在 `D:\Desktop\huancai`，但**换电脑只带 IPA 文件夹就够** ——
`make_lantern_res.py` 现在优先用 `_lantern_src` 里的 res 生成 `Res.java`（找不到才回退 `D:\Desktop\huancai`），
**已实测**：用归档素材重新生成的 `Res.java` 与现有那份**逐字节一致**（SHA256 `2E7C26E3…`）。

**从零重建内置版（比如以后氛围灯又改了界面）的顺序**：

```powershell
# ① 先备份 5 个"手写文件"（它们不在原始工程里，被 ② 覆盖就没了）
#    CarDash\lantern\src\com\magiclantern\rgb\ 下的：
#    LanternPanel.java / Page.java / MainActivity.java / Res.java / LanternBootstrap.java
# ② 同步原始源码进车机仓库
robocopy "<IPA>\_lantern_src\MagicLantern\src" "<IPA>\CarDash\lantern\src" /MIR
# ③ 把 ① 备份的 5 个文件放回去（Res.java 会被 ④ 重新生成，其余 4 个必须还原）
# ④ 重新生成资源 + 批量改写 + 自检
cd <IPA>\CarDash
python tools\make_lantern_res.py     # 无参数 = 用 _lantern_src 里的 res
python tools\port_lantern.py         # R.drawable → Res.*（幂等）
python tools\refactor_lantern.py     # 多监听 + 宿主替换（幂等）
python tools\check_lantern_res.py    # 31 段内联 XML 语法自检
```

> ⚠️ `port/refactor` 的规则是**对着 huancai 原始写法**写的（`MainActivity activity`、
> `R.drawable.*` 这些）。对已经改造过的文件再跑是幂等的；但**从归档原始 src 重同步**时，
> 上面那 4 个手写文件**必须先备份再还回去**，否则面板化/多监听那套改造会被原始版覆盖掉。

### 15.2 三个硬约束决定了实现方式

1. **我们的构建流程不能加资源**：`build_local.py` = 改 AXML + 追加 `classesN.dex` + 重签名，
   **不碰 `resources.arsc` / `res/`** ⇒ 原来那 31 个 drawable 不能走资源表。
2. **不能改原厂 smali**：设置页是 Kotlin 写的（`settings.SettingActivity` + `SettingsFragment`），
   我们只能**运行时挂载**。
3. **氛围灯自己的 Manifest 也不生效**（我们不是独立安装）⇒ 静态 receiver、
   `application` 标签之类都用不上。

### 15.3 怎么解决的

**(a) 资源去表化**：`make_lantern_res.py` 读 `MagicLantern\res\drawable\*.xml`，
把 `@color/*` 内联成字面量、selector 里的 `@drawable/*` 展开成内嵌节点，
生成 **`Res.java`**（31 个 drawable 的 XML 文本 + `Drawable.createFromXml` 解析 + 缓存）。
用法与原 `R.drawable` 一一对应：`Res.bg(view, Res.bg_card)` / `Res.setIcon(iv, Res.ic_arrow)`。

**(b) 面板化**：新增 **`LanternPanel extends FrameLayout`**，把 `MainActivity` 原来的
UI（顶栏 + 内容区 + 页面栈）全部改成**代码构建**（不再有 `activity_main.xml`），
Page 基类的宿主改成"双字段"：`host`（导航/状态）+ `activity`（Context，沿用旧名，
所以 7 个子页里原有的 `activity.xxx` 调用**几乎一行没改**，只有 8 个宿主方法改成 `host.`）。
`MainActivity` 变成 3 行的壳（独立 apk 仍可构建），**同一份界面逻辑两种承载方式**。

**(c) 进入点**：`LanternEntry.install()` 在 `BootProvider`（进程启动）里调一次，然后
用 `Application.registerActivityLifecycleCallbacks` 盯 `SettingActivity`；一 `onResume` 就：

* `findViewById("radioGroup")`（左列表，**是 `RadioGroup` + `RadioButton`**）——
  取最后一个原厂项当样板，**复刻外观**（字号/颜色/内边距/背景/尺寸 + 换成 `Res.ic_palette` 图标），
  造一个真的 `RadioButton` 加进去（id 用自留号 `0x0C4D0001`）；
* 点它（`setOnCheckedChangeListener`）→ `findViewById("settingsViewContainer")`
  （右侧 `FrameLayout`）→ `removeAllViews()` → `Ui.setScaleForContainer(容器宽高)` → addView 面板；
* 点回原厂项时**原厂自己 `removeAllViews`** 把我们的面板摘掉，面板 `onDetachedFromWindow`
  里解绑监听/广播（**蓝牙连接与渐变播放不动** —— 那是保活的活）。

> ⚠️ **两个 id 都是"先按名字查、查不到再用本基座的数值兜底"**
> （`getIdentifier("radioGroup","id",pkg)` / 兜底 `0x7f0b0551`；
> `settingsViewContainer` / 兜底 `0x7f0b05f2`）—— 换底包时资源 id 可能变，名字一般不变。

**(d) 尺寸**：`Ui.scale()` 原来按**整屏**算（设计基准 900×470dp）。嵌进设置页右侧后
容器只有整屏的一部分 ⇒ 新增 `Ui.setScaleForContainer(ctx, w, h)`，**在构造面板前**按容器算，
所以内容刚好铺满不溢出；面板被移除时 `Ui.clearScaleOverride()`。

**(e) 保活**：`LanternBootstrap.onAmapStart(app)`（原样保留这个名字，地图内置版仍在用）
在桌面进程启动时被调 ⇒ 常驻、自动重连灯、恢复上次渐变的播放。
本轮另外补了**定时开关灯的常驻接收者**：内置后清单里没有 `TimingReceiver`，
所以 `TimingReceiver` 的 PendingIntent 改成 **action + 包名**投递
（`ACTION_TIMING` + `setPackage`），由 `LanternBootstrap` 动态注册的接收者接住；
`BleController` 的单一 listener 改成**多监听**（`addListener/removeListener`，旧的
`setListener` 保留语义）——**界面与保活互不覆盖**。

### 15.4 上车怎么验 / 出问题怎么查

按 §0.0 第六轮那 4 条走。排查看 **`/log`（桥接运行日志）**，这几行是分界点：

| 日志行 | 含义 |
|---|---|
| `氛围灯保活已挂上（跟随桌面进程）` | (e) 生效 |
| `氛围灯设置入口已注册` | 生命周期回调注册成功 |
| `氛围灯入口已插入桌面设置（N 项）` | (c) 生效 —— N 应该 = 原厂项数 + 1（10 项左右） |
| `氛围灯面板已打开（容器 WxHpx）` | 点了入口、面板已挂上（W/H 是右侧容器实际像素，可核对缩放） |
| `氛围灯面板打开失败: xxx` / `氛围灯入口挂载失败: xxx` | 有异常，把那行原文发我 |

⚠️ 已知限制：**没打开过设置页 ≠ 氛围灯不能用**（保活是进程级的）；
但**面板本身必须有 Activity 宿主**才显示（`LanternEntry` 找不到 Activity 就放弃并记一行日志）。
另外界面外观来自**运行时克隆原厂项**，所以原厂改版式后我们的项会跟着变样式 —— 这是故意的。

### 15.5 下一轮可选项（等用户定）

**退出导航还差一点**：现在自动退是**15 秒**（`AmapSignals.GUIDE_END_MS`），
用户实测"能退，但要等一会"。本轮诊断证明 10001（引导广播）在退出后**确实完全停了**，
但**没抓到高德自己的"结束导航"日志原文**（取证段只有我们自己的那行）
⇒ 想更快只剩两条路：
1. **停车/低速时缩短窗口**（车速≈0 且 P 档 ⇒ 4 秒）：改动小、效果明显，
   代价是"停车长时间等待"时有极小概率被误判成结束（行车中保持 15 秒不动）；
2. 继续找高德的原话日志（需要用户下次退出导航后 **20 秒内**再抓一次诊断）。

### 15.6 线序 / 颜色错乱的坑（2026-09-25 晚 · 第七轮修）

**现象**：把颜色调成红色，灯显示绿色；去「设置 → 调整线序」改了**也没用**。

**根因**（**不是移植引入的** —— `LedCommand.java` 与原始工程逐字节相同）：
错在**下发时机**。线序、灯带点数是**写在灯固件里**的配置，灯一掉电/重连就回到默认线序；
而 App 只在「用户手动改设置那一刻」发过一次（`SettingsPage.showSequenceDialog` 的
`setSingleChoiceItems` onClick → `LedOutput.sendPinSequence`），**连接建立时从不补发** ⇒

* 重连之后颜色必错（灯回到默认线序）；
* 用户回设置里看，显示的还是他改过的值（存的是 App 侧 `magic_lantern` 这份 SharedPreferences）
  ⇒ 就变成"改了也没用"的错觉。
* 📌 **顺带解释了另一件事**：内置版的 `Prefs` 文件跟独立 APK 那份**不共享**
  ⇒ 内置版一上来就是**默认线序**（RGB），用户之前在独立版里调好的线序没带过来。

**修法**：新增 `LedOutput.reapplyDeviceConfig(c)`，连上后按"用户手动设一遍"的顺序补发：
**线序 → 灯带点数 → 开关 → 亮度 → 颜色**（发色用 `sendRawColor`，不打断正在播的渐变）。
两个调用点都要有：`LanternPanel.onStateChanged`（界面那条路）与
`LanternBootstrap`（常驻保活那条路）。

> ⚠️ **以后再加"设备侧设置"（麦克风灵敏度、通道开关之类）都必须加进 `reapplyDeviceConfig`** ——
> 凡是"存在灯里、掉电会复位"的设置，都要在每次连接后重放一遍。

---

## 16. 「卡片里嵌任意 App（比如抖音）」可行性调研 —— 2026-09-25 第六轮附带

> 用户问：桌面那三张卡片（截图）**点右上角三个点**，能不能加一项"显示其他应用在卡片内（比如抖音）"、
> 并且**能自己选**？用户明确说"**不用先改代码，先告诉我能不能实现**" ⇒ 本轮**只调研、没动一行代码**。

### 16.1 结论：能 —— 而且车机里已经有两套现成机制，卡在"权益/订阅"上

截图那三张卡片**不是桌面小部件**，是桌面的**窗口系统**（`com.deepalhome.launcher.window.AppWindowsManager`）：

| 屏幕位置 | 对应类 |
|---|---|
| 左（车辆信息 / 胎压 / 3D 车模） | `carinfo.CarInfoAppWindow` |
| 中（高德地图占位 + 包名） | `map.MapAppWindow`（`mapMenuPopup` = 它的三个点菜单） |
| 右（酷我音乐播放卡） | `music.MusicAppWindow` |

窗口体系里**已经实现了"能塞任意 App"的两类**：

1. **`customapp.CustomAppWindow`（自定义应用卡片）** —— 方法名就是说明书：
   `showAppMenuPopup(apps)` / `selectApp(App)` / `isSelectableApp(pkg)`（弹菜单选应用）、
   `startSelectedAppIfReady()` / `restartSelectedApp()` / `releaseEmbeddedApp()`（启动/释放内嵌 App）、
   渲染靠 `pip.VirtualDisplayHost`（**虚拟显示**），输入靠 `window.WindowEmbeddedInputAppWindow`
   （**嵌入式输入转发**）；配套 UI `pip.PipAppPickerDialog`（96dp 图标网格的**应用选择器**）也已经写好。
2. **`VideoFloat` / `CheyuVideo`（视频浮窗 / 车娱视频）** —— 从名字就知道是"在卡片里看视频"那类。

### 16.2 三道闸（解释了"为什么现在看不到/不能用"）

`AppWindowsManager.shouldShowWindowInfo()` 的反编译逻辑（原文照抄）：

```java
if ("CustomApp".equals(pkg))                       → CustomAppAuthorizationEnforcer（授权）
if ("VideoFloat"/"CheyuVideo".equals(pkg))         → VideoFloatPackManager.isEntitlementGranted()（插件包权益）
```

1. **权益 / 订阅**：`*PackManager` + `isEntitlementGranted` + 插件市场设置页里的
   `handleCustomAppActivityViewChange` ⇒ **这两个功能是插件市场里的插件**，没订阅根本不显示。
   ⚠️ **"插件市场能不能订阅"从第三轮问到现在一直没回报**，很可能它就是钥匙。
2. **行车安全**：`CustomAppWindow.isCustomAppAllowedByDrivingSafetyLimit()` +
   `handleDrivingGearChanged()` ⇒ 按档位限制（法规考量，**不建议绕**）。
3. **前提检查**：`hasVirtualDisplayViewPrerequisites()` ⇒ 车型/配置不支持时会 toast
   "需要 VirtualDisplayView"。

⚠️ **另外一条硬限制**：`selectApp` / `isSelectableApp` / `wrapMenuContent` / `addAppMenuDivider`
这些核心判断**是 native 方法**，而这个底包 apk 里**没有 .so** ⇒ 实现在系统分区里。
**我们能调用它，但改不动它的白名单。**

### 16.3 要用户做的验证（已在车上做，等照片）

1. 打开**插件市场**，看有没有「视频浮窗 / 车娱视频 / 自定义应用」这类插件 → 试订阅/激活（**截图**）；
2. **点卡片上的三个点** → 菜单**截图**（判断它是"音乐来源列表"还是"应用列表"）；
3. 在**添加卡片**处（长按桌面 / `AddWidgetDialog`）找有没有"自定义应用 / 视频浮窗"这类卡片类型。

### 16.4 如果插件市场那条走不通：自己做（可行，但是重活）

底牌是现成的：`pip.VirtualDisplayHost`（虚拟显示宿主）+ `com.wt.utils.WtActivityViews`
（厂商封装，类名就在包里）都在，我们跑在 launcher 进程里（system uid + 平台签名），
挂载手法可以复用氛围灯那套（`LanternEntry` 的思路）。

**但要如实评估**：虚拟显示 + Surface 合成 + **输入转发** + 生命周期 + 音频路由 + 性能
（车机 SoC 跑一个 1080p 虚拟显示再合成到桌面），比内置氛围灯大**一个量级**；
而且抖音是**竖屏** app，塞进横卡片两侧会留黑边。
⇒ **第一步应该先做一个"最小测试件"**：能不能在虚拟显示里拉起抖音、能不能点、帧率多少。
**没验证这一步之前不要开工。**

### 16.5 为这件事留下的素材（换电脑 / 新会话能直接接着干）

| 位置 | 内容 |
|---|---|
| `_refs\jadx_card\sources\...` | 已反编译的：`CustomAppWindow`、`AppWindowsManager`、`BaseAppWindow`、`MapAppWindow`、`WindowEmbeddedInputAppWindow`、`PipAppPickerDialog`、`VirtualDisplayHost`、`CustomAppAuthorizationEnforcer`、`WidgetInfo`、`AddWidgetDialog` |
| `_refs\card_scan.txt` | 卡片 / 窗口 / 隐藏 API 的类名扫描结果 |
| `_tools\scan_card_classes.py`、`_tools\scan_names.py` | 生成上面那份的脚本（`python _tools\scan_names.py <关键字>` 可继续扫别的） |
| `_refs\jadx_set\` + `_refs\res_dump.txt` | 设置页反编译 + 资源表 dump（氛围灯入口用到的 `radioGroup` / `settingsViewContainer` id 名就是从这查的） |

### 16.6 ⚠️ **已回退（第九轮）** —— 第七轮做的「应用小窗」验证件（包 234/235 起，238 起已删）

> **注意：这个验证件和它的 `AppWindowTest.java` 已经在第九轮删掉了** —— 用户决定
> 不做"卡片自定义显示 App"。下面整段是**方法留档**，不是现状；哪天要捡回来照着做即可
> （技术调研 §16.1~16.5 仍然有效）。**判读表原样保留**，万一重做还能直接用。

用户实测**插件市场里没有**能自定义卡片的插件（只有 HUD 转向辅助影像 ¥9.90 /
S05 后台管理 ¥36.00 / HUD 导航 ¥19.90 —— 见用户 2026-09-25 晚拍的插件市场截图）
⇒ 那条路断了，只能自己做。**但第一步不是做卡片，是先把技术路线验证掉**：

* **入口**：桌面设置 → 左侧列表最后第二项「**应用小窗**」（`AppWindowTest`）；
* **面板**：顶部**状态文本**（成败都写在这里，⚠️ 让用户拍照发回）+「选择应用 / 启动 / 停止」
  + 下方 SurfaceView（画面就投在这）；
* **逻辑**：`DisplayManager.createVirtualDisplay`（把 SurfaceView 的 Surface 交给虚拟显示）
  → `ActivityOptions.setLaunchDisplayId` 启动目标 App → 触摸按比例换算后用
  `InputManager.injectInputEvent` 回注（`MotionEvent.setDisplayId` 是隐藏 API 走反射，
  `InputManager` 是隐藏类整段反射）；
* **三条签名权限**已加进构建流程（`inject_manifest.EXTRA_PERMISSIONS`）：
  `CAPTURE_VIDEO_OUTPUT` / `INTERNAL_SYSTEM_WINDOW` / `INJECT_EVENTS`。
  ⚠️ 与文件里那条"别乱加权限"的警告**不冲突**：那说的是 POST_NOTIFICATIONS 这种
  "Android 11 上不存在 ⇒ checkSelfPermission 永远 DENIED ⇒ 每次开机弹授权页"的权限；
  这三条是**签名权限、在 Android 14 上存在**，platform 签名 + system uid ⇒ 直接授予。

**用户回图后照这张表判读**：

| 状态文本 | 含义 | 下一步 |
|---|---|---|
| `① 创建虚拟显示失败（多半是 CAPTURE_VIDEO_OUTPUT…）` | 权限或系统限制 | 查 `dumpsys package com.deepalhome.launcher` 里的权限是否真授予；再查虚拟显示数量上限 |
| `① 虚拟显示已建: id=N` + SurfaceView 里有画面 | **技术路线通过** ✅ | 可以做正式版（选 App + 卡片化 + 生命周期） |
| `② 启动失败（多半是 INTERNAL_SYSTEM_WINDOW…）` | 跨显示启动被拒 | 同上查权限；退一步试 `ActivityOptions.setLaunchTaskDisplayArea`（API 29+） |
| 有画面但**点不动** | 只有输入那条没通 | 看状态文本有没有 `触摸注入失败`（`setDisplayId` 反射被 hidden-api 拦就会打） |

> ⚠️ **它不是成品**：只是"能拉起来看看"的验证件 —— 没有卡片化、没有生命周期/音频/性能处理。
> **验证通过之前不要承诺做成正式功能**（工作量与风险见 16.4）。

### 16.7 ⚠️ **已回退（第九轮）** —— 第八轮做的「卡片上直接选 App」（包 236/237 起，238 起已删除）

> **注意：`CardAppEntry.java` 已在第九轮删除**，卡片上不再有「＋」按钮。
> 下面同样是**方法留档**。当时为它放开的两条签名权限也已收回
> （`CAPTURE_VIDEO_OUTPUT` / `INTERNAL_SYSTEM_WINDOW`）。

用户原话："不能直接让我在那三个卡片那里选择我要显示的 app 吗？不一定非得是抖音。"
⇒ 他要的是**原位**，不是另开面板。**已按这个改**：

* **怎么认出卡片**：桌面上那几张卡片就是 `*AppWindow` 类本身
  （`map.MapAppWindow` / `music.MusicAppWindow` / `carinfo.CarInfoAppWindow`，
  `extends BaseAppWindow`）。所以遍历 decorView，按"包名以 `com.deepalhome.launcher.`
  开头 + 类名以 `AppWindow` 结尾 + 宽高 > 220px"认出来，找到就不再往下钻。
* **入口为什么不塞进那三个点的菜单**：那是原厂 Kotlin 构建的原生 PopupWindow，
  我们拿不到它的菜单对象，塞不进去 ⇒ 改成**在卡片上加一个自己的小按钮**（`CardAppEntry`）
  —— 不碰原厂逻辑、位置就在卡片上、还能跟着卡片走（它是卡片的子 View）。
* **交互**：卡片右上角蓝色圆形「＋」→ 弹出**全部可启动 App**（按名字排序，不止抖音）
  → 选完就在**这张卡片里**跑起来；再点「＋」→ 列表第一项是「✕ 关闭，还原成原来的卡片」。
* **底层**与 16.6 的验证件同源（虚拟显示 + 跨显示启动 + 触摸回注），三件套权限一样。
* **日志关键字**：`[卡片入口] 找到卡片 N 张: MapAppWindow(…) MusicAppWindow(…)`、
  `[卡片入口] 虚拟显示已建 id=…`、`[卡片入口] 已启动 <包名> → 卡片`。

**第一版的已知不足**（等用户反馈再迭代）：卡片被拖动/缩放后 SurfaceView 不跟随；
没有音频处理；目标 App 退出/崩溃后卡片不会自动还原；竖屏 App 两侧会留黑边。
